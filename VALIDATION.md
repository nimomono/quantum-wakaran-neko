# 検算と品質確認

## 適用版

2026年8月1日、draft-12 の第II部を、旧終端関数・中央比較器模型から、構造化有限浴、3モード共通作用殻、全殻等方拡散、残余ファイバー体積を用いる模型へ置き換えた。本書はその改訂後の検証記録である。

## 数式・数値検算

実行コマンド：

```bash
python3 tools/verify_action_distribution.py
```

乱数種 `20260801`、標本数 `600000` で全10項目が合格した。

| 項目 | 値 | 合格条件 |
|---|---:|---:|
| 同一浴の正準1形式の最大誤差 | $7.1054\times10^{-15}$ | $\leq 5.0\times10^{-14}$ |
| 和・差作用恒等式の最大誤差 | $3.5527\times10^{-15}$ | $\leq 2.0\times10^{-14}$ |
| $S^5$ 一様測度における $J_+$ 周辺分布の KS 誤差 | $0.00153172$ | $\leq 0.003$ |
| 固定 $J_+$ ファイバー内の $U(2)$ 一様分配の KS 誤差 | $0.000848405$ | $\leq 0.003$ |
| $U(2)$ 不変反例の重み公式の誤差 | $0$ | $\leq 1.0\times10^{-14}$ |
| $U(2)$ だけではファイバー間質量を決めないことの歪み検出量 | $0.0191235$ | $\geq 0.01$ |
| Bell ファイバー共同確率の最大誤差 | $0.000100388$ | $\leq 0.0025$ |
| 非信号周辺確率の誤差 | $0$ | $\leq 0.0025$ |
| 狭い半径分布を持つ作用殻での線形平均則の誤差 | $0.000121049$ | $\leq 0.0005$ |
| CHSH 恒等式の誤差 | $0$ | $\leq 1.0\times10^{-14}$ |

検算は、次の区別を明示的に監査する。

- 固定 $J_+$ の残余2モードに対する $U(2)$ 等方性は、ファイバー内部を一様化する。
- $U(2)$ 等方性だけでは、任意の周辺因子 $f(J_+)$ を排除できず、異なる結果ファイバー間の相対質量を決めない。
- 3モード共通作用殻上の $U(3)$ 一様測度では、$p(J_+)=2(C_0-J_+)/C_0^2$ が得られる。
- 共通殻と結果適合条件の下で、残余ファイバー体積は $C_0-I_+^{AB}=J_*+I_-^{AB}$ に比例する。

## Markdown、章構成、生成物

- `sections/` の全13章別Markdownを `tools/build_paper.py` の検査へ通した：全て合格。
- 第6章を `06_three_mode_action_shell.md`、第7章を `07_boundary_action_shell_bell_measure.md` へ置換した。
- 旧終端関数・中央比較器模型は本文の現行模型から外し、`notes/superseded_terminal_function_model.md` に不採用案として保存した。
- 規約外の数式区切り、独自数式マクロ、生の定理環境、見出し内の数式区切り：なし。
- `paper.md`、`main.tex`、`paper.pdf` は同じ章別原稿から再生成済み。
- `paper.md` と標準Markdown生成結果：完全一致。
- `notes/` は生成対象に含まれない。
- `python3 -m py_compile tools/*.py`：成功。
- `git diff --check`：空白エラーなし。

## 参考文献

- 参考文献1〜33：全件が本文または付録から参照されている。
- 未定義引用：なし。
- 未使用項目：なし。
- Wallstrom 問題を未解決事項として明記し、対応文献 [19] を第8章から参照した。

## TeXとPDF

実行コマンド：

```bash
python3 tools/build_paper.py
```

- XeLaTeX 3回実行：成功。
- 未定義引用、未定義参照、内容が切れるはみ出し、致命的エラー、欠落文字：なし。
- `paper.pdf`：75ページ、A4。
- PDFメタデータの題名：章別原稿の題名と一致。
- CIの期待ページ数：75へ更新。
- `paper.pdf` の SHA-256：

```text
f51fb9a963b80171f1df0771c58c024f5fd71004bf62b1fcc2110ad391e48249
```

残る組版警告は `unicode-math` と `mathtools` の既知の命令上書き、および英語参考文献の一部に対する `Underfull` だけであり、内容欠落や表示の切れを生じない。

## PDF表示確認

最終生成物の全75ページを80 dpiのPNGへ変換し、4枚の一覧画像で全ページを確認した。主要ページは高解像度表示でも確認した。

- 表紙：draft-12の題名、副題、改訂稿表記に欠落なし。
- 目次：全8章と付録A〜Cを収録。
- 第5章：構造化有限浴、局所反応座標、共通境界セクターの記述に欠落なし。
- 第6章：3モード作用殻、$U(2)$ 不足の反例、$U(3)$ 等方拡散の記述に欠落なし。
- 第7章：押し出し測度、残余ファイバー体積、Bell 型共同法則、非信号条件の数式に欠落なし。
- 第8章：近似誤差、適用限界、証明状態、未解決問題に欠落なし。
- 付録C：正準基底変換、拡散生成子、coarea 計算に数式切れなし。
- 参考文献：33件を収録し、表示に欠落なし。
- 全ページ：文字化け、黒塗り、重なり、内容が切れるはみ出し、改ページの破綻なし。
- ヘッダー、フッター、ページ番号：全ページで整合。

## 再現性

`paper.md`、`main.tex`、`paper.pdf` は `sections/` から `tools/build_paper.py` で再生成できる。数値検算は固定乱数種を使用し、JSON形式で各判定値、閾値、合否を出力する。

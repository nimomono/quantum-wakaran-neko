@number: 2
@chapter: 本文
@title: 有限正準信号担体とM50共通読出し
@status: R112の有限正準担体、M35補助標本器、M50の作用殻状態数と有限熱化をQ1・Q2・Q3の共通部品として整理する。各系列の単一試行信号と物理周期は同一視しない。

## 2.1 共通代数と統一M0の違い

本章は、有限次元の複素信号を実正準座標で運び、排他的な有限枝へ受け渡す共通部品を定める。Q1はM47、Q2-1はM49、Q2-2はM48、Q3はM37からR167--R170へ進む。これらが共有するのは有限正準担体、作用殻状態数、有限熱化、局所記録の代数であり、同じ物理ハードウェアまたは同じ試行母測度ではない。

有限グラフを

```math
\mathcal G=(\Omega,E)
```

とし、各頂点 $z\in\Omega$ に実正準対 $(Q_z,P_z)$ と複素信号

```math
d_z=\frac{Q_z+iP_z}{\sqrt{2\mathcal J_0}}
```

を置く。$d$ は排他的な粒子位置ではなく、グラフ全体へ広がる有限正準担体である。各系列でその物理的由来は異なる。

| 系列 | M50へ渡す単一試行信号 | 排他的出力 | 現行役割 |
|---|---|---|---|
| Q1 | M47の信号bath座標 $Z(\omega)$ | 左右井戸 | 操作面ごとのBorn型再平衡化 |
| Q2-1 | M49の物理program担体 $d_{\rm prog}(\omega)$ | 4中央枝と2粒子位置register | CNOT共同入力--出力標本化 |
| Q2-2 | M48切断後の各翼の局所信号 | 各翼の2枝 | 条件付き局所再平衡化 |
| Q3 | M37標本包絡 $Z_{t_\star}(\omega)$ | 有限空間セル枝 | 固定入力時刻の位置instrument |

集団共分散 $C_Z$ は、この表の単一試行信号と同じ変数ではない。本稿の $C_Z$ は非中心化された規格化第2モーメントであり、中心化共分散と混同しない。Q3ではR167がM37標本集団をこの量へ持ち上げ、R168またはR169が、その集団記述をM50の単一試行入力契約へ接続する。統一M0には、これら全系列の信号保持、作用容量結合、衝突bath、記録、resetを一つの有限局所Hamiltonian周期へ統合する仕事が残る。

## 2.2 有限正準信号の辺生成子

全信号作用を

```math
J_{\rm sig}=\mathcal J_0d^\dagger d
```

とする。時間依存Hermitian行列 $h(t)$ に対するHamiltonian

```math
H_h(t)=d^\dagger h(t)d
```

は

```math
i\mathcal J_0\dot d=h(t)d
```

を与える。無向辺 $e=\{u,v\}$ には差モード射影

```math
\Pi_e
=
\frac12
(|u\rangle-|v\rangle)
(\langle u|-\langle v|)
```

と辺生成子

```math
G_e
=
\mathcal J_0d^\dagger\Pi_ed
=
\frac14
\left[(Q_u-Q_v)^2+(P_u-P_v)^2\right]
```

を対応させる。重み付きLaplacianは

```math
L_{\mathcal G}=2\sum_{e\in E}g_e\Pi_e
```

である。M49のCNOTは1本の辺射影を面積 $\pi$ で駆動する特殊例であり、Q3の空間伝播は多数の辺と頂点作用を同時に駆動する特殊例である。

**共通構造（R112：有限正準信号担体の共通辺代数）**

有限正準信号の可逆有効力学は、有限配置グラフ上の頂点作用項と差モード辺生成子の有限プログラムとして表せる。Q1、Q2、Q3の違いは、頂点集合、信号の物理的由来、係数、パルス列、排他的出力の実装にある。この共通代数だけから、粒子位置、確率分布、記録、resetは従わない。

R112は採用する共通有効モデルの定義である。設計済みの $QQ+PP$ 生成子と、M37の位置ばね網が有限時間で与える近似生成子は区別する。

## 2.3 M35有限基底補助モジュール

M35は、一般有限 $L$ の局所2モード回路、作用区間、滑らかな比較器、条件付き担体準備、内部逆計算を有限Hamiltonian化する補助モジュールである。選択器角が信号、基底回路、全作用に条件付けて一様なら、安全区間 $\mathcal O_i$ の長期頻度は対応する作用比に一致する。比較境界は正式な無反応結果 $\varnothing$ とし、無反応を除いて再規格化しない。

M49では、中央作用殻の枝内一様座標を作用区間ラベルへ押し出し、空の2粒子位置registerへ可逆SWAPする。Q3では、M35はM37包絡の有限基底分布を独立に検算する補助診断に限る。R170の主結果はM50で熱化した空間セル枝であり、M35作用区間を第2の結果として数えない。

M35自体は、作用殻fiber内のGibbs平衡化、長期頻度列の独立同分布性、二項型有限標本揺らぎを証明しない。一般有限 $L$ の複数段測定、永久外部記録、resetセル流、長距離時計配線を含む完全周期も未構成である。

## 2.4 M50の有限信号作用と枝容量

M50は、単一試行の非零有限信号 $v\in\mathbb C^m$、等長埋込み $\Psi:\mathbb C^m\to\mathbb C^L$、排他的枝 $i\in\mathcal I$ を結ぶ共通理論部品である。正の基準分布 $q_i>0$、$\sum_iq_i=1$ と正則化 $\delta>0$ を固定する。

```math
J_{\rm sig}(v)
=
\mathcal J_0v^\dagger v,
\qquad
J_i(v)
=
\mathcal J_0| (\Psi v)_i |^2,
```

```math
A_i^\delta(v)
=
J_i(v)+\delta q_iJ_{\rm sig}(v),
\qquad
\Omega_i^\delta(v)
=
\frac{(2\pi)^2}{J_{\rm ref}}A_i^\delta(v).
```

各枝の内部に2つの非負作用を置くため、殻状態数は容量に線形である。枝数が2であるとは仮定しない。単一Liouville母測度を1回だけ規格化すると

```math
\pi_i^\delta(v)
=
\frac{\Omega_i^\delta(v)}{\sum_j\Omega_j^\delta(v)}
=
\frac{|(\Psi v)_i|^2/(v^\dagger v)+\delta q_i}{1+\delta}
```

を得る。零信号または安全閾値未満の信号は $\varnothing$ へ送る。$\delta$ はnodeで有効自由エネルギーが発散することを避け、全信号rayに一様な有限混合率を与える。

作用殻を消去する表示では

```math
E_i^\delta(v)=-\Theta\log\pi_i^\delta(v)
```

を条件付き中間状態有効自由エネルギーとして使う。状態数を残す表示と消去表示は同値である。同じ縮約分配関数に $\Omega_i^\delta e^{-\beta E_i^\delta}$ を入れると状態数を二重計数するため禁止する。全系の平衡Hamiltonianを別に構成しない限り、$E_i^\delta$ を無条件に平均力Hamiltonianとは呼ばない。

## 2.5 M50の有限再平衡化と記録

有限連結枝グラフ $G_X=(\mathcal I,E_X)$ に対して、R161の平方根型率を

```math
k_{i\to j}^\delta(v)
=
\kappa_Xa_{ij}
\sqrt{\frac{\pi_j^\delta(v)}{\pi_i^\delta(v)}}
```

とする。$q_{\min}=\min_iq_i$、$a_{\min}=\min_{\{i,j\}\in E_X}a_{ij}$、無重みグラフLaplacianの第1非零固有値を $\lambda_G$ とし、

```math
m_\delta
=
\frac{\delta q_{\min}}{1+\delta},
\qquad
\lambda_\delta
=
\kappa_Xa_{\min}m_\delta\lambda_G,
```

```math
C_\delta
=
\frac12\sqrt{m_\delta^{-1}-1}
```

と置く。任意の初期枝分布 $p_0$ から時間 $\tau_X$ 後には

```math
D_{\rm TV}
\left(p_{\tau_X},\pi^\delta(v)\right)
\leq
C_\delta e^{-\lambda_\delta\tau_X}
```

が成り立つ。R162は、この率を有限衝突熱浴と履歴セルを持つ局所Hamiltonian散乱へ任意精度で近似する。固定した信号を保持するcontroller、作用容量結合、作用殻fiber、衝突bathは別の物理部分系である。

熱化終了後は枝間ゲートを閉じ、枝 $i$ に支持を持つ滑らかな局所関数 $d_i(x)$ だけを局所記録器へ結合する。空の記録運動量 $P_{D_i}$ に対する生成子

```math
G_{\rm rec}
=
\sum_i d_i(x)P_{D_i}
```

は、該当枝の記録だけを動かす。旧枝、時計、衝突履歴、記録を残せば拡大写像は1対1である。この操作は固定時刻の局所読出しであり、初回到達、吸収、時間積分流束を与えない。

## 2.6 Q3への使用規約

Q3では入力を読む時刻を $t_\star$、枝を記録する時刻を $t_{\rm out}>t_\star$ と書く。$t_\star$ にM37標本包絡を有限信号registerへ正準SWAPし、標本保持中にM50容量を準備して、R161/R162を有限時間だけ作用させる。粒子位置枝は $t_\star$ の前からM37作用比を追跡せず、$t_\star$ と $t_{\rm out}$ の間の連続軌道をSchrödinger型確率流と同一視しない。

R168は階数1支持により、各安全試行の信号rayが同じ統計因子を持つ場合を扱う。R169は各試行の全信号作用が固定される高階数の場合を扱う。作用が試行ごとに変わる一般集団では、試行ごとのray規格化と集団共分散の規格化が可換でないため、R169の結論を使わない。

Q3-4とQ3-5では、R170の読出し誤差を理想分布差より小さく選ぶ。R170は作用容量結合、殻内平衡化、信号保持、衝突bath、枝固定、局所記録を一つの有限局所Hamiltonianへ統合した定理ではない。この未統合部分のため、Q3-4とQ3-5の達成判定は条件付きとする。

## 2.7 共通部品と限界

| 共通部品 | 主な利用先 | 役割 |
|---|---|---|
| 頂点作用と差モード辺生成子 | M47、M49、M37 | 有限正準信号プログラム |
| M35作用区間と比較器 | M49、Q3補助検算 | 可逆標本化と無反応境界 |
| M50作用容量と2作用殻 | Q1、Q2、Q3 | Born型枝状態数 |
| R161/R162 | Q1、Q2-2、Q3 | 有限時間枝再平衡化と衝突実現 |
| 局所ラッチ、記録、履歴 | Q1、Q2、Q3 | 排他的結果の固定と可逆帳簿 |

一意エルゴードな内部角または有限熱化は、結果列の独立同分布性を与えない。集団共分散を物理制御器が読み取るとも仮定しない。現行主張は、固定有限入力、固定有限グラフ、固定有限時刻における条件付き分布と誤差上界である。

@number: 1
@chapter: 本文
@title: 記述層、測度、主結果の範囲
@status: M38の2モード完全装置、M37の空間振動子網、M35の一般有限基底装置、M36のBell 二側履歴を分離する。Q1-1は厳密、Q1-2からQ1-4は制御された任意精度、Q3-1は弱結合・有限時間で達成とする。Q2-2は部分達成であり、一般有限次元への拡張はM35の適用範囲と一般化課題として扱う。未構成のM0と統一母測度を現行定理の仮定に使わない。

## 1.1 問題設定

本論文の目的は、明示的な古典Hamiltonian 系から、量子力学に特徴的な可逆力学と確率構造が有効理論として現れ得る範囲を明らかにすることである。量子力学をミクロHamiltonian の入力には使わず、得られた力学、測定操作、統計を判定するときに比較対象として使う。

有限次元Schrödinger 方程式を実正準座標へ写すことだけでは、量子力学の創発にならない。本稿は次を分けて要求する。

1. $L=2$ の同じ正準担体上で、状態空間、可逆操作、Rabi 振動を構成する。
2. その担体を滑らかな測定後状態、逐次測定、記録、reset へ接続する。
3. 継続するRabi 駆動と有限回の反復測定を同じM38周期で接続する。
4. 空間Schrödinger 型包絡は、通常の位置結合を持つ有限実振動子網から誤差付きで導く。
5. 一般有限 $L$ の測定回路と完全周期を、$L=2$ の達成から分離して評価する。
6. Bell 統計は二側履歴支持に依存する条件付き模型として監査する。

M38は1--3、M37は4、M35は5、M36は6を担う。これらを1つのM0へ統合したとは主張しない。

## 1.2 5つの記述層

| 層 | 変数 | 主な法則 | 確立度 |
|---|---|---|---|
| M37ミクロ振動子層 | $q,p$ | 位置ばね結合 $H_{\rm micro}$ | 厳密 |
| M37局所有効包絡層 | $b$ | 反回転項を含む厳密式、$h_L$ による有限時間近似 | 近似誤差付き |
| M38の2モード装置層 | 2モード信号、単一測定コア、段セル列、局所時計、外部記録 | $SU(2)$、Rabi 振動、逐次測定、有限Zeno周期、記録、逆計算、reset | 厳密または明示誤差付き |
| M35の一般有限基底層 | $L$ モード信号、テンプレート、作用・記録レジスター、時計 | 隣接2モード回路、滑らかな比較、SWAP 、内部逆計算 | 固定プログラムで厳密または明示誤差付き |
| M36のBell 二側履歴層 | 局所結果角、未来角、終端支持 | 条件付き履歴測度 | 支持条件に依存 |

M37の正常モード包絡 $\widetilde b$ は、ミクロ振動子を解析するための厳密だが一般に非局所な正準変数である。独立した非局所物理場として追加しない。

M38とM35の任意複素基底変換は、局所位相回転と $QQ+PP$ 交換の有限列で実装する。$QQ+PP$ 交換は運動量間結合を含むため、M37の位置ばね網と同じハードウェアではない。Q1-1からQ1-4はM38内部で閉じ、M37との統合を達成条件へ入れない。

## 1.3 現行測度と将来の統一測度

理想有効担体の調製集団については

```math
C_M(t)
=
\mathbb E_{\mu_{\mathcal P,M}}
\left[
d_td_t^\dagger
\right]
```

と相関行列を定める。$\mu_{\mathcal P,M}$ は調製条件 $\mathcal P$ とプログラム $M$ を固定した集団測度であり、補助統計理論に使う。

M35の固定純粋準備 $\chi$ と固定基底 $W$ の周期では、

```math
d\mu_{\chi,W}^{\rm M35}
=
\frac{d\vartheta}{2\pi}
\otimes
\delta_*
```

がPoincaré 不変測度になる。$\delta_*$ は選択器角以外の内部準備集合を表す。

M38の固定有限 $N_{\rm Z}$ 段プログラムでは、

```math
d\mu_{\chi,N_{\rm Z}}^{\rm M38}
=
\frac{
d\vartheta_1\cdots d\vartheta_{N_{\rm Z}}
}{
(2\pi)^{N_{\rm Z}}
}
\otimes
\delta_*
```

が理想Poincaré 不変測度になる。$1,\alpha_1,\ldots,\alpha_{N_{\rm Z}}$ の有理数体上の1次独立性により、固定有限次元の角平行移動は一意エルゴード的である。Q1-2の2段測定は $N_{\rm Z}=2$ の場合である。

Bell 側では制約前基準測度を $\nu_{\rm B}^0$、二側整合事象を $G$ とし、

```math
d\mu_{\rm B}
=
4\mathbf1_G
\,d\nu_{\rm B}^0
```

を使う。

これらの測度は役割が異なる。可変調製、可変基底、空間伝播、Bell 境界、外部記録までを同じ反復系へ統合する将来目標の母測度を $\mu_*$ と書く。本稿は $\mu_*$ の存在、不変性、エルゴード性を現行定理の仮定に使わない。

## 1.4 主結果1A：2モード可逆力学

M38の固定作用面を

```math
I_{\rm ph}
=
\frac12
\sum_{j=1}^2
\left(
Q_j^2+P_j^2
\right)
=
\mathcal J_0
```

とする。共通位相に不変なBloch 写像により、有効状態空間は $S^2$ になる。Bloch 成分のPoisson 代数は

```math
\{r_i,r_j\}
=
\frac{2}{\mathcal J_0}
\epsilon_{ijk}r_k
```

である。任意の実ベクトル $\boldsymbol\Omega$ に対する

```math
H_{\boldsymbol\Omega}
=
\frac{\mathcal J_0}{2}
b^\dagger
\left(
\boldsymbol\Omega\cdot\boldsymbol\sigma
\right)b
```

は

```math
\dot{\boldsymbol r}
=
\boldsymbol\Omega\times\boldsymbol r
```

を厳密に与える。局所位相回転と2モード交換のEuler 分解により、任意の $SU(2)$ を同じ担体上で実装できる。

駆動作用・角変数を加えた自律Hamiltonian は、共鳴振幅、一般化Rabi 振動数、離調による振幅低下を厳密に与える。従ってQ1-1はM38内部で達成である。

## 1.5 主結果1B：任意軸と逐次測定

任意の Bloch 軸 $\boldsymbol n$ と結果 $s=\pm1$ に対し、M38の出力作用比は

```math
p_s(\boldsymbol n)
=
\frac{1+s\boldsymbol n\cdot\boldsymbol r}{2}
```

である。安全セクターでは正準SWAP 後の信号が厳密に $|\boldsymbol n,s\rangle$ となる。

滑らかな有限時間Hamiltonian 流の連続性により、連結入力領域全体を相異なる2固有状態だけへ写すことはできない。本稿は比較境界の遷移領域を正式な無反応結果とし、無反応率を

```math
\delta_j
\leq
2\frac{w_{{\rm eff},j}}{\mathcal J_0}
+
\varepsilon_{{\rm cut},j}
```

で抑える。

同軸再測定では反対符号の安全結果が生じない。異軸測定では独立な2選択器を使い、理想共同分布

```math
P(s,t)
=
\frac{1+s\boldsymbol n\cdot\boldsymbol r}{2}
\frac{1+st\boldsymbol n\cdot\boldsymbol m}{2}
```

を得る。無反応を含む実分布との距離は $\delta_1+\delta_2$ 以下である。従ってQ1-2は制御された任意精度で達成である。

## 1.6 主結果1C：永久記録と弱開放周期

滑らかな比較ポインター $\Pi_j(z)$ と空の外部記録セルを

```math
G_{\rm rec}
=
\sum_{j=1}^2
P_j^R\Pi_j(z)
```

で結合する。理想入口 $P_j^R=0$ では装置へ反作用せず、記録座標だけが移動する。記録後に第2段、第1段の順で逆計算すれば、内部装置は準備点へ戻り、記録は外部セルへ残る。

外部空モードとの正準交換は

```math
\delta a^+
=
\cos\phi\,\delta a^-
+
\sin\phi\,\eta
```

を与える。完全交換では装置偏差を空セルへ置換し、部分交換では収縮する。局所作用・角時計、外部記録セル、reset セル流を加えると、固定有限プログラムの能動装置は次周期の準備集合へ任意精度で戻る。

有限 $K$ 実験周期は外部セルを含む有限閉鎖Hamiltonian 系へ埋め込める。無期限運転では外部セル流が必要であり、情報と旧偏差を消去していない。従ってQ1-3は制御された任意精度で達成である。

## 1.7 主結果1D：有限反復Zeno効果

初期信号を $z$ 軸の正固有状態とし、任意に指定した有限回数 $N_{\rm Z}$ だけ $z$ 測定を繰り返す。第 $j$ 区間のRabi 反転作用比を $q_j$ とすると、全履歴分布は

```math
\pi_{N_{\rm Z}}
(s_1,\ldots,s_{N_{\rm Z}})
=
\prod_{j=1}^{N_{\rm Z}}
\left[
(1-q_j)\mathbf1_{s_j=s_{j-1}}
+
q_j\mathbf1_{s_j=-s_{j-1}}
\right]
```

である。一度も反転しない確率と最終正占有率は

```math
S_{N_{\rm Z}}
=
\prod_{j=1}^{N_{\rm Z}}(1-q_j),
```

```math
F_{N_{\rm Z}}
=
\frac12
\left[
1+
\prod_{j=1}^{N_{\rm Z}}(1-2q_j)
\right]
```

となる。無反応、反転、再反転を含む全履歴を保持し、生存試行だけを再規格化しない。

等間隔 $\tau$ の有効反転率は短時間域で

```math
\Gamma_{\rm Z}(\tau)
=
-\frac{\log[1-q(\tau)]}{\tau}
=
\frac{\Omega^2}{4}\tau
+
O(\tau^3)
```

となる。各構成は同じ総観測時間の未測定対照と比較するが、異なる $N_{\rm Z}$ の間で総時間を共通に固定しない。

実装は1個の測定コア、$N_{\rm Z}$ 個の独立選択器、有限長の循環メモリー／reset セル列、永久記録セルを使う。測定コアの補助6対だけを段セルへSWAPし、信号は次のRabi 区間へ残す。全結果を記録した後に逆順復帰し、選択器ベクトルを進める。

測定窓中もRabi 項を非零に保つ。幅 $h_j$ の有限パルスは理想測定写像から $O(h_j)$ だけずれ、無反応込みの全履歴分布誤差は各段の条件付き全変動誤差の和以下になる。固定有限 $N_{\rm Z}$ ごとにこの誤差と周期帰還誤差を任意に小さくできるため、Q1-4は有限Zeno抑制の範囲で達成である。完全凍結または $N_{\rm Z}\to\infty$ に一様な固定性能は含まない。

## 1.8 主結果2：局所振動子網からの空間包絡

M37の有限実振動子網を

```math
H_{\rm micro}
=
\frac{1}{2M_{\rm osc}}p^{\mathsf T}p
+
\frac12q^{\mathsf T}
\left(
M_{\rm osc}\omega_0^2I+A
\right)q
```

とする。局所包絡は反回転項を含む厳密式

```math
i\mathcal J_0\dot b
=
h_Lb
+
h_Le^{2i\omega_0t}\overline b
```

を満たす。厳密正常モード生成子と目標生成子の差は、$\eta=2\|h_L\|/(\mathcal J_0\omega_0)<1$ の下で

```math
\left\|h_{\rm ex}-h_L\right\|
\leq
\frac{\|h_L\|^2}
{2\mathcal J_0\omega_0(1-\eta)^{3/2}}
```

である。自然時間では局所包絡状態誤差を $O(\eta)$ で抑える。Q3-1は有限自由度、実対称時間非依存演算子、弱結合・有限時間の範囲で達成である。

## 1.9 主結果3：一般有限基底測定

M35は任意の有限 $L$、固定純粋準備、固定基底について、$3L+4$ 正準対の内部測定周期を構成する。任意基底変換を隣接2モード回路へ分解し、滑らかな安全結果、無反応、測定後状態、内部逆計算、無理数回転による長期頻度を得る。

Q3-1の規格化包絡 $\widehat b_{\rm mic}$ と目標状態 $\chi_L$ について、任意有限基底の作用比分布は

```math
D_{\rm TV}
\left(
p^{\rm mic},p^L
\right)
\leq
\sqrt{
1-
\left|
\langle\widehat b_{\rm mic},\chi_L\rangle
\right|^2
}
```

を満たす。この数学的インターフェースはM37とM35の同一ハードウェア化を意味しない。

一般有限 $L$ の複数段測定、永久外部記録、弱開放 reset、長距離時計配線は未完成である。一般有限次元への拡張は独立した長期目標とはせず、M35の適用範囲と一般化課題として残す。

## 1.10 主結果4：条件付きBell 二側履歴

左右の反対称集団交差相関を規格化すると、

```math
w_{AB}^{xy}
=
\frac14
\left[
1-AB\cos\Delta_{xy}
\right]
```

を得る。独立局所Haar 角の半円分割から4結果の基準密度 $1/4$ を得る。未来角が記録済み結果の余弦区間へ入る事象を $G$ とし、物理履歴を $G$ に支持させれば、余弦共同確率、非信号周辺、標準設定でのCHSH値 $2\sqrt2$ を得る。完全履歴測度は設定依存なので、Bell の測定設定独立性は成立しない。

単一試行変数からの余弦区間多重度、$G$ の物理的必然性、完全境界流束の因子化は未解決である。Q2-2は仮説依存の条件付き模型として部分達成である。

## 1.11 導出状態

| 主張 | 導出状態 | 主な制限 |
|---|---|---|
| M38のBloch 球と共通位相縮約 | 厳密結果 | 固定作用面、採用操作・測定代数 |
| M38の任意の $SU(2)$ | 厳密結果 | $QQ+PP$ と局所位相回転 |
| M38の自律Rabi 振動 | 厳密結果 | 固定2モード模型 |
| 任意の Bloch 軸の安全測定後状態 | 厳密結果 | 無反応込み |
| 同軸・異軸逐次測定分布 | 明示誤差付き結果 | 固定純粋入力、固定2軸、非混合 |
| 外部記録剪断と内部逆計算 | 厳密結果 | 理想空記録セル |
| 外部セル交換 reset | 厳密写像と誤差上界 | 外部セル準備幅、有限流路 |
| M38の完全周期 | 明示誤差付き結果 | 固定有限プログラム、弱開放 |
| 有限反復測定の理想履歴則 | 厳密結果 | 固定有限 $N_{\rm Z}$、理想瞬時測定 |
| 単一測定コアと循環セル列の正準合成 | 厳密結果 | 理想空記録セル、固定有限プログラム |
| 継続Rabi 駆動下の有限Zeno周期 | 明示誤差付き結果 | 固定有限 $N_{\rm Z}$、短時間域 |
| M37の局所位置結合振動子網 | 厳密結果 | 有限自由度、時間非依存 |
| M37のSchrödinger 型局所包絡 | 近似結果 | $\eta<1$、有限時間 |
| M35の一般有限基底周期 | 厳密または明示誤差付き | 固定純粋準備、固定基底 |
| Bell 二側履歴統計 | 仮説依存結果 | 余弦区間と支持 $G$ を採用 |
| 全プログラム共通のM0と $\mu_*$ | 予想・未解決 | 統一周期は未構成 |

Q1-2からQ1-4の「達成」は、任意の $\epsilon>0$ に対して固定有限プログラムの無反応率、分布誤差、周期帰還誤差を $\epsilon$ 未満にできることを意味する。Q1-4では $N_{\rm Z}$ と測定間隔列も先に有限値へ固定する。有限資源で誤差が厳密に零であること、または $N_{\rm Z}\to\infty$ に一様であることを意味しない。

## 1.12 本論文が主張しないこと

本論文は次を主張しない。

1. 量子力学の全構造を導出したこと。
2. M37、M38、M35、M36を1つのM0へ統合したこと。
3. M38の $QQ+PP$ 操作をM37の位置ばねだけから導いたこと。
4. M37とM38が同じハードウェアであること。
5. 一般有限 $L$ の完全操作・測定周期を閉じたこと。
6. 可変調製・可変基底を含む統一エルゴード周期。
7. 無理数回転が独立同分布または二項分布型有限標本統計を与えること。
8. 無反応なしに滑らかな有限時間Hamiltonian で連結入力領域全体を厳密な2固有状態だけへ写すこと。
9. 有限閉鎖系の固定容量で永久記録を無期限に蓄積すること。
10. 異なる結果履歴を外部記録ごと同じ位相点へ押しつぶすこと。
11. reset の熱力学的最小仕事、総エネルギー収支、資源下界を導いたこと。
12. 局所包絡作用がM37のミクロ発展中に厳密保存されること。
13. 正常モード包絡が局所物理場であること。
14. 位相担体のセル選択が粒子の局所検出そのものであること。
15. 粒子の連続軌道、Wallstrom 問題、連続スペクトルを含む一般Born 則。
16. Bell 余弦区間を単一試行のミクロ源から導いたこと。
17. Bell 二側測度を通常の前向き初期値問題の不変測度として生成したこと。
18. 一般Tsirelson 原理または外部設定介入に対する統計安定性。
19. 固定帯域・固定性能の1台による $N_{\rm Z}\to\infty$。
20. 有限測定間隔での完全凍結。
21. Rabi 周期全域で測定間隔の短縮が単調抑制を与えること。
22. 生存試行または安全試行だけを残す事後選別によるZeno則。
23. 測定窓中のRabi Hamiltonian を停止して得る抑制。
24. 観測中の摩擦または周期末 reset が遷移抑制を作ること。
25. 固定最大結合強度下で任意に短い測定窓を作ること。

配置拡散・Nelson経路、旧2成分誘導場、旧位置作用殻、前周期記憶模型は現行主線に採用しない。再利用価値のある結果と不採用理由は研究メモまたは版履歴で管理する。

## 1.13 本文と付録の構成

第2章と第3章はM37によるQ3-1のミクロ導出を扱う。第4章はM38によるQ1-1からQ1-4、第5章はM35による一般有限基底への適用範囲、第6章はM36によるQ2-2の条件付きBell 二側履歴、第7章は各模型の誤差と資源、第8章は達成範囲、未完成目標、反証条件を扱う。

付録Aは正常モード正準変換、付録Bは相関行列と作用選択、付録CはM35の一般有限基底装置、付録DはBell 境界模型、付録EはM38の2モード完全周期、付録Fは有限反復Zeno周期を扱う。

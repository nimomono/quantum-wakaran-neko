# 論文リポジトリの構成・執筆・更新規約

## 1. この文書の目的

このリポジトリを論文の唯一の正本として管理するため、次の事項を定める。

1. 各ファイルと各フォルダーの役割
2. 直接編集するファイルと自動生成するファイルの区別
3. 論文の執筆方針
4. 理論的主張の管理方法
5. 更新、検算、版管理の手順
6. 旧版と置換済みモデルの保存方法

この規約と個別の原稿が食い違う場合は、意図的な規約変更でない限り、この規約に合わせて原稿を直す。

## 2. 正本と生成物

### 2.1 内容上の正本

`sections/` 内の章別マークダウン原稿を、現行論文の内容上の唯一の正本とする。本文、付録、参考文献として論文へ収録する内容だけを置き、通常の本文修正、数式修正、参考文献への言及、付録の修正は、まず該当する章別原稿に対して行う。

`notes/` は、論文へ収録しない研究記録の正本とし、`paper.md`、`main.tex`、`paper.pdf` の生成対象には含めない。収録条件と保存方法は4.5.3節に定める。

`paper.md`、`main.tex`、`paper.pdf` は章別原稿から作る生成物であり、原則として直接編集しない。`paper.md` はGitHubでそのまま閲覧できる公開Markdownとし、TeX専用命令を含む中間Markdownは `build/` 内だけに生成する。

### 2.2 モデル・導出結果・解釈の管理

論文中のモデル、導出結果、物理的解釈は、次の区分で整理し、`PROJECT_STATUS.md` で管理する。

| 対象 | 必須の区分 | 区分 |
|---|---|---|
| モデル | 運用状態 | 現行モデル／補助モデル／不採用モデル |
| 導出結果 | 導出状態 | 厳密結果／近似結果／予想・未解決 |
| 物理的解釈 | なし | 分類せず、根拠と推測を明記する |

必要な場合に限り、次の情報を付記する。

| 対象 | 付記する情報 |
|---|---|
| 導出結果 | 仮説依存／否定的結果／成立条件・適用限界 |
| 物理的解釈 | 根拠となるモデル・結果・仮定／未検証の推測 |
| モデル・導出結果 | 数値計算、収束試験、既知の極限との比較などの検証 |

モデルの採否、結果の導出状態、物理的解釈を混同しない。

モデルの運用状態は、そのモデルの真偽や、そこから得られる結果の導出状態を表さない。現行モデルに未解決部分が残る場合も、不採用モデルから厳密な否定的結果が得られる場合もある。

導出状態と付記情報は別に記録する。仮説を置いた後の計算が厳密なら「厳密結果」、近似を用いるなら「近似結果」とし、必要に応じて「仮説依存」を付記する。「仮説依存」を独立した導出状態として扱わない。

数値計算、次数評価、収束試験、既知の極限との一致、先行研究との比較は、モデルまたは結果を支える検証として記録する。数値的一致だけを厳密結果とは呼ばない。

本文でモデルの運用状態、結果の導出状態、付記する情報、物理的解釈の位置づけ、主要誤差の評価を変えた場合は、同じ更新の中で `PROJECT_STATUS.md` も直す。

#### 2.2.1 固定長期目標の管理

`PROJECT_STANCE.md` には長期目標の要約を置き、具体的な固定目標と達成判定基準は `PROJECT_STATUS.md` の固定目標一覧で管理する。固定目標の現在地、根拠となる結果、残る課題は、固定一覧と分けた可変表で管理する。

1. 通常の論文更新では、固定目標のID、名称、意味、達成判定基準を変更しない。
2. 論文更新では、現在地、根拠となる結果、残る課題だけを更新する。
3. 達成した目標も固定一覧から削除せず、達成状態と根拠を保存する。
4. 固定目標の追加、削除、統合、分割または達成判定基準の変更は、通常の論文改訂と分けた方針変更の変更提案で行う。
5. 固定目標を変更する場合は、`PROJECT_STANCE.md`、`PROJECT_STATUS.md`、`PROJECT_GUIDE.md` の整合性を同じ変更提案で確認する。

### 2.3 検算記録の正本

数式検算、数値検算、組版、参照、図表、完成版の目視確認は `VALIDATION.md` に記録する。単に「確認済み」と書かず、実行した命令、条件、許容誤差、結果、残った警告を記す。

### 2.4 READMEの運用

`README.md` は、プロジェクトの全情報を収録する文書ではなく、初めて読む人が目的、現在地、主要文書へ短時間で到達するための入口とする。題名と短い導入文の後は、原則として次の4節で構成する。

1. 目的
2. 長期目標の現在地
3. 運用文書
4. 現行論文

「目的」は `PROJECT_STANCE.md` の長期的方針を要約する。「長期目標の現在地」は `PROJECT_STATUS.md` の固定目標一覧と可変の現在地表から、目標ID、名称、達成判定の中心、現在地を掲載する。目標体系に段階がある場合は段階別の表に分け、各表の列を目標ID、名称、達成判定の中心、現在地とする。現在地は表の右端に置き、「部分達成」の項目だけ、同じセルに残る課題の要約を添える。達成判定の中心、現在地、残る課題は `PROJECT_STATUS.md` の内容を要約して転載し、`README.md` だけで独自に変更しない。「運用文書」は各管理文書への案内、「現行論文」は論文名、閲覧用原稿、完成版、最小限の再生成手順への案内とする。

根拠となる結果、およびモデル、導出結果、物理的解釈、成立条件、適用限界、検算結果、変更履歴の網羅的な一覧は `README.md` に置かない。これらは各正本で管理し、`README.md` には現在地の要約とリンクだけを置く。

関連文書を変更した場合は、次の対応関係に従って `README.md` を確認する。

- `PROJECT_STANCE.md` の変更時は「目的」
- `PROJECT_STATUS.md` の固定目標または現在地の変更時は「長期目標の現在地」
- 管理文書の追加、削除、改名時は「運用文書」
- 論文名、現行成果物、再生成方法の変更時は「現行論文」

対応する要約またはリンクが変わる場合は、原則として同じ更新の中で `README.md` も直す。確認しても表示内容が変わらない場合は、形式的な変更を加える必要はない。11節に定める方針文書だけの先行更新では、`README.md` への反映を後続の変更提案へ分けてよい。

`README.md` だけに新しい方針、導出結果、導出状態、付記情報または未解決問題を書いてはならない。内容が食い違う場合は、長期的方針については `PROJECT_STANCE.md`、導出結果と未解決問題については `PROJECT_STATUS.md` を正しいものとして扱い、`README.md` を直す。

## 3. 標準ファイル構成

最終的に次の構成へ統一する。

```text
quantum-wakaran-neko/
├── README.md
├── PROJECT_STANCE.md
├── PROJECT_GUIDE.md
├── PROJECT_STATUS.md
├── CHANGELOG.md
├── VALIDATION.md
├── MANIFEST.md
├── CITATION.cff
├── paper.md
├── main.tex
├── paper.pdf
├── references.bib
├── sections/
├── notes/
├── tools/
├── figures/
├── fonts/
├── .gitignore
└── .github/
    └── workflows/
        └── verify.yml
```

### 3.1 入口文書

| 場所 | 役割 | 直接編集 |
|---|---|---:|
| `README.md` | 目的、長期目標の現在地、運用文書と現行論文への案内 | する |
| `PROJECT_STANCE.md` | プロジェクトの目的、理論構成、量子力学とBellの定理に対する長期的方針 | する |
| `PROJECT_GUIDE.md` | リポジトリ構成、執筆、更新、版管理の規約 | する |
| `PROJECT_STATUS.md` | 固定長期目標と現在地、モデルの運用状態、導出結果の導出状態と付記情報、物理的解釈、未解決問題の整理 | する |
| `CHANGELOG.md` | 各版で生じた理論上の変更の要約 | する |
| `VALIDATION.md` | 数式、数値、組版、完成版の検査記録 | する |
| `MANIFEST.md` | 現行版に含まれるファイルと用途の一覧 | 原則として生成する |
| `CITATION.cff` | 著者、題名、版、公開日などの引用情報 | する |

### 3.2 論文原稿

| 場所 | 役割 | 直接編集 |
|---|---|---:|
| `sections/*.md` | 本文と付録の章別原稿。内容上の正本 | する |
| `paper.md` | 全章を結合した閲覧用・解析用原稿 | しない |
| `main.tex` | 組版用原稿 | しない |
| `paper.pdf` | 現行完成版 | しない |
| `references.bib` | 参考文献データ | する |

章番号は、本文を `00` から始まる2桁、付録を `A1` から始まる記号付き番号、参考文献を `90` とする。ファイル名を変えた場合は、生成処理の読み込み順も同時に直す。

### 3.3 補助ファイル

| 場所 | 役割 | 管理方針 |
|---|---|---|
| `notes/` | 論文へ収録しない研究記録 | 論文生成には含めず、4.5.3節に従って整理する |
| `tools/` | 原稿の結合、組版、数式検算、数値検算 | 再現に必要なものだけを置く |
| `figures/` | 本文と付録から参照する図 | 元データと生成方法を追跡可能にする |
| `fonts/` | 組版の再現に必要な書体 | 使用許諾を確認し、必要最小限にする |
| `.github/workflows/verify.yml` | 自動検算 | 手元と同じ検算を実行する |
| `.gitignore` | 一時ファイル、補助ファイル、組版途中の出力の除外 | 完成版と再現に必要なものは除外しない |

## 4. 論文の執筆方針

### 4.1 使用言語

論文の題名、概要、本文、付録、図表見出し、脚注では、人名以外の英単語を説明語として使わず、日本語で書く。

- 一般的な日本語訳がある専門語は日本語訳を使う。
- 定着したカタカナ語しかない場合は、カタカナで書く。
- 人名に由来する用語は、人名部分だけをアルファベットで表記し、それ以外の部分は日本語で書く。例：`Bell の定理`、`Wallstrom 問題`、`Tsirelson 限界`、`Born 則`。
- 数式記号、物理量の記号、略号、文献の原題、引用文、プログラムの識別子、命令、ファイル名は、説明語ではないため原表記を保ってよい。
- 原語を示す必要がある場合は初出の注または用語表に限り、本文で原語を繰り返さない。

たとえば、本文では `flux` ではなく「流束」、`terminal condition` ではなく「終端条件」、`working range` ではなく「適用範囲」、`no-go result` ではなく「否定的結果」と書く。

抽象的な比喩語は避け、物理的役割または文書上の役割を直接書く。固定総作用を2つのモードへ配分する物理系には「2モード作用分配系」、残りの作用を保持するモードには「残余作用モード」、管理文書には「一覧」または「整理」を用いる。

物理構成一般は「モデル」、低周波側のモードは「ソフトモード」と表記する。

### 4.2 数字

数量、章番号、列挙、版番号、日付を含め、漢数字を使わずアラビア数字を使う。

- 「2つ」「第3節」「1回」「2026年7月25日」と書く。
- 熟語として定着した表現も、誤解なく置き換えられる場合はアラビア数字へ統一する。
- 引用する論文題名、固有名詞、歴史的名称は原表記を保ってよい。

### 4.3 本文と付録の役割

本文では、読者が物理的な因果関係と近似の見通しを追えることを優先する。各節は原則として次の順序で書く。

1. 何を説明または導出する節なのか
2. どの物理系を考えるのか
3. 何が時間発展し、何が保存され、何が選別されるのか
4. 支配的な時間、長さ、エネルギーの尺度と、小さい無次元量
5. 中心となる式
6. その式の物理的意味
7. 成立に必要な仮定、近似、適用範囲
8. 主要誤差と、それが中心結論へ与える影響
9. どこまで導けて、何が残るのか

本文には物理像、尺度評価、近似を採用する理由、中心結果、主要誤差を置く。長い代数計算、厳密証明、詳細な誤差上界、正準変換の逐次検算、測度論上の補足、例外条件の網羅、数値手順の詳細は付録へ置く。本文では付録の結論と、その結論が物理的議論にどう効くかを説明する。

付録へ送ったことを理由に、本文から仮定、結果の導出状態、適用範囲、主要誤差、結論の範囲を消してはならない。本文だけを読んでも、何を厳密に導き、何を近似し、何を仮定し、何を得たかが判別できるようにする。

付録は、本文中の特定の主張、証明、誤差評価または検証を実質的に支える場合だけ `sections/` に置く。付録と `notes/` を分ける基準と移動時の規則は4.5.3節に定める。

### 4.4 数式の書き方

- 数式を出す前に、何を表す式かを文章で示す。
- 数式の直後に、主要な各項の物理的意味を書く。
- 同じ物理量に複数の記号を使わない。
- 記号を変更した場合は、本文、付録、検算処理を同時に更新する。
- 近似記号、比例記号、極限、誤差項を使う場合は、その意味、展開に使う小さい量、適用範囲、保持した次数を明示する。
- 近似式の後には、捨てた主要項の次数と、長時間または高周波などで誤差が蓄積する可能性を書く。
- 完全な誤差上界がない場合は、次数評価、数値収束、元の有限モデルとの比較のどれで妥当性を支えるかを示す。
- 無次元化を行った場合は、元の次元を回復する方法を示す。
- 数式番号は本文から参照する式に付け、単なる途中計算には付けすぎない。
- 定理、命題、補題、仮定、定義、予想を区別する。

#### 4.4.1 GitHub上の数式表記

リポジトリ内の全ての `.md` ファイルでは、GitHub上で数式として表示される次の記法へ統一する。論文の章別原稿だけでなく、`README.md`、状態管理文書、検算記録、メモにも適用する。

- 文章中のインライン数式は `$...$` で囲む。`\\(...\\)` は使わない。
- GitHubは `tools/template.tex` の独自マクロ定義を読み込まないため、`\\R`、`\\dd`、`\\E`、`\\Tr`、`\\GM`、`\\Nel` などの独自数式マクロをMarkdownで使わない。`\\mathbb{R}`、`\\,\\mathrm{d}`、`\\mathbb{E}`、`\\operatorname{tr}` など、GitHubが解釈できる標準LaTeX表記へ展開する。
- `\\begin{theorem}`、`\\begin{proposition}`、`\\begin{proof}` などの生の定理・証明環境をMarkdownへ置かない。表示用の太字見出しと、生成処理が読む `<!-- theorem-start:種類 -->`、`<!-- theorem-end:種類 -->` の境界コメントを使う。
- 数式外には `\\chapter`、`\\part`、`\\appendix`、`\\cite`、`\\bibitem`、`\\url` などの生のTeX命令を置かない。章構造、引用、参考文献、URLはMarkdown記法で書く。
- 見出し行と、見出しへ変換される `@title:` メタデータでは、`$...$`、`\(...\)`、`\[...\]` などの数式区切りを使わない。GitHubでは見出し内の数式描画が安定しないためである。
- 見出しに簡単な数式表記が必要な場合は、Unicode記号とGitHubが表示できるインラインHTMLの `<i>`、`<sup>`、`<sub>` を使う。複雑な式は見出しへ入れず、直後の本文または表示数式へ置く。
- 見出し用のHTML表記をTeX生成時に数式へ戻す必要がある場合は、生成処理に対応関係を明示し、数式の意味とPDFの表記を変えない。
- 独立した行に置く表示数式は、開始行を ` ```math `、終了行を ` ``` ` とする `math` コードフェンスで囲む。`$$...$$` と `\[...\]` は使わない。
- フェンスの開始記号、数式本体、終了記号はそれぞれ別の行に置き、フェンスの前後には空行を置く。
- 数式の途中にある `=` や `-` がマークダウンの見出しや箇条書きとして解釈されないよう、複数行の数式を通常の本文領域へ直接置かない。
- 金額など、数式区切りではない `$` は必要に応じてエスケープする。

表示数式は次の形で書く。

````markdown
```math
H
= H_{\mathrm{sys}} + H_{\mathrm{bath}}
```
````

原稿の結合処理とTeX生成処理は、この記法を入力として扱えるように保つ。公開される `paper.md` にはTeX専用命令を注入せず、Pandocへ渡す一時Markdownを作る段階でのみ、定理・証明・部・章・引用・参考文献をTeX表記へ復元する。記法を変更する場合は、生成処理と既存の全 `.md` ファイルを同じ変更提案で更新する。

### 4.5 物理的主張の書き方

- 2.2節の区分に従い、モデル、そのモデルから得られる結果、その物理的解釈を分ける。
- Hamilton 方程式から導いた事実と、初期分布または境界分布に課した統計条件を分ける。
- 力学的実現を与えたことと、確率重みを導いたことを分ける。
- 同じ項目に異なる導出状態の主張が含まれる場合は、項目を分けて記述する。
- 量子力学と同じ式を得たことと、量子力学全体を導出したことを区別する。
- `Wallstrom 問題`、`Tsirelson 限界`、`Born 則`など、未導出の事項を暗黙に解決済みとして扱わない。
- 否定的結果は削除せず、どの条件で何が成立しないかを残す。

#### 4.5.1 近似を採用する基準

近似は、明示的なミクロモデルとの対応を保ち、適用範囲と誤差を追跡でき、中心結論が主要な補正に対して維持される場合に採用する。次の近似は、この基準を満たす限り用いてよい。

- 弱結合近似
- 速い自由度と遅い自由度の時間尺度分離
- 多数モード浴のGauss近似
- 有限観測時間内のMarkov近似と白色雑音近似
- 断熱消去
- 有限分解能での粗視化混合
- 短時間パルス近似
- 狭い運動量分布に対するモーメント閉鎖
- 再帰時間より十分短い測定窓での安定記録
- 測定器と外界を含む弱い開放系としての近似

近似ごとに、次の5点を本文または付録へ記す。

1. 小さい無次元量
2. 適用範囲
3. 捨てた項または有限幅補正の主要な大きさ
4. 中心結論への影響
5. 解析、元の有限モデルとの比較、収束試験などの検証方法

完全な数学的誤差上界が難しい場合は、次数評価と数値収束で代えてよい。その場合は、厳密結果とは分類しない。

既存の問題を未確立の仮説へ移しただけの場合は、解決とは呼ばない。特に、確率重み、境界条件、測定設定独立性、Born則、Tsirelson限界、Wallstrom問題について、2.2節の区分と付記情報を適用する。

Nelson作用を扱う場合は、数式の形式的一致と、有効運動を実際に支配することを区別する。

#### 4.5.2 Bellの前提監査

Bell型相関を扱う場合は、相関式の再現とは別に、次の項目を監査する。

| 監査項目 | 確認する内容 |
|---|---|
| 局所性 | 各翼の結果が、反対側の設定または結果へどのように依存するか。共通過去、境界条件、測定器間の結合を含めて示す |
| 測定設定独立性 | 隠れた変数または履歴の試行分布が設定に依存するか。依存する場合は、初期分布へ直接書き込まず、どの力学または条件付けから生じるかを示す |
| 結果の一意性 | 各試行で記録される結果が一意に定まるか。確率的応答を用いる場合は、その確率の物理的起源を示す |
| 事後選別 | 採用する試行が設定または結果に依存するか。除外率と分母の定義を示す |
| 非信号性 | 一側周辺分布が反対側の設定に依存しないか。相関式とは別に確認する |
| 試行測度 | Bell重みを生む装置、境界条件または試行の数え方を示し、求める重みを外から置いていないか確認する |

近似を採用しても、この監査を省略しない。監査結果は、Bellの定理のどの前提が成立せず、その機構が非信号性とどのように両立するかが分かる形で記録する。

#### 4.5.3 不採用モデルと `notes/`

現行モデルと補助モデルのうち、本文の中心主張、成立条件、適用限界または検証に必要な内容だけを `sections/` に置く。次の規則に従って、論文へ収録する内容と研究記録を分ける。

1. 不採用モデルの詳細は本文と付録へ残さず、`sections/` から `notes/` へ移す。
2. 付録は、削除すると本文中の特定の主張の根拠が欠ける場合だけ `sections/` に置く。形式的な参照しかない付録は `notes/` へ移す。
3. 不採用モデルから得られた否定的結果を現行論文の論証に用いる場合は、そのモデルを補助モデルとして扱い、必要な範囲だけ本文または付録へ残す。
4. `notes/` には、不採用理由、再利用価値のある計算、将来の再検討条件、元となった版、現行理論における位置づけを整理した研究メモを置く。
5. 旧章や旧原稿をそのまま `notes/` へ複製しない。完全な旧原稿は版タグと公開版で保存する。

移動によって導出結果または未解決問題の位置づけが変わる場合は、`PROJECT_STATUS.md` と `CHANGELOG.md` も更新する。

### 4.6 読みやすさ

- 1つの段落では、原則として1つの論点だけを扱う。
- 長い名詞の連結を避け、主語と述語を近づける。
- 新しい記号を一度に大量に導入しない。
- 装置の構成を説明するときは、入力、相互作用、記録、終端条件の順に書く。
- 抽象的な測度の説明には、それが実験の試行をどう数えるかという説明を添える。
- 「自然である」「明らかである」「容易に分かる」だけで推論を済ませない。

## 5. 主張を変更するときの規則

理論上の変更を加えるときは、該当するものを記録する。

1. 以前のモデルまたは主張
2. 新しいモデルまたは主張
3. モデルの運用状態
4. 導出結果の導出状態と付記情報
5. 新たに導出できたこと
6. 新たに必要となった仮説または近似
7. 近似に使う小さい量、適用範囲、主要誤差
8. 中心結論への影響と検証方法
9. 残った未解決問題または新しく判明した否定的結果

既存の問題を別の仮説へ移しただけの場合は、「解決」と書かず、問題の移動先を明記する。

数式上は同じ確率則が得られても、その重みが力学から導かれたのか、近似結果なのか、未確立の仮説に依存するのか、境界条件として置かれたのか、試行の数え方として導入されたのかを区別する。

## 6. 更新手順

通常の更新は次の順序で行う。

1. `main` ブランチの最新版から作業用ブランチを作る。
2. 変更対象と、変更しない範囲を決める。
3. `sections/` の該当章を修正する。
4. 主張の状態または固定長期目標の現在地が変わる場合は `PROJECT_STATUS.md` を修正する。
5. 2.4節の対応関係に従って `README.md` を確認し、要約またはリンクが変わる場合は更新する。11節の方針文書だけの先行更新では、後続の変更提案へ分けてよい。
6. 版として記録すべき変更なら `CHANGELOG.md` を修正する。
7. `references.bib` と本文中の参照を確認する。
8. 生成処理を実行し、`paper.md`、`main.tex`、`paper.pdf` を更新する。
9. 数式検算と数値検算を実行する。
10. 未定義の引用、未定義の参照、はみ出し、文字化け、図表の欠落を確認する。
11. 完成版の全ページを画像化し、主要ページと改ページを目視確認する。
12. `VALIDATION.md` と `MANIFEST.md` を更新する。
13. 差分を確認し、1つの理論的変更を1つの変更提案にまとめる。

生成物だけに差分があり、正本に対応する変更がない場合は、生成手順の不一致を疑う。

## 7. 変更提案に記載する事項

各変更提案には、該当する範囲で次を記す。

- 変更の目的
- 変更した中心主張
- 変更したファイル
- モデルの運用状態
- 導出結果の導出状態と付記情報
- 新しい仮説または近似
- 採用した近似の小さい量、適用範囲、主要誤差
- 中心結論への影響と検証方法
- 削除または弱めた主張
- 残る未解決問題
- 実行した検算
- 完成版のページ数
- 旧版との互換性または置換関係

誤字、参照修正、組版修正だけの場合は、理論的内容を変えていないことを明記する。

## 8. 版番号、タグ、公開版

小さな誤字修正や組版修正では、新しい版番号を付けない。次のいずれかが生じたときに新しい版とする。

- 中心定理または中心モデルが変わった
- 重要な導出が追加または撤回された
- 必要な仮定が変わった
- Bellの前提違反の位置づけが変わった
- 本文と付録の構成が大きく変わった
- 査読への応答により理論の範囲が変わった

版タグは `draft-01`、`draft-02` の形式を使う。各公開版には次を添付する。

- 当時の完成版
- 当時のソース一式
- 原本の検査値
- 変更点の要約
- 現行版で置換済みかどうか

旧版の圧縮ファイルは公開版の添付資料として保存し、`main` ブランチへ展開して並べない。

## 9. 旧説と否定的結果の扱い

旧説を現行本文から外す場合は、次のいずれかに分類する。

1. 内容は正しいが本筋から外れた
2. より明確なモデルに置き換えた
3. 必要な仮定が強すぎるため採用をやめた
4. 計算または論理に誤りがあった
5. 数学的には成立するが、試行確率の説明にならなかった

`CHANGELOG.md` に分類と理由を書き、旧版のタグまたは公開版を示す。現行の検索結果に旧説が混ざらないよう、旧章を `main` ブランチへ常置しない。

保存先と整理方法は4.5.3節に従う。旧章や旧原稿の完全な内容は版タグと公開版で保存し、`notes/` には複製しない。

否定的結果が現行理論の境界を示す場合は、`PROJECT_STATUS.md` と必要な本文または付録にも残す。

## 10. 検算の最低条件

### 10.1 原稿

- 全ての章が所定の順序で結合される。
- 本文中の引用が `references.bib` に存在する。
- 使われていない参考文献を意図なく残さない。
- 数式記号の定義が初出より後になっていない。
- インライン数式と表示数式が、GitHub上の実際の表示で数式として描画され、文章を分断していない。
- 見出し行と `@title:` メタデータに数式区切りが残っておらず、上付き、下付き、斜体がGitHub上で意図どおり表示される。
- `\\(...\\)`、`\\[...\\]`、`$$...$$` など、規約外の数式区切りが残っていない。
- 独自数式マクロ、生の定理・証明環境、数式外のTeX命令が章別原稿と `paper.md` に残っていない。
- GitHub閲覧用の `paper.md` とPandoc用の一時Markdownが分離され、後者から従来と同じTeX組版を再生成できる。
- 本文と付録で同じ仮定を別の意味に使っていない。
- `sections/` に不採用モデルの詳細が残っていない。
- 全ての付録が本文から実質的に参照されている。
- `notes/` の内容が論文の生成対象に含まれていない。

### 10.2 数式と数値

- 解析的に確かめられる恒等式は自動検算する。
- 乱数を使う検算では種、標本数、許容誤差を固定する。
- 数値的一致を解析的証明として扱わない。
- 近似の収束を主張する場合は、小さい量、比較する観測量、誤差の尺度、適用時間を明示する。
- 刻み幅、モード数、結合強度、時間尺度比、観測窓のうち該当する量を変え、期待する次数または安定域を確認する。
- 可能な場合は、元の有限モデルと近似モデルを同じ初期条件または同じ試行分布で比較する。
- Bell型相関では、CHSH値の誤差余裕、一側周辺分布、事後選別率を別々に記録する。

### 10.3 組版

- 組版処理を最初から最後まで再実行できる。
- 未定義の引用と未定義の参照がない。
- 内容が切れるはみ出しがない。
- 全ページを画像化し、題名、目次、定理、長い数式、表、参考文献を目視確認する。
- 完成版の重複を置かない。

## 11. 規約自体の変更

この規約を変える場合は、論文内容の変更と分けた変更提案にする。変更理由、影響するファイル、既存原稿を一括修正する必要があるかを記す。

長期方針または規約を先に確定する必要がある場合は、`PROJECT_STANCE.md` と `PROJECT_GUIDE.md` だけを先行する変更提案にしてよい。この段階では本文、状態整理、README、生成物を変更せず、後続の変更提案で反映する対象を明記する。先行する変更提案を統合した後は、未反映の文書を放置せず、適用作業を別の変更提案として続ける。

`PROJECT_STATUS.md` の固定目標一覧を変更する場合も、論文内容の変更と分けた方針変更の変更提案にする。その変更提案では、目標を変える理由、旧IDとの対応、達成判定への影響を記し、`PROJECT_STANCE.md` と本規約を同時に確認する。現在地、根拠となる結果、残る課題だけの更新は通常の状態更新として扱う。

規約を追加しただけで過去の不整合が直るわけではない。規約変更後、現行原稿、状態整理、README、生成処理、検算処理を順に点検する。

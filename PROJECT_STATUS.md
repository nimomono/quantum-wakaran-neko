# 固定長期目標、モデル運用状態、結果の導出状態、理論の境界

この文書は、固定長期目標とその現在地に加え、モデル、導出結果、物理的解釈を分けて管理する。固定目標と可変の進捗、モデルの運用状態、結果の導出状態、付記情報を混同しない。

## 固定長期目標

以下の目標ID、名称、意味、達成判定基準は、通常の論文更新では変更しない。個々の論文や現行モデルが変わっても一覧から削除せず、追加、削除、統合、分割または判定基準の変更が必要な場合は、独立した方針変更として扱う。

### 固定目標一覧

理論目標と工学的実証目標を分けず、基礎構造、代表的現象、操作・測定、実装評価の依存関係に沿う1つのQ系列として管理する。

| ID | 固定目標 | 達成判定の中心 |
|---|---|---|
| Q1 | Schrödinger 型有効力学 | 明示的な古典ミクロモデルから、適用範囲と誤差を伴う有効時間発展として導く |
| Q2 | Born 則 | 確率重みを外から置かず、準備、力学、測定過程から導く |
| Q3 | 位相量子化 | 循環量子化、節、単価性、位相すべりを統合し、 Wallstrom 問題へ回答する |
| Q4 | 束縛状態 | 井戸型・調和型ポテンシャルで固有エネルギー、確率密度、節、状態選択、安定性を再現する |
| Q5 | トンネル効果 | 障壁高、幅、入射エネルギーに対する透過率を再現し、古典的熱活性化と区別する |
| Q6 | Rabi 振動 | 駆動2準位系の振動数、振幅、離調依存性を再現する |
| Q7 | 2重スリット干渉 | 準備、伝播、干渉、局所検出を含む明示的なモデルから位置分布を再現する |
| Q8 | 2量子ビット型結合ゲート | 2つの論理自由度に対する CNOT 型または制御位相型の結合操作を、外付けの行列演算ではなく明示的な古典 Hamiltonian 装置として実現する |
| Q9 | Bell 型測定統計 | 明示的な複合系、局所測定、設定生成、試行測度から、余弦共同確率、 CHSH 不等式の破れ、 Tsirelson 限界、非信号性を導き、 Bell のどの前提がどの物理機構で破れるかを示す |
| Q10 | 完全な操作・測定周期 | 準備、必要な操作、設定生成、結果形成、増幅、外部記録、内部消去、再初期化を1周期として構成し、同じ装置で連続反復できるようにする |
| Q11 | Zeno 効果 | 非測定時の短時間生存確率と反復測定後の生存確率を同じミクロモデルから導き、測定間隔を短くしたときの遷移抑制を定量的に再現する。摩擦、 Hamiltonian の停止、事後選別とは区別する |
| Q12 | 性能と資源増加の評価 | 忠実度、雑音耐性、エネルギー収支、動作時間、必要自由度の系サイズ依存性を評価する |

### 現在地

「達成」は達成判定基準を満たした状態、「部分達成」は限定されたモデルまたは一部の構成要素で達成した状態、「未達」は理論上の作業対象だが基準を満たしていない状態、「未着手」は具体的なモデルまたは検証がまだない状態を表す。

| 目標ID | 現在地 | 根拠となる結果 | 残る課題 |
|---|---|---|---|
| Q1 | 達成 | R83--R86、R88 | — |
| Q2 | 達成 | R70--R72、R76--R79、R87、R89--R91。固定純粋準備・固定有限基底・有限次元・任意精度に限定 | — |
| Q3 | 未達 | — | 空間グラフの閉路巻数、節、位相すべり、連続補間を統合し、 Wallstrom 問題へ回答すること |
| Q4 | 未達 | R86の有効演算子 | 井戸型・調和型ポテンシャルの定量検証、エネルギー測定、状態選択、安定性 |
| Q5 | 未達 | — | 透過率の定量導出と熱活性化との識別 |
| Q6 | 未達 | R83--R86の時間非依存基準 | 時間依存駆動の非共鳴条件、駆動2準位系の明示模型と定量検証 |
| Q7 | 未達 | R86、R87 | 位相担体の空間セル標本化を、局所検出器、時間積分流束、無反応を含む完全結果集合へ接続すること |
| Q8 | 未着手 | — | 2つの論理自由度の最小符号化、結合 Hamiltonian、入力状態に対する作用、相関生成、忠実度の検証 |
| Q9 | 部分達成 | R63、R66、R73、R74、R80--R82 | 単一試行変数からの余弦区間多重度、Bell 整合支持の物理的正当化、完全モデルでの基準密度因子化、一般 Tsirelson 原理、非対称誤差での非信号性 |
| Q10 | 部分達成 | R67、R76--R79、R87、R89--R91 | Q1位置ばね網とQ2交換回路の統合、局所時計配線、永久外部記録、弱開放reset、同一装置の直後反復、Bell 整合支持 |
| Q11 | 未着手 | — | Rabi 型遷移と明示的な反復測定を接続し、生存確率の測定間隔依存を導出すること |
| Q12 | 部分達成 | R79、R85--R91 | 強度制約下の絶対動作時間、総エネルギー収支、雑音耐性、完全周期の長期安定性、資源下界、多体系での規模依存性 |

## 1. モデルの運用状態

| 識別 | モデル | 運用状態 | 役割と限界 |
|---|---|---|---|
| M0 | 局所振動子、粒子候補、源、選択器、測定器、Bell 装置、記録、外部流路を持つ弱開放統一試行系 | 現行モデル | M37の伝播、M35の測定、M36の二側履歴を1つの反復系へ統合する将来目標。単一の統一母測度は未構成 |
| M1 | 1試行内の有限閉鎖 Hamiltonian 系 | 補助モデル | 正準写像、保存量、理想読出し、比較器を厳密に計算する |
| M2--M5 | 運動量結合、二側配置拡散、 Fourier--Gauss 表示、 Gauss 幅 Routh 縮約 | 補助モデル | 配置拡散、 Fisher 項、 Guerra--Morato 作用の研究記録。M0と現行相関行列主線の前提にしない |
| M6 | 最小結果符号化器 | 補助モデル | 既存の2値セクターを指針と記録へ写す。一般的な唯一結果形成ではない |
| M7--M13 | 旧3モード Bell 殻、終端関数、順時間重み、旧位置結合閉包 | 不採用モデル | 詳細と不採用理由は `notes/` で管理する |
| M14、M20--M25 | 実在2成分誘導場、位相接続、局所作用整合浴、低速・高速交換、標本場弱縮約 | 不採用モデル | draft-19までの力学主線。研究メモで置換理由と再利用価値を保存する |
| M15、M29 | 位置入口への局所作用転送と2モード作用殻 | 補助モデル | 作用殻容量は版履歴へ残す。現行の確率機構には使わない |
| M16--M19 | 旧表記の Bell 対モード、比較器、境界流束、逆計算 | 補助モデル | 余弦集団重みと逆計算はM30、M33へ継承。2モード殻容量は確率機構から外した |
| M26 | 設計済み有限正準位相担体網 | 補助モデル | $H=d^\dagger h_Ld$ を持つ理想有効層。測定回路と相関行列の補助計算に使い、Q1のミクロ根拠には使わない |
| M27 | 共通源と有限正準準備回路 | 補助モデル | 理想有効層で階数1相関を準備する。任意初期集団からの純化またはM37の物理準備ではない |
| M28 | 相関行列による調製集団の補助統計 | 補助モデル | $C$ を指定集団の2次モーメントとする。ミクロ基礎状態または単一試行変数ではない |
| M30 | 左右位相担体の反対称交差相関と局所分析器 | 現行モデル | Bell 余弦集団重みを与える。頻度と完全周期は単独では与えない |
| M31 | 全プログラム共通の試行開始面、周期写像、不変かつエルゴード的な統一母測度 | 補助モデル | 将来の統一目標。具体的生成は未完成で、現行定理の仮定には使わない |
| M32 | 固定作用の正準角と累積作用区間による有限基底選択器 | 現行モデル | 高階数を含む一般作用区間公式を与える。一般集団では条件付き一様性を仮定するが、固定純粋準備ではM35の Poincaré 写像が供給する |
| M33 | Bell 境界角、履歴整合事象、一般基準セクター密度 | 現行モデル | 一般完全模型の頑健性公式を与える。整合支持と結果非依存因子化は未完成 |
| M34 | 累積、比較、ラッチ、記録、逆計算の正準レジスター | 補助モデル | draft-21の構成要素。M35で固定有限基底の1本の自律周期へ統合した |
| M35 | 固定純粋準備・固定有限基底の局所滑らか可逆内部測定周期 | 現行モデル | $3L+4$ 正準対、隣接 $QQ+PP$ 交換、局所位相回転、滑らかな比較、無反応結果で測定後状態、内部記録、逆計算、無理数回転を構成。永久記録と局所時計配線は含まない |
| M36 | 局所結果角と未来整合角を持つ最小 Bell 二側履歴模型 | 現行モデル | 局所 Haar 角から基準密度 $1/4$ を導く。余弦区間は集団反対称源から指定し、単一試行生成、二側支持の必然性、前向き不変生成は未完成 |
| M37 | 局所位置結合された有限実古典振動子網 | 現行モデル | $q,p$ と実ばね結合から局所包絡を作り、弱結合・有限時間で実対称 $h_L$ の Schrödinger 型発展を誤差付きで導く |

M0はM37、M35、M36と必要な粒子・記録・外部流路を同じ反復系へ統合する目標模型である。M37は伝播、M35は固定プログラムの内部周期、M36は条件付き二側履歴模型として別々に成立するが、これらを1つのM0へ統合したとは呼ばない。

## 2. ミクロ振動子網と補助有効担体の導出結果

### R83 局所包絡の反回転項を含む厳密方程式

- 導出状態：**厳密結果**
- 対象モデル：M37

```math
H_{\rm micro}
=
\frac{p^{\mathsf T}p}{2M_{\rm osc}}
+
\frac12q^{\mathsf T}
\left(
M_{\rm osc}\omega_0^2I+A
\right)q
```

に対し、局所回転包絡は

```math
i\mathcal J_0\dot b
=
h_0b
+
h_0e^{2i\omega_0t}\overline b,
\qquad
h_0
=
\frac{\mathcal J_0A}
{2M_{\rm osc}\omega_0}
```

を厳密に満たす。位置結合だけでは反回転項を厳密に消せず、局所包絡作用は一般に厳密保存されない。

### R84 正常モード包絡と厳密生成子

- 導出状態：**厳密結果**
- 対象モデル：M37

```math
\Omega
=
\left(
\omega_0^2I
+
\frac{A}{M_{\rm osc}}
\right)^{1/2}
```

を使う正常モード正準包絡 $\widetilde b$ は

```math
i\mathcal J_0\dot{\widetilde b}
=
h_{\rm ex}\widetilde b,
\qquad
h_{\rm ex}
=
\mathcal J_0
\left(
\Omega-\omega_0I
\right)
```

を満たし、$\mathcal J_0\widetilde b^\dagger\widetilde b$ を厳密保存する。$\widetilde b$ は行列平方根を含み、一般には非局所である。

### R85 目標演算子との作用素誤差

- 導出状態：**厳密結果**
- 性質：誤差上界
- 対象モデル：M37

$h_0=h_L$、

```math
\eta
=
\frac{2\left\|h_L\right\|}
{\mathcal J_0\omega_0}
<1
```

なら、

```math
\left\|h_{\rm ex}-h_L\right\|
\leq
\frac{
\left\|h_L\right\|^2
}{
2\mathcal J_0\omega_0
\left(1-\eta\right)^{3/2}
}
```

である。実ばね係数と離調を選ぶことで、実対称なグラフ Laplacian と局所ポテンシャルに係数対応できる。

### R86 局所包絡の有限時間 Schrödinger 型近似

- 導出状態：**近似のもとで導出済み**
- 性質：有限時間誤差上界
- 対象モデル：M37

実際の局所包絡と、同じ局所初期値から始める目標解 $b_L=e^{-ih_Lt/\mathcal J_0}b(0)$ の差は

```math
\sup_{0\leq t\leq T}
\left\|b(t)-b_L(t)\right\|
\leq
\varepsilon_{\rm car}(T)
\left\|\widetilde b(0)\right\|
```

で抑えられる。$\varepsilon_{\rm car}$ は局所・正常モード変換差2項とR85の時間積分からなり、$T=O(\mathcal J_0/\|h_L\|)$ で $O(\eta)$ である。時間非依存・実対称・有限次元に限定する。

### R87 包絡誤差から有限基底分布誤差への伝播

- 導出状態：**厳密結果**
- 性質：誤差伝播
- 対象モデル：M35、M37

規格化した実際の局所包絡 $\widehat b_{\rm mic}$ と目標状態 $\chi_L$ に対し、任意の有限基底 $W$ で

```math
D_{\rm TV}
\left(
p^{\rm mic},p^L
\right)
\leq
\sqrt{
1-
\left|
\left\langle
\widehat b_{\rm mic},
\chi_L
\right\rangle
\right|^2
}
```

が成立する。R86から右辺を $O(\eta)$ で抑えられる。比較器、角切断、パルス、準備誤差は別に加える。

### R88 8振動子鎖の数値収束

- 導出状態：**数値検証**
- 対象モデル：M37

8振動子の1次元鎖と弱い調和型離調で、$\omega_0=20,40,80$ を比較した。作用素誤差、厳密包絡状態誤差、局所包絡状態誤差、局所作用変動、任意基底の全変動距離が解析上界を満たし、弱結合極限で概ね $O(\eta)$ で減少した。`tools/verify_envelope_reduction.py` で再現する。

### R53 正準位相担体と全位相作用

- 導出状態：**厳密結果**
- 性質：理想有効層内部
- 対象モデル：M1、M26

実正準対から

```math
b_i
=
\frac{Q_i+iP_i}{\sqrt{2\mathcal J_0}}
```

を定め、$H_{\rm ph}=b^\dagger hb$、$h=h^\dagger$ とすると、$i\mathcal J_0\dot b=hb$ が厳密に従う。全位相作用 $I_{\rm ph}=\mathcal J_0b^\dagger b$ は保存される。これは設計済み理想有効層または測定回路の結果であり、M37の位置ばね網ではR83--R86を使う。

### R54 集団モーメントとしての相関行列発展

- 導出状態：**厳密結果**
- 性質：仮説依存・成立条件
- 対象モデル：M28

調製条件 $\mathcal P$ と観測プログラム $M$ を固定した集団測度を $\mu_{\mathcal P,M}$ とし、

```math
C_M(t)
=
\mathbb E_{\mu_{\mathcal P,M}}
\left[
b_tb_t^\dagger
\right]
```

とする。同じ条件付き集団の全試行が共通 $h(t)$ に従うなら、

```math
i\mathcal J_0\dot C_M
=
\left[h,C_M\right]
```

を満たす。$C_M$ は単一試行の正準変数ではない。

### R55 相関スペクトルと純度の保存

- 導出状態：**厳密結果**
- 性質：保存則・否定的結果
- 対象モデル：M28

閉鎖線形発展では $C(t)=U(t)C(0)U(t)^\dagger$ である。跡、固有値、階数、純度、階数欠陥が保存される。混合集団は閉鎖線形発展だけで階数1へ純化しない。

### R56 階数1の同値条件と共通源準備

- 導出状態：**厳密結果**
- 性質：成立条件
- 対象モデル：M27、M28

$C=\Lambda\chi\chi^\dagger$ であることは、$b^\omega=c^\omega\chi$ がほとんど確実に成立することと同値である。有限正準回路 $U_{\rm prep}$ により階数1方向を準備できる。

### R57 階数1因子の Schrödinger 型発展

- 導出状態：**厳密結果**
- 性質：仮説依存
- 対象モデル：M28

R54に加えて $C=\Lambda\chi\chi^\dagger$ を仮定すると、共通位相を除いて

```math
i\mathcal J_0\dot\chi
=
h\chi
```

を得る。$h$ の物理的由来はこの結果に含まない。

### R58 空間グラフ上の局所発展、干渉、節

- 導出状態：**厳密結果**
- 性質：理想有効層内部
- 対象モデル：M26、M28

辺結合からグラフ Laplacian $L_G$ を作り、

```math
h_L
=
\frac{\mathcal J_0^2}{2m}L_G
+
V_L
```

と置けば、理想有効層内部で局所疎結合による離散 Schrödinger 型発展、位相差依存の干渉、節を得る。Q1のミクロ根拠はこの結果でなくR83--R86であり、連続極限と粒子軌道は含まない。

### R59 階数欠陥と節の残留強度

- 導出状態：**厳密結果**
- 性質：誤差上界
- 対象モデル：M28

最大固有値以外の相対重量を $\varepsilon_{\rm rank}$ とすると、主固有因子が理想節を持つ出力の正規化強度は $\varepsilon_{\rm rank}$ 以下である。

### R60 セル規格化と単一試行局所作用

- 導出状態：**厳密結果**
- 対象モデル：M26、M28、M32

セル体積を $b_i=\sqrt{\Delta V}a_i$ へ吸収すると、$C_{ii}/\operatorname{tr}C$ は規格化セル重みである。各試行の局所作用は $I_i=\mathcal J_0|b_i|^2$ である。セル重みを粒子検出確率と同一視するには追加の接続が必要である。

### R62 相関行列だけでは粒子軌道を決めない

- 導出状態：**厳密結果**
- 性質：否定的結果・適用限界
- 対象モデル：M0、M28、M32

$C$ と階数1因子 $\chi$ は集団量である。相関行列方程式と位相担体の有限基底頻度だけから、粒子の連続軌道、統計流への等変性、局所位置検出は従わない。

### R68 非線形閉包残差

- 導出状態：**厳密結果**
- 性質：誤差伝播・成立条件
- 対象モデル：M28

試行方程式が $i\mathcal J_0\dot b=hb+r$ なら、

```math
i\mathcal J_0\dot C
=
\left[h,C\right]
+
D_C,
\qquad
D_C
=
\mathbb E
\left[
rb^\dagger-br^\dagger
\right]
```

である。一般非線形 Hamiltonian では高次モーメントが必要となり、$C$だけの閉包は未解決である。

## 3. 有限基底標本化と明示周期

### R69 統一母測度の将来目標

- 導出状態：**予想・未解決**
- 性質：統一目標
- 対象モデル：M31

相関観測、可変調製、可変基底、設定生成器、 Bell 二側条件、外部記録まで含む単一の開始面、周期写像、統一母測度 $\mu_*$ は未構成である。固定プログラムごとの不変測度を混合しても、一般に全体のエルゴード性は従わない。現行定理は $\mu_*$ の存在またはエルゴード性を仮定に使わない。

### R70 正準作用選択器の一般確率式

- 導出状態：**厳密結果**
- 性質：仮説依存・成立条件
- 対象モデル：M32

選択器角が $(b,W,\mathcal P)$ の下で条件付き一様なら、累積作用区間の排他的結果は

```math
P(k\mid W,\mathcal P)
=
\mathbb E
\left[
\frac{I_k}{I_{\rm ph}}
\middle|
W,\mathcal P
\right]
```

を満たす。階数1と共通入口流束は不要である。

### R71 固定作用下の有限基底 Born 型則

- 導出状態：**厳密結果**
- 性質：仮説依存・成立条件
- 対象モデル：M28、M32

$I_{\rm ph}$ が試行ごとに固定されるなら、

```math
P(k\mid W,\mathcal P)
=
\frac{
\left(
WC_WW^\dagger
\right)_{kk}
}{
\operatorname{tr}C_W
}
```

を得る。$C_W=C$ が許容基底で共通なら、同じ状態 $C$ に対する任意有限直交基底の Born 型則となる。高階数相関行列を含む。

### R72 全作用変動の共分散補正

- 導出状態：**厳密結果**
- 性質：誤差恒等式
- 対象モデル：M32

$r_k=I_k/I_{\rm ph}$ とすると、

```math
P_k
-
\frac{
\mathbb E[I_k]
}{
\mathbb E[I_{\rm ph}]
}
=
-
\frac{
\operatorname{Cov}
\left(
I_{\rm ph},r_k
\right)
}{
\mathbb E[I_{\rm ph}]
}
```

である。固定全作用または無相関が平均比との一致条件になる。

### R75 正準レジスターの理想読出し

- 導出状態：**厳密結果**
- 性質：理想パルス・成立条件
- 対象モデル：M34

$G_{\rm read}=\sum_kP_kI_k+P_UI_{\rm ph}f(\vartheta)$ は $P_k=P_U=0$ の理想入口で入力へ反作用せず、作用と閾値を正準レジスターへ移す。分岐生成子の保存量を含めて共役運動量の反作用を追跡する必要がある。固定有限基底への周期統合はR76--R79で扱う。

### R76 固定有限基底の自律内部測定周期

- 導出状態：**厳密結果**
- 性質：固定プログラム・有限自律周期
- 対象モデル：M35

任意の有限 $L$、固定純粋準備 $\chi$、固定基底変換 $W$ に対し、$3L+4$ 正準対を使って準備、作用読出し、累積比較、テンプレート準備、正準 SWAP、内部記録、保持、逆計算、選択器ドリフトを1本の有限自律 Hamiltonian へまとめる。基底回路の局所実装はR89、滑らかな結果形成はR90で与える。

### R77 無理数回転 Poincaré 写像と Haar 不変測度

- 導出状態：**厳密結果**
- 対象モデル：M35

内部準備値を固定した不変集合上で、1周期後に選択器角だけが

```math
\vartheta
\longmapsto
\vartheta+2\pi\alpha
\pmod{2\pi},
\qquad
\alpha\notin\mathbb Q
```

と進む。円周 Haar 測度は唯一の不変確率測度であり、写像は一意エルゴード的である。固定プログラムでは不変性とエルゴード性を仮定せず導出する。

### R78 零幅極限の Born 型長期頻度と条件付き測定後状態

- 導出状態：**厳密結果**
- 性質：固定純粋準備・固定基底
- 対象モデル：M35

比較幅を零にした理想累積区間では、結果 $k$ の長期頻度は $|\langle u_k|\chi\rangle|^2$ である。有限幅の安全セクターでは正準 SWAP 後の信号は厳密に $|u_k\rangle$、測定前情報はテンプレートの $-W\chi$ へ退避する。新しい空テンプレートを持つ第2装置では同じ結果が確率1で反復される。同じ装置の逆計算前の再使用は含まない。滑らかな有限幅分布はR91で扱う。

### R79 内部逆計算と永久記録の限界

- 導出状態：**厳密結果**
- 性質：内部逆計算・誤差上界・否定的結果
- 対象モデル：M35

滑らかな比較、無反応領域、中間テンプレートを含め、前向き写像全体を逆順・逆符号で実行すると、全内部正準変数と時計運動量が周期末に準備値へ戻る。永久外部記録まで結果非依存の同一点へ戻すことは Hamiltonian 流の1対1性と両立しない。無理数回転は混合的でなく、二項分布型有限標本揺らぎを与えない。

### R89 任意有限基底の隣接2モード正準回路

- 導出状態：**厳密結果**
- 性質：局所構成・資源上界
- 対象モデル：M35

局所位相回転

```math
G_{Z,j}
=
\frac{Q_j^2+P_j^2}{2}
```

と隣接交換

```math
G_{X,j}
=
Q_jQ_{j+1}+P_jP_{j+1}
```

の有限列は任意の隣接 $U(2)$ を生成する。隣接 Givens 消去により、任意の $W\in U(L)$ を $L(L-1)/2$ 個以下の隣接2モード混合と対角位相へ分解できる。全ゲートは作用と正準性を厳密に保存し、逆回路は順序と角度の反転で得る。$G_{X,j}$ は運動量間結合を含むため、Q1の位置ばね網と同じハードウェアではない。

### R90 滑らかな累積比較ポインターと完全結果集合

- 導出状態：**厳密結果**
- 性質：滑らかな結果形成・成立条件
- 対象モデル：M35

作用レジスターを隣接剪断して $Q_j=S_j-u$ を作り、双曲型増幅後に平坦部を持つ滑らかな段差関数を適用する。安全セクター $\mathcal O_k$ は互いに素であり、その内部ではテンプレートが隣接経路により厳密に $e_k$、内部記録が $k$ となる。補集合を正式な無反応結果 $\mathcal O_\varnothing$ とすることで、$\{1,\ldots,L,\varnothing\}$ が全入力を覆う。結果判定は比較ポインターセクターで行い、遷移領域で偶然整数になり得る記録座標だけを使わない。

### R91 無反応込みの任意精度 Born 型長期頻度

- 導出状態：**近似のもとで導出済み**
- 性質：明示誤差上界・固定純粋準備・固定有限基底
- 対象モデル：M35

入力換算比較幅を $w=Xe^{-\Lambda}$、角切断接続領域の Haar 質量を $\varepsilon_{\rm cut}$ とすると、実長期分布

```math
p^{\rm cyc}
=
\left(
p_1^{\rm cyc},\ldots,p_L^{\rm cyc},p_\varnothing^{\rm cyc}
\right)
```

と理想分布の距離は

```math
D_{\rm TV}
\left(
p^{\rm cyc},
\left(
|\langle u_1|\chi\rangle|^2,
\ldots,
|\langle u_L|\chi\rangle|^2,
0
\right)
\right)
=
p_\varnothing^{\rm cyc}
\leq
2(L-1)
\frac{Xe^{-\Lambda}}{I_{\rm ph}}
+
\varepsilon_{\rm cut}
```

を満たす。有限 $L$ では $\Lambda$ と切断幅を選び、任意の $\epsilon>0$ に対して右辺を $\epsilon$ 未満にできる。無反応試行を除いて再規格化しない。

## 4. Bell 側の導出結果

### R63 左右集団交差相関と余弦重み

- 導出状態：**厳密結果**
- 性質：準備した源模型内部
- 対象モデル：M30

反対称な集団交差相関と左右局所回転から、

```math
K_{AB}^{xy}
=
\frac{\mathcal K}{4}
\left[
1-AB\cos\Delta_{xy}
\right]
```

を得る。$K_{AB}^{xy}$ は集団交差相関成分の絶対値2乗であり、単一試行の保存作用ではない。反対称源集団の反復可能な物理準備は未完成である。

### R80 Bell 余弦重みと4成分作用重みの代数的同値

- 導出状態：**厳密結果**
- 性質：仮説依存・成立条件
- 対象モデル：M30、M36

規格化交差相関 $\chi_{\rm B}=\operatorname{rvec}(\Xi_0)/\sqrt{\mathcal K}$ に左右局所回転を作用させると、

```math
w_{AB}^{xy}
=
\frac{K_{AB}^{xy}}{\mathcal K}
=
\frac14
\left[
1-AB\cos\Delta_{xy}
\right]
```

を得る。これは一般有限基底作用重みの $L=4$ 代数的特殊例である。$\chi_{\rm B}$ は局所担体の派生集団交差相関であり、新しい非局所実在場または単一試行変数ではない。

### R73 基準セクター密度を含む Bell 履歴公式

- 導出状態：**厳密結果**
- 性質：仮説依存・成立条件
- 対象モデル：M33、M36

Bell 境界角が候補履歴の残る変数の下で条件付き一様で、結果セクターの基準密度を $q_{AB}^{xy}$ とすると、整合履歴の共同確率は

```math
P(A,B\mid x,y,G)
=
\frac{
q_{AB}^{xy}K_{AB}^{xy}
}{
\sum_{A',B'}
q_{A'B'}^{xy}K_{A'B'}^{xy}
}
```

である。これは付随自由度を含む完全模型の頑健性公式として維持する。未来角の一様性だけでは $q_{AB}^{xy}$ の共通性は従わない。

### R74 共通基準密度下の Bell 共同確率

- 導出状態：**厳密結果**
- 性質：仮説依存・成立条件
- 対象モデル：M33、M36

物理的二側履歴測度が整合事象 $G$ に支持を持ち、各設定対で $q_{AB}^{xy}=q^{xy}$ が4結果に共通なら、

```math
P(A,B\mid x,y)
=
\frac14
\left[
1-AB\cos\Delta_{xy}
\right]
```

を得る。最小模型での共通基準密度はR81で導出するが、完全模型での因子化と整合支持の物理的準備は未完成である。

### R81 局所 Haar 角からの基準セクター密度

- 導出状態：**厳密結果**
- 性質：最小因子化模型
- 対象モデル：M36

左右の局所結果角を独立な Haar 角とし、各翼を設定依存の半円へ分割すると、全設定・全結果について $q_{AB}^{xy}=1/4$ を得る。共通未来角を独立な Haar 角とすると、整合事象は

```math
\nu_{\rm B}^0
\left(
G
\mid
x,y
\right)
=
\frac14
```

を満たす。完全模型へ拡張するには、付随測度、境界 Jacobian 、解多重度、結果別失敗率の因子化が必要である。

### R82 二側履歴測度、設定分布保存、測定設定独立性の破れ

- 導出状態：**厳密結果**
- 性質：仮説依存・ Bell 前提監査
- 対象モデル：M36

$d\mu_{\rm B}=4\mathbf1_G\,d\nu_{\rm B}^0$ とすると、 Bell 余弦共同確率と設定分布 $P(x,y)=\pi_x\pi_y$ を得る。理想反対称重みから非信号周辺と CHSH 値 $2\sqrt2$ が従う。一方、$G_{xy}$ により完全履歴分布は設定依存となり、測定設定独立性は成立しない。$G$ を物理的二側境界法則として採用することは仮説であり、前向き候補生成後の25%除外として解釈しない。

### R66 非信号性と Bell 前提監査

- 導出状態：**厳密結果**
- 性質：仮説依存・ Bell 前提監査
- 対象モデル：M33、M36

R82の対称条件では一側周辺が $1/2$ となり、標準角で CHSH 値 $2\sqrt2$ を得る。一般に

```math
\mu_{\rm B}
\left(
d\Lambda
\mid
x,y
\right)
\neq
\mu_{\rm B}
\left(
d\Lambda
\right)
```

であり、 Bell の前提違反は測定設定独立性にある。一般 Tsirelson 原理と外部設定介入への安定性は未証明である。

### R67 比較器と選択器の理想逆計算

- 導出状態：**厳密結果**
- 性質：理想周期
- 対象モデル：M33、M34、M36

結果を空の外部記録へ可逆にコピーした後、設定からの余弦区間計算、累積比較、戻り伝播、局所分析を逆実行できる。集団相関 $\Xi$ を単一試行で読む操作は含まない。外部記録を含む全状態を同じ点へ戻すことは Hamiltonian では不可能であり、不要情報または外部流路が必要である。

## 5. 置換済み結果IDの扱い

draft-19までのR1--R52は版履歴上の結果IDとして保持するが、現行主張の根拠には使わない。draft-24での位置・測定・Bell 確率機構の扱いは次の通りである。

| 旧結果 | 現在の扱い |
|---|---|
| R1--R12 | 配置拡散、 Fisher 項、線形 Gauss 型作用の補助理論。現行主線に不採用 |
| R13--R20 | 旧3モード Bell 模型と一般測定周期の結果。研究メモまたは未解決問題として保存 |
| R21、R22、R25--R28 | 実在2成分場、位相接続、密度同期、循環量子化。現行主張から撤回 |
| R23、R24 | 作用殻容量と位置入口頻度。draft-21で確率機構から外し、R70--R72、R76--R79へ置換 |
| R29--R38 | Bell 余弦計算のうち集団重みと局所性をR63、R80へ継承。殻容量と共通境界頻度をR73、R74、R81、R82へ置換 |
| R39--R52 | 旧2成分場の準備、交換、弱縮約。現行主線に不採用 |
| R61 | 2モード作用殻による位置入口頻度。R70、R71へ置換 |
| R64 | 比較作用と2モード殻容量。補助的幾何結果として版履歴に保存し、現行 Bell 確率機構には使わない |
| R65 | 共通境界測度下の Bell 共同確率。基準密度と二側測度を明示したR73、R74、R81、R82へ置換 |

旧結果の完全な式は過去版で保存する。現行検索結果へ旧主張を混在させないため、完全な旧章を `notes/` へ複製しない。

## 6. 否定的結果と限界

1. 閉鎖線形 Hamiltonian 発展は相関行列を純化しない。
2. 一般非線形 Hamiltonian では相関行列方程式が高次モーメントへ結合する。
3. M37は実対称 $h_L$ の局所結合形を位置ばね網へ対応させるが、$\mathcal J_0$ と有効質量 $m$ の普遍値を導かない。
4. 古典振動子による有限次元 Schrödinger 方程式の表示自体は既知である。
5. 相関行列と干渉節だけでは粒子の連続運動を決めない。
6. 選択器角の周辺一様性だけでは Born 型則に足りず、作用分配に対する条件付き一様性が必要である。
7. 一般には比の平均と平均の比は一致しない。
8. 同じ状態の任意基底測定には、単粒子側の調製相関が基底に依存しないことが必要である。
9. 位相担体の空間セル選択は、粒子の局所位置検出を自動的に与えない。
10. 旧2モード作用殻と共通入口流束は現行 Born 型機構に使わない。
11. Bell 左右交差相関 $\Xi$ は単粒子側の正半定値相関行列 $C$ ではなく、単一試行の正準変数でもない。
12. 反対称交差相関と局所回転だけでは Bell 頻度は生じない。
13. Bell 未来選択器は、先に記録された局所結果を生成または変更しない。
14. 一様 Bell 未来角だけでは、結果セクターの基準密度を共通にしない。最小模型では独立局所 Haar 角と半円分割が追加で必要である。
15. 不整合周期を観測後に捨てれば事後選別になる。
16. Bell の定理は否定されず、測定設定独立性が成立しない。
17. 観測条件付き設定分布と外部設定介入後の分布は一般に同じでない。
18. 異なる結果履歴を外部記録ごと同じ位相点へ押しつぶす完全再初期化は Hamiltonian ではない。
19. 現 Bell 模型は平面内2出力に限定され、一般 Tsirelson 原理を与えない。
20. 旧2成分場の循環量子化を外したため、 Wallstrom 問題は未解決である。
21. 無理数円回転は一意エルゴード的だが混合的でなく、二項分布型有限標本揺らぎを与えない。
22. 同じ測定装置のテンプレートは測定前状態を保持するため、内部逆計算前の直後再使用には空テンプレートを追加する必要がある。
23. 滑らかな有限時間 Hamiltonian 流は、無反応領域なしに連結した初期領域全体を厳密な離散基底状態だけへ写すことができない。
24. 位置ばね網の局所包絡には反回転項があり、局所作用は一般に厳密保存されない。
25. 厳密正常モード包絡は一般に非局所であり、局所物理場として追加できない。
26. 集団交差相関 $\Xi$ を単一試行の Hamiltonian が直接読み、Bell 余弦区間を作る構成は成立しない。
27. Q2の厳密な局所交換回路は運動量間結合を含み、Q1の通常の位置ばねだけからは導かれない。
28. 1個の時計自由度を各局所辺へ有限速度で配る物理的配線は、局所基底回路の代数からは従わない。

## 7. 物理的解釈

- 各試行で実在する候補は、局所古典振動子、粒子候補、源、選択器、装置、記録、外部自由度である。
- 相関集団測度 $\mu_{\mathcal P,M}$、固定測定周期の $\mu_{\chi,W}^{\rm cyc}$、 Bell 二側測度 $\mu_{\rm B}$ は役割が異なる。全てを統合する $\mu_*$ は将来目標である。
- $b$ は局所振動子から作る包絡、$\widetilde b$ は解析用の正常モード包絡、$d$ は補助有効層の正準担体であり、同じ確立度の変数ではない。
- $C$ と $\Xi$ は指定した調製集団の2次相関であり、独立した物質または単一試行の変数ではない。
- 階数1因子 $\chi$ は共通位相を除いて相対振幅と相対位相を表す。
- 固定純粋準備・固定基底の Born 型長期頻度は、各試行の非負作用、滑らかな累積比較ポインター、安全セクターと無反応セクター、無理数回転 Poincaré 写像の Haar 測度から生じる。
- 条件付き測定後状態は、信号と空テンプレートの正準 SWAP を信号部分へ縮約して得る。
- Bell 余弦重みは反対称な左右集団交差相関と局所回転から代数的に生じ、有限 $L=4$ 作用重みと同型になる。
- Bell 共同頻度は、局所 Haar 基準測度を整合事象 $G$ で条件付けた二側履歴測度から生じる。
- Bell 二側条件付けは終端から過去へ制御力を送る記述ではなく、初期・終端の双方に適合する完結履歴の支持条件である。

## 8. 最重要の未解決問題

1. M37の準備、Q1位置ばね伝播、M35の $QQ+PP$ 局所測定回路、永久外部記録、弱開放resetを同じ明示装置へ統合する。
2. 1個の時計自由度から各局所辺へ制御窓を有限速度で配り、配線遅延と時計誤差をR87、R91の誤差台帳へ接続する。
3. 位相担体の有限基底結果を局所検出器、時間積分流束、無反応を含む完全結果集合へ接続する。
4. 集団交差相関 $\Xi$ を読まず、単一試行の源変数と局所相互作用から Bell 余弦区間多重度を生成する。
5. Bell 整合支持条件 $G$ を有限 Hamiltonian 境界値問題の物理から正当化する。
6. 完全 Bell 模型で基準密度因子化、非対称誤差での非信号条件、外部介入安定性を示す。
7. 可変調製、可変基底、相関観測、Bell 二側条件、外部記録を含む統一 Poincaré 写像と $\mu_*$ を構成する。
8. 高階数相関行列を1本の軌道から生成する源状態用エルゴード自由度を構成する。
9. 無理数回転を混合的なシンプレクティック写像へ強化し、有限標本統計を評価する。
10. 空間グラフの閉路巻数、節、位相すり抜け、連続補間を統合し、Wallstrom 問題へ回答する。
11. 井戸型・調和型ポテンシャル、トンネル効果、時間依存Rabi振動を定量検証する。
12. 強度制約下の動作時間、総エネルギー収支、雑音耐性、完全周期の資源増加を評価する。
13. 平面内2出力を超える一般複合状態と局所測定へ拡張する。

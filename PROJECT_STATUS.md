# 固定長期目標、モデル運用状態、結果の導出状態、理論の境界

この文書は、固定長期目標とその現在地に加え、モデル、導出結果、物理的解釈を分けて管理する。固定目標と可変の進捗、モデルの運用状態、結果の導出状態、付記情報を混同しない。

## 固定長期目標

以下の目標ID、名称、意味、達成判定基準は、通常の論文更新では変更しない。個々の論文や現行モデルが変わっても一覧から削除せず、追加、削除、統合、分割または判定基準の変更が必要な場合は、独立した方針変更として扱う。

### 固定目標一覧

固定目標を、単一量子ビット型装置、複合系・量子計算、空間量子力学の3段階に分ける。IDは通し番号にせず、段階番号と段階内番号を組み合わせた `Q1-1`、`Q1-2` などの形式で管理する。

#### 第1段階：単一量子ビット型装置

| ID | 固定目標 | 達成判定の中心 |
|---|---|---|
| Q1-1 | 単一量子ビット型可逆力学 | Bloch 球に相当する有効状態空間、共通位相不変性、任意の $SU(2)$ 操作を同じ古典 Hamiltonian 担体で実現する。Rabi 振動の振幅、振動数、離調依存性も含める |
| Q1-2 | 射影測定操作 | 任意の Bloch 軸について、2値 Born 重み、排他的結果、測定後固有状態、同軸再測定の反復性、異なる軸による逐次測定を導く |
| Q1-3 | 完全操作・測定周期 | 準備、回転、測定、増幅、永久外部記録、内部逆計算、弱開放 reset、次周期への復帰を1つの明示装置として閉じる |
| Q1-4 | Zeno 効果 | Rabi 型遷移と反復測定を同じミクロモデルで接続し、測定間隔に応じた遷移抑制を導く。摩擦、Hamiltonian の停止、事後選別による抑制とは区別する |

#### 第2段階：複合系・量子計算

| ID | 固定目標 | 達成判定の中心 |
|---|---|---|
| Q2-1 | 2量子ビット型結合ゲート | 2量子ビット型結合ゲートと同一の共同入力--出力統計を生成する有限古典Hamiltonian過程を構成する。結合ゲート内部に非分離状態が実在することを要求しない。 |
| Q2-2 | Bell 型測定統計 | Q2-1の出力を2つの物理的な測定端へ接続し、各端での局所操作・測定・記録から、余弦共同確率、CHSH 不等式の破れ、Tsirelson 限界、非信号性、測定設定独立性の破れを整合的に導く。 |
| Q2-3 | 量子ゲート型計算機の実装コスト | $n$ 量子ビット、回路深さ $d$、目標誤差 $\epsilon$ の回路について、物理自由度、補助装置、動作時間、制御精度、測定・reset コストの増加率を評価し、隠れた指数コストの有無を判定する |

#### 第3段階：空間量子力学

| ID | 固定目標 | 達成判定の中心 |
|---|---|---|
| Q3-1 | 空間 Schrödinger 型有効力学 | 局所位置結合振動子網から空間格子上の Schrödinger 型時間発展を、近似範囲と誤差を伴って導く。連続空間を主張する場合は、制御された連続極限も示す |
| Q3-2 | 位相量子化 | 閉路巻数、節、単価性、位相すべりを一貫して扱い、位相量子化の成立条件を示すことで、Wallstrom 問題へ限定的な回答を与える |
| Q3-3 | 束縛状態 | 1次元の井戸型・調和型ポテンシャルについて有限個の非縮退低位固有状態の固有値、密度、節構造を再現し、有限環境との弱結合を縮約したエネルギー固有基底で、非対角相関の有限時間減衰と対角占有率の安定性を導く |
| Q3-4 | トンネル効果 | 有限障壁を持つSchrödinger型発展で、障壁値より低いエネルギー成分だけからなる状態が障壁反対側へ位置確率を移すことを示し、その確率を位置読出しへ接続する |
| Q3-5 | 2重スリット干渉 | 有限空間グラフ上の2経路入力について、コヒーレント分布が非干渉混合と異なり、相対位相に応じて位置分布が変化することを示し、位置読出しへ接続する |

### Q3-2からQ3-5の達成判定の詳細

固定目標と現行モデルの達成経路を混同しないため、Q3-2からQ3-5のモデル非依存な最低条件を次に固定する。現行モデルによる実現方針と必要な証明は第7章、横断的な反証条件は第8章で管理する。

#### Q3-2 位相量子化

Q3-2は未達のまま凍結中とし、凍結中は新しいモデル構成、証明、数値検証の優先対象にしない。次の条件は再開時の判定基準として保持する。

1. 非零閉路に対して巻数を定義し、その整数性を示す。
2. 零点を通らない連続変形で巻数が保存されることを示す。
3. 巻数変化を、節を介した位相すべりとして記述する。
4. 離散化を細かくしたときの巻数と位相すべりの安定性を示す。
5. 非整数モノドロミーを、数値的な丸めまたは追加の量子化条件によらず排除する。
6. 単価性または整数巻数を別表現で仮定しただけの構成は達成としない。

ここでいう限定的な回答は、位相を持つ波動的自由度を物理模型の一部として許す範囲を指す。密度と流れだけを基本変数とする一般的な確率力学に対する完全解決、節を横切る閉路での巻数、外部ゲージ場を含む一般化は固定達成条件に含めない。

#### Q3-3 束縛状態

1. 1次元の井戸型・調和型ポテンシャルについて、有限個の非縮退低位固有値を求める。
2. 対応する固有状態の密度と節構造を求める。
3. 離散化幅、領域幅その他の近似量に対する収束を示す。
4. 注目系と有限環境のHamiltonian、初期調製、縮約を明示し、外部自由度を読まないという意味で開放系であることを示す。
5. 注目系のエネルギー占有率を保存する結合条件の下で、指定した固有状態基底の非対角相関が有限の観測時間内に減衰することを示す。
6. 同じ観測時間内で対角占有率が保存されるか、その変化を許容誤差以下に抑える。
7. 有限環境の再帰またはコヒーレンス回復が起こる時間尺度と、主張が有効な時間窓を示す。

開放系であるために注目系と環境の間のエネルギー交換は必要ない。全系がHamiltonian発展し、注目系が環境と相互作用し、環境を読まずに縮約されるなら、注目系のエネルギーが保存しても純位相緩和型の開放系になり得る。Q3-3ではこのエネルギー保存型を採用してよい。

1試行ごとの固有状態選択、固有状態を吸引状態とする機構、基底状態への自発的緩和、射影的な状態収縮、独立したエネルギー測定器は固定達成条件に含めない。

#### Q3-4 トンネル効果

1. 有限空間グラフを障壁手前、有限障壁、障壁反対側に分け、Schrödinger型生成子と障壁値を明示する。
2. 初期状態のエネルギースペクトル支持が障壁値より真に低いことを示す。
3. 初期時刻の障壁反対側確率を基準値として明示する。
4. ある有限時刻に、障壁反対側の位置確率が基準値から正の幅だけ増えることを示す。
5. その位置確率を、同じ誤差範囲を保つ位置読出し結果へ接続する。

障壁高・幅・入射エネルギーに対する連続的な透過率曲線、散乱極限、透過・反射・未確定を含む散乱装置、初回到達時刻、吸収、時間積分流束、熱活性化との装置比較は、固定達成条件より強い拡張として扱う。

#### Q3-5 2重スリット干渉

1. 有限空間グラフ上で直交する2経路入力と、その後の同一のSchrödinger型発展を明示する。
2. 2経路のコヒーレント入力と、同じ経路重みを持つ非干渉混合を定める。
3. ある有限時刻の位置分布で、コヒーレント入力と非干渉混合の差が正であることを示す。
4. 2経路間の相対位相を変えると、有限時刻の位置分布が正の幅だけ変化することを示す。
5. これらの位置分布を、同じ誤差範囲を保つ位置読出し結果へ接続する。

固定目標名の「2重スリット」は、この最小の有限グラフ2経路干渉を指す。幾何学的な開口、源、シャッター、多画素スクリーン、全検出器のHamiltonian、無検出を含む完全装置、初回到達、吸収、永久記録、反復resetは、固定達成条件より強い拡張として扱う。

### 現在地

「達成」は、固定された達成範囲で基準を厳密に満たすか、任意精度極限を持つ明示構成を与えた状態を表す。後者は、任意の $\epsilon>0$ に対して誤差が $\epsilon$ 未満となる有限自由度・有限パラメータの構成を実際に選べることを要求する。「条件付き達成」「部分達成」「未達」「未着手」「凍結中」の意味は従来どおりである。M49/R160/R165は固定singlet出力を同じbath・配置registerでM48へ渡し、R151--R156とR166はその後の固定有限設定Bell周期と切断後局所作用殻因子化を閉じる。従ってQ2-2全体を、固定singlet型、固定有限設定族、準備先行、非空間分離、採用開放法則という明示範囲で条件付き達成とする。

| 目標ID | 現在地 | 根拠となる結果 | 残る課題 |
|---|---|---|---|
| Q1-1 | 達成 | R139、R140 | 全W型制御は有限2モード誤差と精度--時間交換を持つ |
| Q1-2 | 部分達成 | R141--R145、R161--R164 | Born型状態数と有効自由エネルギーの条件付き起源を導出。作用容量結合、fiber内平衡化、枝対称性、信号bath反作用の有限局所Hamiltonian統合が残る |
| Q1-3 | 部分達成 | R98、R99、R143--R145、R161--R164 | 連続matching保存は不要化。Hopf pump、fiber、controller、記録、resetを含む周期総収支が残る |
| Q1-4 | 未達（凍結中） | — | 判定基準を保持し、M47上の新規構成・証明・数値検証を凍結 |
| Q2-1 | 達成 | R104--R106、R157--R159、R165 | 中央4枝作用殻の実際の平衡準備とM35運用上界の最小資源は未評価だが、固定達成範囲は満たす |
| Q2-2 | 条件付き達成 | R146--R166、付録K--N | 固定singlet、固定有限設定族、準備先行、非空間分離、採用開放法則に限定。切断後局所殻は完全共通原因に条件付けて積因子化するが、paired-Hopf・作用容量殻・衝突熱浴の同一有限Hamiltonian統合、自由設定変更、空間分離、一般状態は未達 |
| Q2-3 | 未着手 | R79、R89、R100、R102--R106、R157--R159の予備的資源評価 | M49内部の直接符号化で生じる $2^n$ モードコストの監査、$n$ 量子ビット回路の構成、物理自由度、補助装置、動作時間、制御精度、測定・reset コストの規模依存性 |
| Q3-1 | 達成 | R83--R88（固定基準）、R112--R118（強化） | — |
| Q3-2 | 未達（凍結中） | — | 判定基準を保持したまま、新しい構成・証明・数値検証を凍結 |
| Q3-3 | 達成 | R86、R123 | — |
| Q3-4 | 達成 | R113、R116、R117、R124 | — |
| Q3-5 | 達成 | R113、R116、R117、R125 | — |

R165はM49中央4枝作用殻とM35作用区間標本化の同値性、R157--R160は行分解共同bath、CNOT、固定有限共同統計、setting-free受渡しを与える。R151--R156はM48周期、R166は切断後fresh局所作用殻の条件付き積因子化を与える。M50/R161--R164はQ1・Q2に共通なBorn型状態数と条件付き配置熱化を与えるが、同一ハードウェアを意味しない。付録K--Nが受渡し、配置熱化、共通作用殻、Q2特殊化を監査する。

### 直前版IDとの対応

直前版の通し番号と現行の段階別IDの対応だけを示す。それ以前の目標再編履歴は `CHANGELOG.md` で管理し、「旧Q1」のような世代を特定できない表記は現行文書で使わない。

| 直前版ID | 直前版の目標 | 現在の扱い |
|---|---|---|
| Q1 | 単一量子ビット型可逆力学 | Q1-1 |
| Q2 | 射影測定操作 | Q1-2 |
| Q3 | 完全操作・測定周期 | Q1-3 |
| Q4 | Zeno 効果 | Q1-4 |
| Q5 | 一般有限 $L$ への拡張 | 固定長期目標から削除。M35の適用範囲、一般化課題、資源評価として継続 |
| Q6 | 2量子ビット型結合ゲート | Q2-1 |
| Q7 | Bell 型測定統計 | Q2-2 |
| Q8 | 量子ゲート型計算機の実装コスト | Q2-3 |
| Q9 | 空間 Schrödinger 型有効力学 | Q3-1 |
| Q10 | 位相量子化 | Q3-2 |
| Q11 | 束縛状態 | Q3-3 |
| Q12 | トンネル効果 | Q3-4 |
| Q13 | 2重スリット干渉 | Q3-5 |

## 1. モデルの運用状態

| 識別 | モデル | 運用状態 | 役割と限界 |
|---|---|---|---|
| M0 | 局所振動子、実現配置、源、選択器、測定器、結合ゲート、Bell装置、記録、外部流路を持つ弱開放統一試行系 | 将来目標 | M47のQ1装置、M49--M48のQ2周期、M42のQ2-3・Q3実現配置、M37ミクロ場、M35を1つの物理ハードウェアと反復周期へ統合する。単一の統一母測度は未構成 |
| M1 | 1試行内の有限閉鎖 Hamiltonian 系 | 補助モデル | 正準写像、保存量、理想読出し、比較器を厳密に計算する |
| M2--M5 | 運動量結合、二側配置拡散、 Fourier--Gauss 表示、 Gauss 幅 Routh 縮約 | 補助モデル | 配置拡散、 Fisher 項、 Guerra--Morato 作用の研究記録。M0と現行相関行列主線の前提にしない |
| M6 | 最小結果符号化器 | 補助モデル | 既存の2値セクターを指針と記録へ写す。一般的な唯一結果形成ではない |
| M7--M13 | 旧3モード Bell 殻、終端関数、順時間重み、旧位置結合閉包 | 不採用モデル | 詳細と不採用理由は `notes/` で管理する |
| M14、M20--M25 | 実在2成分誘導場、位相接続、局所作用整合浴、低速・高速交換、標本場弱縮約 | 不採用モデル | draft-19までの力学主線。研究メモで置換理由と再利用価値を保存する |
| M15、M29 | 位置入口への局所作用転送と2モード作用殻 | 補助モデル | 旧M15周期は不活性のまま。一般作用殻容量と2作用殻の線形性だけをR164へ再導入し、現行M47の単一試行信号作用、正則化、単一母測度、有効自由エネルギーへ再定式化する |
| M16--M19 | 旧表記のBell対モード、比較器、境界流束、逆計算 | 補助モデル | 旧M30、M33系列へ継承した歴史的構成。2モード殻容量は現行確率機構から外した |
| M26 | 設計済み有限正準複素振幅場網 | 補助モデル | $H=d^\dagger h_Ld$ を持つ理想有効層。測定回路と相関行列の補助計算に使い、Q3-1のミクロ根拠には使わない |
| M27 | 共通源と有限正準準備回路 | 補助モデル | 理想有効層で階数1相関を準備する。任意初期集団からの純化またはM37の物理準備ではない |
| M28 | 相関行列による調製集団の補助統計 | 補助モデル | $C$ を指定集団の2次モーメントとする。ミクロ基礎状態または単一試行変数ではない |
| M30 | 左右複素振幅場の反対称交差相関と局所分析器 | 不採用モデル | 集団余弦重みは与えるが単一試行頻度と完全周期を与えない。M48も交差共分散だけを結果頻度とせず、完全matchingと局所instrumentを別条件にする |
| M31 | 全プログラム共通の試行開始面、周期写像、不変かつエルゴード的な統一母測度 | 補助モデル | 将来の統一目標。具体的生成は未完成で、現行定理の仮定には使わない |
| M32 | 固定作用の正準角と累積作用区間による有限基底選択器 | 現行モデル | 高階数を含む一般作用区間公式を与える。一般集団では条件付き一様性を仮定するが、固定純粋準備ではM35の Poincaré 写像が供給する |
| M33 | Bell境界角、履歴整合事象、一般基準セクター密度 | 不採用モデル | 結果別基準密度の頑健性公式は研究メモへ保存する。終端整合支持を使わないM41へ置換した |
| M34 | 累積、比較、ラッチ、記録、逆計算の正準レジスター | 補助モデル | draft-21の構成要素。M35で固定有限基底の1本の自律周期へ統合した |
| M35 | 一般有限基底の分析・実現配置更新・条件付き場準備モジュール | 現行補助モデル | 一般有限 $L$ について作用区間、滑らかな比較、無反応、内部逆計算を構成。M49ではM50中央殻の枝周辺を作用区間へ押し出す有限Hamiltonian標本器として使うが、殻内熱化の証明ではない |
| M36 | 局所結果角と未来整合角を持つ最小Bell二側履歴モデル | 不採用モデル | 終端整合支持を独立仮定として必要とするため、前向き初期共通原因型M41へ置換した。旧論証と再検討条件は研究メモで管理する |
| M37 | 局所位置結合された有限実古典振動子網 | 現行モデル | $q,p$ と実ばね結合から局所包絡を作り、弱結合・有限時間で実対称 $h_L$ のSchrödinger型複素振幅場を誤差付きで導く。M42共通モデルへQ3の場を供給するが、実現配置または最小率を単独では与えない |
| M38 | 2頂点場・実現配置の操作、逐次測定、弱開放完全周期 | 置換済みモデル | 旧Q1のBloch球、任意の $SU(2)$、Rabi振動、M42/R113読出し、固定有限Zeno周期を構成した。現行Q1ではM47/R139--R145に置換し、旧定理と検証は `notes/superseded_m38_m42_q1.md` とGit履歴へ保存する |
| M39 | 4頂点共同配置の2論理部分系・結合ゲート | 置換済みモデル | 4頂点場とM42最小率でCNOT共同入力--出力統計を構成した旧Q2-1模型。R104--R106の有限正準担体はM49へ再利用し、旧R118・R120適用とR122は `notes/superseded_m39_m42_q2_1.md` とGit履歴で管理する |
| M40 | AからBへの逐次依存型Bell測定 | 不採用モデル | A局所結果とA設定をB条件付き準備へ送るため、測定段階の局所因子化を満たさない。M41と因果仮定が異なるため研究メモだけで管理する |
| M41 | 初期共通原因型2端Bell測定周期 | 置換済みモデル | R107--R111、R121の有限Hamiltonian周期を数学的に保存するが、draft-45B以後のQ2-2根拠には使わない。本文と付録から外し、`notes/superseded_m41_bell_cycle.md` と `notes/superseded_m41_cycle_proofs.md` で管理する |
| M42 | 有限配置グラフ上の複素振幅場-実現配置モデル | Q2-3・Q3限定の現行モデル | 任意有限配置グラフの複素振幅場 $b$、実現配置 $X$、局所辺流、最小率、鋭い基準配置からの準備、節一様正則化、有限Hamiltonian更新、履歴、固定時刻検出を統合する。Q1、Q2-1、Q2-2の因果鎖には使用しない |
| M43 | 固有モード作用結合型有限環境 | 現行補助モデル | $K$ 個の系モード作用と $K$ 個の環境運動量を結ぶ有限自律Hamiltonian。環境を縮約すると全非対角相関が同じ有限時刻に消え、対角占有率と注目系エネルギーは厳密保存される。有限環境なので完全回復を持ち、不可逆熱浴または状態選択は含まない |
| M44 | 有限Hamiltonian捕捉エントロピー準備模型 | 不採用モデル | 多数の内部調和モードによる指数位相体積と対称準備作用素を提案したが、基準位置核、時間比例選別、反復帰還、周辺可逆性を一つの有限Hamiltonianから導けなかった。本文から外し、再利用可能な計算と不採用理由を `notes/` へ保存する |
| M45 | 開放自己組織化準臨界準備模型 | 現行補助モデル | 周期反応座標 $s$、対数型捕捉エントロピー座標 $r$、熱雑音、受動散逸、Rayleigh型能動供給を明示する。R127とR128は準臨界殻、自律帰還、指数位相体積を扱い、R129は局所帳簿、時間比例切片則、周辺可逆性を仮定した後の定常基底密度とNelson則を与える |
| M46 | 共通作用ポート型三角形capacity-current統合開放模型 | 不採用モデル | 物理的複素振幅場からcurrent rateを作って実現配置を動かす因果律が、複素振幅を実現配置--浴共同統計として定義するM47と両立しない。R130--R132の再利用可能な補助計算だけを保持し、R133、R134は不採用模型内の結果として `notes/rejected_m46_current_transducer.md` で管理する |
| M47 | W型2モードHopf準備・条件付き作用殻・Gibbs配置緩和・傾斜測定模型 | 現行Q1モデル | M50の $m=2$ 特殊化を使い、R145の信号方向準備、R161--R164の作用殻状態数・配置熱化、R139--R144の操作・記録を合成する。Q1-1達成、Q1-2・Q1-3部分達成、Q1-4凍結を維持 |
| M48 | 設定依存paired-Hopf初期共通原因Bell周期模型 | 現行Q2-2 receiverモデル | M48単独の対称2枝作用殻またはM49中央4枝状態数からseedを得る。paired-Hopfは共有rayを準備し、R166は切断後fresh局所作用殻を完全共通原因に条件付けて積因子化する。条件付き達成の範囲は維持 |
| M49 | 行分解共同bath--実現配置CNOT供給模型 | 現行Q2-1モデル・Q2-2供給模型 | M50中央4枝作用殻、M35作用区間標本器、行分解bath、同期CNOT、fresh出力殻を使う。R165が共同Born状態数、R159が固定有限共同統計、R160が固定singlet受渡しを与える |
| M50 | 有限信号作用・作用殻・配置熱化共通モジュール | 現行Q1・Q2共通補助モデル | $v\in\mathbb C^m$、等長埋込み、各枝2作用殻からBorn型状態数を作り、必要に応じてR161--R163の配置熱化へ接続する。Q1、Q2-1、Q2-2の特殊化を統一する理論部品であり、同一ハードウェアまたは統一母測度は主張しない |

M0はM47、M49、M48、M50、M42、M37、M35と必要な記録・外部流路を同じ物理反復系へ統合する目標モデルである。M50はQ1とQ2のBorn型状態数・条件付き熱化の記述を統一するが、物理ハードウェアは統合しない。M42を全面退役させるにはQ3の固定時刻位置読出しを別模型で再導出する必要がある。

## 2. 置換済みM38結果と保持する一般装置結果

R92--R96とR100は旧M38/M42のQ1構成として数学的内容を保存するが、現行Q1の根拠には使わない。R101--R103は旧Zeno構成として凍結し、`notes/frozen_q1_zeno.md` で管理する。R97の連続性障害とR98、R99の可逆記録・交換resetは模型に依存しない一般装置結果として保持し、M47のR143、R144で再利用する。

### R92 固定作用面の共通位相縮約とBloch 球

- 導出状態：**置換済み模型内の厳密結果**
- 性質：旧M38結果。現行Q1ではR139に置換
- 対象モデル：M38

$I_{\rm ph}=\mathcal J_0$ の2モード正準担体について、共通位相に不変な操作・測定代数へ縮約すると

```math
S^3/U(1)
\simeq
\mathbb{CP}^1
\simeq
S^2
```

を得る。Bloch 成分は

```math
\{r_i,r_j\}
=
\frac{2}{\mathcal J_0}
\epsilon_{ijk}r_k
```

を満たす。共通位相変数が古典位相空間に存在しないとは主張せず、採用代数が識別しない方向として扱う。

### R93 同じ2頂点複素振幅場上の任意のSU(2)

- 導出状態：**置換済み模型内の厳密結果**
- 性質：旧M38結果。現行Q1ではR140に置換
- 対象モデル：M38

局所位相回転 $G_{Z,j}=(Q_j^2+P_j^2)/2$ と交換 $G_X=Q_1Q_2+P_1P_2$ の有限列は、Euler 分解により任意の $U\in SU(2)$ を同じ2頂点複素振幅場上で生成する。全パルスは $I_{\rm ph}$ と正準性を保存し、逆操作は順序と符号の反転で得る。

### R94 自律Rabi Hamiltonian と遷移公式

- 導出状態：**置換済み模型内の厳密結果**
- 性質：旧M38結果。現行Q1ではR140の傾斜Rabi式に置換
- 対象モデル：M38

駆動作用・角変数を加えた有限時間非依存Hamiltonian は、回転座標で

```math
i\dot c
=
\frac12
\left(
\Delta\sigma_z+\Omega\sigma_x
\right)c
```

となる。極状態からの遷移作用比は

```math
P_{1\to2}(t)
=
\frac{\Omega^2}{\Omega^2+\Delta^2}
\sin^2
\left(
\frac{\sqrt{\Omega^2+\Delta^2}}{2}t
\right)
```

であり、共鳴振幅、振動数、駆動強度、離調依存性を厳密に与える。M37の位置ばね近似は使わない。

### R95 任意の Bloch 軸の排他的安全結果と測定後状態

- 導出状態：**置換済み模型内の厳密結果**
- 性質：旧M38/M42結果。現行Q1ではR141--R143に置換
- 対象モデル：M38

任意軸 $\boldsymbol n$ と結果 $s=\pm1$ について、2モード作用比は

```math
p_s(\boldsymbol n)
=
\frac{1+s\boldsymbol n\cdot\boldsymbol r}{2}
```

である。M35型比較器の安全セクターは排他的で、安全結果 $s$ の正準SWAP 後の信号は厳密に $|\boldsymbol n,s\rangle$ となる。補集合は正式な無反応結果であり、除外して再規格化しない。

### R96 2選択器による同軸・異軸逐次測定

- 導出状態：**置換済み模型内で近似のもと導出済み**
- 性質：旧M38/M42結果。現行Q1ではR143の逐次測定に置換
- 対象モデル：M38

同軸再測定では反対符号の安全結果が生じない。異軸 $\boldsymbol n,\boldsymbol m$ について、独立な2選択器角を2次元トーラス上で一意エルゴード的に進めると、零幅極限の共同頻度は

```math
P(s,t)
=
\frac{1+s\boldsymbol n\cdot\boldsymbol r}{2}
\frac{1+st\boldsymbol n\cdot\boldsymbol m}{2}
```

となる。有限幅で無反応を含む実分布と理想分布の全変動距離は $\delta_1+\delta_2$ 以下である。独立同分布または二項分布型有限標本揺らぎは含まない。

### R97 滑らかな厳密2値測定の連続性障害

- 導出状態：**厳密結果**
- 性質：否定的結果
- 対象モデル：一般有限Hamiltonian装置、M35、M47

滑らかな有限時間Hamiltonian 流から得る連続写像は、連結入力領域を相異なる2出力点だけへ写し、両点を実現することができない。滑らかさ、連結入力、無反応なし、全入力で厳密2固有状態という4条件は同時に成立しない。M47のR143も安全セクター間に無反応遷移領域を置く。

### R98 外部記録への正準コピーと内部逆計算

- 導出状態：**厳密結果**
- 性質：一般装置補題・理想空記録セル・反作用上界
- 対象モデル：M35、M47

$G_{\rm rec}=\sum_jP_j^R\Pi_j(z)$ により、比較ポインターと角切断指標から作る滑らかな結果コードを外部記録座標へコピーする。理想入口 $P_j^R=0$ では装置への反作用は零である。記録後に第2段、第1段の順で逆計算すると、内部装置は準備点へ戻り、外部記録は残る。有限な $P_j^R$ 幅による反作用は状態誤差へ評価する。

### R99 外部空モード交換 reset と収縮上界

- 導出状態：**厳密結果**
- 性質：一般装置補題・誤差上界・弱開放
- 対象モデル：M35、M47

装置偏差 $\delta a$ と外部セル偏差 $\eta$ の正準交換は

```math
\delta a^+
=
\cos\phi\,\delta a^-
+
\sin\phi\,\eta
```

を与える。$\phi=\pi/2$ では完全交換、$|\cos\phi|<1$ では収縮である。周期残差を $\varepsilon_{\rm cyc}$、外部セル幅を $\sigma_E$ とすると

```math
\limsup_{n\to\infty}
\|\delta a_n\|
\leq
\frac{
\varepsilon_{\rm cyc}
+
|\sin\phi|\sigma_E
}{
1-|\cos\phi|
}
```

を得る。旧装置状態は使用済み外部セルへ移り、消去されない。

### R100 2モード弱開放完全周期

- 導出状態：**置換済み模型内で近似のもと導出済み**
- 性質：旧M38/M42主定理。現行Q1ではR144に置換
- 対象モデル：M38

固定純粋入力、固定された有限 $SU(2)$ 操作列、固定された2軸、任意の $\epsilon>0$ に対し、有限能動装置と外部記録・reset セル流を構成できる。無反応を含む2段結果分布、記録誤差、周期末準備集合からの偏差を全て $\epsilon$ 未満に選べる。理想Poincaré 写像は2選択器角の平行移動を除いて恒等写像である。

能動装置の単純上界は17正準対、外部セルは1周期当たり記録2対とreset 最大14対である。有限 $K$ 実験周期は有限閉鎖Hamiltonian 系へ埋め込めるが、無期限運転には外部セル流が必要である。M37との同一ハードウェア化、一般有限 $L$、熱力学的最小コストは含まない。

### R101 有限反復測定の理想履歴則と短時間有効反転率

- 導出状態：**凍結した旧模型内の厳密結果**
- 性質：旧M38/M42のZeno履歴。現行Q1の根拠に使わない
- 対象モデル：M38

初期符号 $s_0=+1$、固定有限測定回数 $N_{\rm Z}$、第 $j$ 区間のRabi 反転作用比 $q_j$ について、理想履歴分布は

```math
\pi_{N_{\rm Z}}
(s_1,\ldots,s_{N_{\rm Z}})
=
\prod_{j=1}^{N_{\rm Z}}
\left[
(1-q_j)\mathbf1_{s_j=s_{j-1}}
+
q_j\mathbf1_{s_j=-s_{j-1}}
\right]
```

である。一度も反転しない確率と最終正占有率は

```math
S_{N_{\rm Z}}
=
\prod_{j=1}^{N_{\rm Z}}(1-q_j),
```

```math
F_{N_{\rm Z}}
=
\frac12
\left[
1+
\prod_{j=1}^{N_{\rm Z}}(1-2q_j)
\right]
```

となる。等間隔の短時間域では

```math
\Gamma_{\rm Z}(\tau)
=
-\frac{\log[1-q(\tau)]}{\tau}
=
\frac{\Omega^2}{4}\tau
+
O(\tau^3)
```

である。無反応、反転、再反転を除外せず、各構成を同じ総時間の未測定対照と比較する。異なる $N_{\rm Z}$ の間で総観測時間を共通に固定しない。

### R102 単一測定コアと循環セル列の正準合成

- 導出状態：**凍結した旧模型内の厳密結果**
- 性質：旧M38/M42のZeno装置。現行Q1の根拠に使わない
- 対象モデル：M38

1個の測定コア、$N_{\rm Z}$ 個の独立選択器、$N_{\rm Z}$ 個の補助状態交換セル、$N_{\rm Z}$ 個の永久記録セルを使う。各段でテンプレート2対、作用レジスター2対、閾値1対、内部記録1対だけを段セルへSWAPし、信号は次のRabi 区間へ残す。全結果記録後に段セル、測定写像、Rabi 区間を逆順で戻すと、内部装置は復帰し、永久記録だけが残る。

固定有限 $N_{\rm Z}$ の選択器ベクトルは、$1,\alpha_1,\ldots,\alpha_{N_{\rm Z}}$ の有理数体上の1次独立性の下で積Haar 測度を持つ。1回の有限観測の単純上界は $10+8N_{\rm Z}$ 正準対、$K$ 実験周期で段セルと選択器を再利用し全記録を保持する上界は $10+7N_{\rm Z}+KN_{\rm Z}$ 正準対である。輸送自由度と追加の残差補正流は含まない。

### R103 継続Rabi 駆動下の有限Zeno周期

- 導出状態：**凍結した旧模型内で近似のもと導出済み**
- 性質：旧M38/M42のZeno主定理。現行Q1の根拠に使わない
- 対象モデル：M38

全前向き測定窓でRabi 項を非零に保つ。幅 $h_j$ の測定窓では、継続Rabi 駆動下の実写像と理想測定写像の差を固定作用殻上で $C_jh_j$ 以下に抑える。第 $j$ 前向き段の条件付き全変動誤差を $\eta_j^{\rm fwd}$ とすると、無反応を含む全履歴分布は

```math
D_{\rm TV}
\left(
p_{N_{\rm Z}},
\pi_{N_{\rm Z}}
\right)
\leq
1-
\prod_{j=1}^{N_{\rm Z}}
\left(
1-\eta_j^{\rm fwd}
\right)
\leq
\sum_{j=1}^{N_{\rm Z}}
\eta_j^{\rm fwd}
```

を満たす。逆計算と周期末 resetは観測後なので、同じ観測の分布誤差ではなく周期帰還誤差として分ける。任意に指定した有限 $N_{\rm Z}$ と任意の $\epsilon>0$ に対し、前向き分布誤差と周期帰還誤差をともに $\epsilon$ 未満へ選べる。$N_{\rm Z}\to\infty$、完全凍結、全時間域の単調抑制、事後選別、測定中のRabi 停止は含まない。

## 3. 4モード2論理部分系・結合ゲートの導出結果

### R104 4モード局所操作代数による操作誘導テンソル積

- 導出状態：**厳密結果**
- 性質：R158、R159を支える補題・単一担体内の論理分解・成立条件
- 対象モデル：M49内部プログラム担体。M39への旧適用は置換済み記録

固定作用4頂点複素振幅場へ

```math
\mathcal A
=
M_2(\mathbb C)\otimes I_2,
\qquad
\mathcal B
=
I_2\otimes M_2(\mathbb C)
```

に対応する2つの局所制御代数を正方形配線として実装する。両代数は相互に可換で、互いの可換代数に一致し、4次元振幅空間へ操作誘導テンソル積を定める。係数行列

```math
B(b)
=
\begin{pmatrix}
b_{00}&b_{01}\\
b_{10}&b_{11}
\end{pmatrix}
```

について、積状態の必要十分条件は $\det B=0$ であり、局所操作 $B\mapsto U_ABU_B^{\mathsf T}$ は行列階数を保存する。これは1つの4モード物理担体内の論理テンソル積であり、2つの独立位相空間への物理分解ではない。

### R105 差モード射影による明示CNOT流と非因子化生成

- 導出状態：**厳密結果**
- 性質：R158、R159を支える補題・有限自律Hamiltonian ・作用保存・局所分解不能
- 対象モデル：M49内部プログラム担体。M39への旧適用は置換済み記録

下側反対称差モードへの射影を

```math
\Pi_{\rm CX}
=
|1\rangle\langle1|_A
\otimes
\frac{I_2-X_B}{2}
```

とする。実2次生成子は

```math
G_{\rm CX}
=
\frac14
\left[
(Q_{10}-Q_{11})^2
+
(P_{10}-P_{11})^2
\right].
```

$H=P_\tau+g(\tau)G_{\rm CX}$ の滑らかな時計窓が面積 $\pi$ を持つと、信号流は $e^{-i\pi\Pi_{\rm CX}}=U_{\rm CX}$ となる。4論理基底上でCNOTと厳密に一致し、積入力 $|+0\rangle$ を $(|00\rangle+|11\rangle)/\sqrt2$ へ写す。出力係数行列は階数2なので、局所操作の積へ分解できない。

### R106 4頂点CNOT場の有限時間安定性と資源上界

- 導出状態：**厳密式と明示誤差上界**
- 性質：R158、R159を支える補題・固定作用面・有限時間・明示誤差・場部分の資源上界
- 対象モデル：M49内部プログラム担体。M39への旧適用は置換済み記録

固定作用面では $0\leq G_{\rm CX}\leq\mathcal J_0$ である。$|g|\leq g_{\max}$ の非負パルスには $T_{\rm g}\geq\pi/g_{\max}$ が必要である。面積誤差を $\delta A$ とすると、平均ゲート忠実度は

```math
F_{\rm avg}
=
1
-
\frac35
\sin^2\frac{\delta A}{2}
```

であり、固定ゲージ距離と共通位相縮約距離は

```math
d_{\rm mat}
=
2
\left|
\sin\frac{\delta A}{2}
\right|,
\qquad
d_{\rm proj}
=
2\sin\frac{|\delta A|}{4}
```

である。一般Hermitian 制御誤差は、共通位相成分を除いた積分作用素ノルム $\eta_{\rm g}$ により、操作距離を $\min\{2,\eta_{\rm g}\}$、任意入力の非忠実度を $\min\{1,\eta_{\rm g}^2\}$ 以下に抑える。

ゲート場単体は4信号正準対と1時計正準対、合計5正準対である。M35の固定作用区間selectorは16正準対だが、共同bath、配置register、履歴、検出を含まない。共同入力--出力統計はR157--R159、1辺位置ばねによる場近似の旧案はR122で別に管理する。非因子化生成は内部診断であり、未知入力、完全過程トモグラフィー、独立同分布型有限標本統計、多量子ビットの多項式資源拡張は主張しない。

## 4. ミクロ振動子網、空間複素振幅場、実現配置の導出結果

### R83 局所包絡の反回転項を含む厳密方程式

- 導出状態：**厳密結果**
- 対象モデル：M37

```math
H_{\rm micro}
=
\frac{p^{\mathsf T}p}{2M_{\rm osc}}
+
\frac12q^{\mathsf T}
\left(
M_{\rm osc}\omega_0^2I+A
\right)q
```

に対し、局所回転包絡は

```math
i\mathcal J_0\dot b
=
h_0b
+
h_0e^{2i\omega_0t}\overline b,
\qquad
h_0
=
\frac{\mathcal J_0A}
{2M_{\rm osc}\omega_0}
```

を厳密に満たす。位置結合だけでは反回転項を厳密に消せず、局所包絡作用は一般に厳密保存されない。

### R84 正常モード包絡と厳密生成子

- 導出状態：**厳密結果**
- 対象モデル：M37

```math
\Omega
=
\left(
\omega_0^2I
+
\frac{A}{M_{\rm osc}}
\right)^{1/2}
```

を使う正常モード正準包絡 $\widetilde b$ は

```math
i\mathcal J_0\dot{\widetilde b}
=
h_{\rm ex}\widetilde b,
\qquad
h_{\rm ex}
=
\mathcal J_0
\left(
\Omega-\omega_0I
\right)
```

を満たし、$\mathcal J_0\widetilde b^\dagger\widetilde b$ を厳密保存する。$\widetilde b$ は行列平方根を含み、一般には非局所である。

### R85 目標演算子との作用素誤差

- 導出状態：**厳密結果**
- 性質：誤差上界
- 対象モデル：M37

$h_0=h_L$、

```math
\eta
=
\frac{2\left\|h_L\right\|}
{\mathcal J_0\omega_0}
<1
```

なら、

```math
\left\|h_{\rm ex}-h_L\right\|
\leq
\frac{
\left\|h_L\right\|^2
}{
2\mathcal J_0\omega_0
\left(1-\eta\right)^{3/2}
}
```

である。実ばね係数と離調を選ぶことで、実対称なグラフ Laplacian と局所ポテンシャルに係数対応できる。

### R86 局所包絡の有限時間 Schrödinger 型近似

- 導出状態：**近似のもとで導出済み**
- 性質：有限時間誤差上界
- 対象モデル：M37

実際の局所包絡と、同じ局所初期値から始める目標解 $b_L=e^{-ih_Lt/\mathcal J_0}b(0)$ の差は

```math
\sup_{0\leq t\leq T}
\left\|b(t)-b_L(t)\right\|
\leq
\varepsilon_{\rm car}(T)
\left\|\widetilde b(0)\right\|
```

で抑えられる。$\varepsilon_{\rm car}$ は局所・正常モード変換差2項とR85の時間積分からなり、$T=O(\mathcal J_0/\|h_L\|)$ で $O(\eta)$ である。時間非依存・実対称・有限次元に限定する。

### R87 包絡誤差から有限基底分布誤差への伝播

- 導出状態：**厳密結果**
- 性質：誤差伝播
- 対象モデル：M35、M37

規格化した実際の局所包絡 $\widehat b_{\rm mic}$ と目標状態 $\chi_L$ に対し、任意の有限基底 $W$ で

```math
D_{\rm TV}
\left(
p^{\rm mic},p^L
\right)
\leq
\sqrt{
1-
\left|
\left\langle
\widehat b_{\rm mic},
\chi_L
\right\rangle
\right|^2
}
```

が成立する。R86から右辺を $O(\eta)$ で抑えられる。比較器、角切断、パルス、準備誤差は別に加える。

### R88 8振動子鎖の数値収束

- 導出状態：**数値検証**
- 対象モデル：M37

8振動子の1次元鎖と弱い調和型離調で、$\omega_0=20,40,80$ を比較した。作用素誤差、厳密包絡状態誤差、局所包絡状態誤差、局所作用変動、任意基底の全変動距離が解析上界を満たし、弱結合極限で概ね $O(\eta)$ で減少した。`tools/verify_envelope_reduction.py` で再現する。

### R112 有限配置グラフ模型のQ2--Q3共通構造

- 導出状態：**モデル定義と共通構造**
- 性質：定理ではない共通有効モデル・同一ハードウェアではない
- 対象モデル：M35、M37、M42、M49内部担体。M39、M41への旧適用は置換済み記録

有限配置グラフ $\mathcal G=(\Omega,E)$ 上の複素振幅場 $b$ と実現配置 $X\in\Omega$ を共通変数とする。Hermitian生成子 $h(t)$ は $i\mathcal J_0\dot b=h(t)b$ を与える。$\Omega=\{00,01,10,11\}$ はQ2、空間セル集合はQ3となる。差モード辺射影 $\Pi_e$ の生成子は

```math
\mathcal J_0b^\dagger\Pi_eb
=
\frac14
\left[
(Q_i-Q_j)^2
+
(P_i-P_j)^2
\right]
```

である。Q2 CNOTとQ3 Laplacian伝播は、頂点作用項と同じ辺生成子の異なるグラフプログラムである。M49内部担体の厳密 $QQ+PP$ 実装と、M37の位置ばねによる弱結合近似は同じハードウェアではない。Q1はこの共通構造から外し、M47のW型2モード共同統計で扱う。

### R113 局所辺流と理想実現配置のBorn型等変性

- 導出状態：**厳密結果**
- 性質：初期分布条件・既知最小率の有限グラフ適用
- 対象モデル：M42（Q2-3・Q3に限定）

理想担体の重み $p_i=|b_i|^2$ と局所辺流

```math
J_{i\to j}
=
\frac{2}{\mathcal J_0}
\operatorname{Im}
\left(
b_j^*h_{ji}b_i
\right)
```

は離散連続方程式を満たす。余分な対称往復流を持たない最小率

```math
\lambda_{i\to j}
=
\frac{[J_{i\to j}]_+}{p_i}
```

をM42の理想則として採用する。$P(X_0=i)=p_i(0)$ なら $P(X_t=i)=p_i(t)$ が等変に保たれ、$\mathbb E[N_T]\leq h_1T/\mathcal J_0$ により有限時間爆発は起こらない。連続方程式だけでは率は一意でなく、等変性だけでは初期分布を生成しない。最小率と大域存在自体は既知結果であり、本稿の新規性は有限Hamiltonian装置とM37への接続にある。

### R114 一様有界率Markov過程の有限時間厳密節追跡no-go

- 導出状態：**厳密結果**
- 性質：否定的結果・適用範囲限定
- 対象モデル：M42

全脱出率が $\Lambda_i(t)\leq\Lambda_{\max}<\infty$ なら、$q_i(s)>0$ から

```math
q_i(t)
\geq
q_i(s)e^{-\Lambda_{\max}(t-s)}
>
0
```

が従う。一様有界率の連続時間Markov跳躍過程は正の頂点占有を有限時間で厳密零へ送れない。一般の決定論的Hamiltonian流、有限時間輸送、離散Poincaré写像を禁止する結果ではない。

### R115 節一様の滑らかな有限率正則化

- 導出状態：**厳密結果**
- 性質：誤差上界・軌道結合
- 対象モデル：M42

無次元の $\rho>0$ と率次元の $\sigma>0$ に対し、

```math
r_\sigma(z)
=
\frac12
\left(
z+\sqrt{z^2+\sigma^2}
\right),
\qquad
\lambda_{i\to j}^{\rho,\sigma}
=
\frac{r_\sigma(J_{i\to j})}{p_i+\rho}
```

とする。最大率は有限で、初期分布を合わせた正則化分布は

```math
\sup_{0\leq t\leq T}
D_{\rm TV}
\left(
q^{\rho,\sigma}(t),p(t)
\right)
\leq
T
\left[
|E|\sigma
+
\frac{2H_E}{\mathcal J_0}
\sqrt\rho
\right]
```

を満たす。同じ上界で理想過程との有限時間軌道結合を構成できる。有限正則化は小さな逆向き跳躍を含み、有限装置での節占有は任意に小さくできるが厳密零ではない。

### R116 共通実現配置過程の有限Hamiltonian近似

- 導出状態：**明示構成・制御された任意精度**
- 性質：有限自由度・有限時計・無反応込み
- 対象モデル：M35、M42。M41への旧適用は置換済み記録

鋭い基準配置から同じ場準備回路を作用させるか、任意初期場にM35型作用区間を使い、実現配置を初期準備する。その後は現在配置と隣接辺だけを読み、待機または1辺を排他的に選び、1個の局在正準自由度を選択辺通信路内で輸送する。異なる旧位置と枝を履歴へ残すため全写像は1対1である。固定 $K$ は有限閉鎖Hamiltonian系へ埋め込め、無期限運転は新規セル流を持つ弱開放系となる。単純資源上界は $O(K(|\Omega|+|E|))$ である。

### R117 M37ミクロ場から空間実現配置分布への有限時間誤差伝播

- 導出状態：**近似のもとで導出済み**
- 性質：有限時間誤差上界・成立条件
- 対象モデル：M37、M42

固定 $\rho,\sigma>0$ では正則化生成子が複素振幅の有界球上でLipschitz連続である。R86の包絡誤差を

```math
\varepsilon_{\rm car\to X}
\leq
TL_{\rho,\sigma,h,R}
\varepsilon_{\rm car}(T)
\|\widetilde b(0)\|
```

として空間実現配置分布へ渡せる。任意の固定有限時間と精度について、$\rho,\sigma$、M37弱結合量 $\eta$、刻み $\Delta$、装置精度の順に有限値を選べば、全更新時刻の位置分布を目標 $|b_L|^2$ へ任意精度で近づけられる。固定帯域の同じ装置による誤差零極限、連続空間、多粒子、質量・電荷・全エネルギーは含まない。

### R118 鋭い基準配置からのQ3準備

- 導出状態：**厳密結果**
- 性質：R112のQ3適用について初期分布の別指定を不要にする準備橋
- 対象モデル：M42。M39、M41への旧適用は置換済み記録

$b=e_{z_0}$、$X=z_0$ から開始し、同じ固定準備回路を場と実現配置へ作用させると、準備終了時に $P(X=z)=|b_z|^2$ となる。任意場を準備履歴なしに直接与える場合は、M35型作用区間による初期配置準備を別に要する。Q1とQ2-1の準備根拠には使わない。

### R119 2頂点実現配置による操作・測定周期

- 導出状態：**置換済み模型内の明示構成**
- 性質：旧R100、R103を支えたQ1補題。現行Q1ではR143、R144に置換
- 対象モデル：M38、M42

2頂点の場と実現配置へ同じ準備、$SU(2)$、分析器プログラムを作用させ、分析器出口の $X$ を結果として読む。検出後は $X$ を制御ラッチにして場を対応固有状態へ条件付き準備する。同軸反復、異軸逐次分布、永久記録、帰還、固定有限Zeno履歴を無反応込みで任意精度へ近づけられる。

### R120 共同実現配置によるCNOT入力--出力統計

- 導出状態：**置換済み模型内の明示構成・制御された任意精度**
- 性質：draft-46までのQ2-1主定理。現行ではR157--R159へ置換
- 対象モデル：M39、M42

任意の有限固定入力、固定積出力基底、入力頻度について、鋭い基準配置から準備、CNOT、積分析器を通す。出力を共同実現配置 $X=(X_A,X_B)$ として読み、無反応を含む入力ラベルと2ビット出力の共同分布を理想CNOT分布へ任意精度で近づけられる。M35の作用区間結果を独立の出力には使わない。

### R121 共同実現配置によるM41 Bell測定周期

- 導出状態：**置換済み模型内の明示構成・制御された任意精度**
- 性質：draft-44までのQ2-2主定理・操作的接続。draft-45BではR151--R156へ置換
- 対象モデル：M39、M41、M42

鋭い基準共同配置からsinglet型場・配置を準備し、中央A分析器後に $X_A$ を読み、条件付き2端場とB配置を準備する。中央結合停止後、局所分析器で $X_B$ を読み、永久記録、逆計算、弱開放resetへ接続する。理想余弦分布、非信号性、CHSH値を与えるが、A設定を中央処理に使うため測定設定独立性は成立しない。

### R122 1辺位置ばねによるCNOT場の有限時間近似

- 導出状態：**置換済み模型内の近似結果**
- 性質：R120の系・旧M39--M37場部分のミクロ橋。現行Q2-1根拠に使わない
- 対象モデル：M37、M39

$h_L=\alpha\Pi_{\rm CX}$、$T_{\rm CX}=\pi\mathcal J_0/\alpha$ とする。4振動子の1辺位置ばね網で、$\eta=2\alpha/(\mathcal J_0\omega_0)<1$ のとき、局所場と理想CNOT出力のノルム誤差は

```math
2\left[(1-\eta)^{-1/4}-1\right]
+
\frac{\pi\eta}{4(1-\eta)^{3/2}}
```

以下であり、固定ゲート時間で $\eta\to0$ に伴い零へ収束する。実現配置更新、測定、記録、resetまでを同じ位置ばねハードウェアへ統合した結果ではない。


### R123 1次元低位束縛状態と有限環境純位相緩和

- 導出状態：**厳密結果**
- 性質：Q3-3達成・有限モード・有限時間・コヒーレンス回復あり
- 対象モデル：M37、M43

Dirichlet井戸の離散固有対を閉形式で求め、固定低位モードの固有値、密度、節位置が $O(a^2)$ で連続井戸へ収束することを示した。有限区間の調和型差分生成子について、単純低位固有値、Sturm節数、Hermite--Gauss状態への格子・領域収束を示した。

先頭 $K$ モードの作用 $I_n$ と有限環境運動量 $P_n$ を

```math
H_{\rm deph}
=
\sum_n
\frac{E_n}{\mathcal J_0}I_n
+
\sum_n
\frac{P_n^2}{2M_n}
+
\frac{\lambda}{\mathcal J_0}
\sum_n
I_nP_n
```

で結合する。独立な $P_n=\pm p_*$ 等重み調製を縮約すると、全ての非対角相関は共通因子 $\cos^2(\lambda p_*t/\mathcal J_0)$ を持つ。$T_{\rm dec}=\pi\mathcal J_0/(2\lambda p_*)$ で厳密に零となり、対角占有率は全時刻で厳密に保存される。有限環境なので $T_{\rm rec}=2T_{\rm dec}$ で完全回復する。全Hamiltonianと注目系エネルギーも保存されるが、環境と相互作用して環境を読まない縮約系なのでエネルギー保存型開放系である。

### R124 3頂点有限障壁の障壁値未満確率移動

- 導出状態：**厳密結果と制御された任意精度**
- 性質：Q3-4達成・最小有限例・固定時刻位置読出し
- 対象モデル：M37、M42

3頂点鎖の中央に障壁値 $V$、辺結合 $\kappa$ を置く。低位対称固有状態と零エネルギー反対称固有状態の重ね合わせは、スペクトル支持が厳密に $V$ 未満である。$E_-=(V-\sqrt{V^2+8\kappa^2})/2$ と

```math
\alpha
=
\left(
1+\frac{E_-^2}{2\kappa^2}
\right)^{-1/2}
```

を置くと、$T_{\rm bar}=\pi\mathcal J_0/|E_-|$ における反対側確率の増分は厳密に $\alpha>0$ である。初期の反対側裾を基準値へ含める。M42読出しの各時刻誤差が全変動距離 $\epsilon$ 以下なら観測増分は $\alpha-2\epsilon$ 以上であり、有限パラメータで正に保てる。散乱透過率、初回到達、吸収は含まない。

### R125 2頂点再結合器のコヒーレンス差と位相差

- 導出状態：**厳密結果と制御された任意精度**
- 性質：Q3-5達成・最小2経路例・固定時刻位置読出し
- 対象モデル：M37、M42

直交2経路 $|L\rangle,|R\rangle$ に同じ生成子 $h_{\rm int}=\kappa(|L\rangle\langle R|+|R\rangle\langle L|)$ を作用させる。$T_{\rm int}=\pi\mathcal J_0/(4\kappa)$ で、相対位相 $\pi/2$ のコヒーレント入力と同じ重みの非干渉混合の位置分布距離は $1/2$、相対位相 $\pi/2$ と $-\pi/2$ の位置分布距離は1である。M42読出し誤差が各分布について $\epsilon$ 以下なら距離はそれぞれ $1/2-2\epsilon$、$1-2\epsilon$ 以上であり、有限パラメータで正に保てる。幾何学的開口、源、シャッター、多画素スクリーン、全検出器のHamiltonianは含まない。

### R53 正準複素振幅場と全位相作用

- 導出状態：**厳密結果**
- 性質：理想有効層内部
- 対象モデル：M1、M26

実正準対から

```math
b_i
=
\frac{Q_i+iP_i}{\sqrt{2\mathcal J_0}}
```

を定め、$H_{\rm ph}=b^\dagger hb$、$h=h^\dagger$ とすると、$i\mathcal J_0\dot b=hb$ が厳密に従う。全位相作用 $I_{\rm ph}=\mathcal J_0b^\dagger b$ は保存される。これは設計済み理想有効層または測定回路の結果であり、M37の位置ばね網ではR83--R86を使う。

### R54 集団モーメントとしての相関行列発展

- 導出状態：**厳密結果**
- 性質：仮説依存・成立条件
- 対象モデル：M28

調製条件 $\mathcal P$ と観測プログラム $M$ を固定した集団測度を $\mu_{\mathcal P,M}$ とし、

```math
C_M(t)
=
\mathbb E_{\mu_{\mathcal P,M}}
\left[
b_tb_t^\dagger
\right]
```

とする。同じ条件付き集団の全試行が共通 $h(t)$ に従うなら、

```math
i\mathcal J_0\dot C_M
=
\left[h,C_M\right]
```

を満たす。$C_M$ は単一試行の正準変数ではない。

### R55 相関スペクトルと純度の保存

- 導出状態：**厳密結果**
- 性質：保存則・否定的結果
- 対象モデル：M28

閉鎖線形発展では $C(t)=U(t)C(0)U(t)^\dagger$ である。跡、固有値、階数、純度、階数欠陥が保存される。混合集団は閉鎖線形発展だけで階数1へ純化しない。

### R56 階数1の同値条件と共通源準備

- 導出状態：**厳密結果**
- 性質：成立条件
- 対象モデル：M27、M28

$C=\Lambda\chi\chi^\dagger$ であることは、$b^\omega=c^\omega\chi$ がほとんど確実に成立することと同値である。有限正準回路 $U_{\rm prep}$ により階数1方向を準備できる。

### R57 階数1因子の Schrödinger 型発展

- 導出状態：**厳密結果**
- 性質：仮説依存
- 対象モデル：M28

R54に加えて $C=\Lambda\chi\chi^\dagger$ を仮定すると、共通位相を除いて

```math
i\mathcal J_0\dot\chi
=
h\chi
```

を得る。$h$ の物理的由来はこの結果に含まない。

### R58 空間グラフ上の局所発展、干渉、節

- 導出状態：**厳密結果**
- 性質：理想有効層内部
- 対象モデル：M26、M28

辺結合からグラフ Laplacian $L_G$ を作り、

```math
h_L
=
\frac{\mathcal J_0^2}{2m}L_G
+
V_L
```

と置けば、理想有効層内部で局所疎結合による離散 Schrödinger 型発展、位相差依存の干渉、節を得る。Q3-1のミクロ根拠はこの結果でなくR83--R86であり、連続極限と粒子軌道は含まない。

### R59 階数欠陥と節の残留強度

- 導出状態：**厳密結果**
- 性質：誤差上界
- 対象モデル：M28

最大固有値以外の相対重量を $\varepsilon_{\rm rank}$ とすると、主固有因子が理想節を持つ出力の正規化強度は $\varepsilon_{\rm rank}$ 以下である。

### R60 セル規格化と単一試行局所作用

- 導出状態：**厳密結果**
- 対象モデル：M26、M28、M32

セル体積を $b_i=\sqrt{\Delta V}a_i$ へ吸収すると、$C_{ii}/\operatorname{tr}C$ は規格化セル重みである。各試行の局所作用は $I_i=\mathcal J_0|b_i|^2$ である。セル重みを粒子検出確率と同一視するには追加の接続が必要である。

### R62 相関行列だけでは粒子軌道を決めない

- 導出状態：**厳密結果**
- 性質：否定的結果・適用限界
- 対象モデル：M0、M28、M32

$C$ と階数1因子 $\chi$ は集団量である。相関行列方程式と複素振幅場の有限基底頻度だけから、粒子の連続軌道、統計流への等変性、局所位置検出は従わない。M42はこの否定的結果を撤回せず、独立物理変数 $X$ と更新装置を追加して不足を補う。

### R68 非線形閉包残差

- 導出状態：**厳密結果**
- 性質：誤差伝播・成立条件
- 対象モデル：M28

試行方程式が $i\mathcal J_0\dot b=hb+r$ なら、

```math
i\mathcal J_0\dot C
=
\left[h,C\right]
+
D_C,
\qquad
D_C
=
\mathbb E
\left[
rb^\dagger-br^\dagger
\right]
```

である。一般非線形 Hamiltonian では高次モーメントが必要となり、$C$だけの閉包は未解決である。

### R127 開放捕捉器の収支と自律帰還

- 導出状態：**厳密恒等式と数値結果**
- 性質：開放方程式後の厳密収支・有限時間検査・成立条件
- 対象モデル：M45

周期反応座標 $s$ と対数型捕捉エントロピー座標 $r$ のItô Langevin方程式を採用する。局所エネルギー $E_C$ は厳密に

```math
dE_C
=
\left[
F_A(v_s)v_s
-\gamma_sv_s^2
-\gamma_rv_r^2
+
\frac{\gamma_sT_b}{M_s}
+
\frac{\gamma_rT_b}{M_r}
\right]dt
+
\sqrt{2\gamma_sT_b}\,v_s\,dB_s
+
\sqrt{2\gamma_rT_b}\,v_r\,dB_r
```

を満たす。定常分布と有限モーメントの存在を仮定すれば平均注入、散逸、Itô熱流入が釣り合う。位相すべり率と帰還写像は方程式へ入力しない。基準計算では分離面近傍率0.8275、3096離脱エピソード中2959件の有限時間帰還、平均収支残差 $3.45\times10^{-6}$ を得た。能動項を外した対照の分離面近傍率は0である。無限時間の正再帰性と帰還時間分布の閉形式は未証明である。

### R128 対数位相体積による捕捉エントロピー選別

- 導出状態：**漸近結果と数値結果**
- 性質：局所エネルギー帳簿に依存・有限エネルギー誤差
- 対象モデル：M45

1周期セルのLiouville劣位相体積は

```math
\Omega_C(E)
=
C_C
\exp(E/\Theta)
\left[
1+o(1)
\right]
```

となる。接触時の利用可能内部エネルギーが $W(X)=V(X)-V_{\min}$ だけ減り、同次数の位置依存Jacobianがなければ、

```math
\frac{\Omega_C(E-W(X))}{\Omega_C(E)}
=
\exp
\left[
-\frac{W(X)}{\Theta}
\right]
\left[
1+o(1)
\right].
```

対数状態体積の導出は解析的で、有限分離面エネルギーの求積では $z=W/\Theta$ に対する対数傾き $-1.0673$、$\exp(-z)$ からの最大偏差0.0220を得た。ただし $W(X)$ を二重計数なく内部利用可能エネルギーへ移す局所結合と、1回の選別を時間比例率へ変える切片則は未導出である。

### R129 条件付き対称作用素からの基底密度と定常Nelson則

- 導出状態：**条件付き厳密結果**
- 性質：3つの橋に依存・定常基底状態・初期共通原因準備
- 対象モデル：M45

無偏向位置拡散、時間比例切片則、捕捉器消去後の周辺可逆性を仮定する。

```math
K_\delta
=
I+\delta\nu\partial_X^2+o(\delta),
\qquad
A_\delta
=
\exp
\left[
-\frac{\delta W}{4m\nu}
\right]
```

とし、

```math
G_{\delta,V}
=
A_\delta K_\delta A_\delta
+
o(\delta)
```

とすれば、

```math
G_{\delta,V}
=
I
-
\frac{\delta}{2m\nu}
\left(
H_V-V_{\min}
\right)
+
o(\delta),
\qquad
H_V
=
-2m\nu^2\partial_X^2+V
```

を得る。正で対称な作用素の左右主固有関数は同じ $\phi_0$ へ収束し、形成方向と保持方向の積から

```math
\rho_0
=
\phi_0^2,
\qquad
H_V\phi_0
=
E_0\phi_0
```

となる。主固有関数によるDoob変換は

```math
b_+
=
2\nu\partial_X\log\phi_0,
\qquad
b_-
=
-2\nu\partial_X\log\phi_0
```

を持ち、定常Nelson時間対称加速度は $ma_N=-V'$ を満たす。有限格子監査では量子基底密度との全変動距離が調和型0.00404、二重井戸型0.00882となった。この結果は3つの橋を仮定した後の結論であり、直接M45軌道から量子基底密度を導いた結果ではない。一般の時間依存Nelson流、励起状態、複素位相は含まない。

### R130 指数capacity消去とFeynman--Kac重み

- 導出状態：**補助仮定後の厳密結果**
- 性質：指数無記憶性・内部変数消去・時間比例率
- 対象モデル：M46の再利用可能なcapacity補題、M45

残りaction capacityを $I_0\sim{\rm Exp}(1/\mathcal J_0)$ とし、

```math
I_{n+1}
=
I_n-\delta W(X_{n+1})
```

とする。位置経路を固定したready生存率は

```math
P
\left(
I_0>
\delta\sum_{k=1}^{n}W(X_k)
\mid
X_0,\ldots,X_n
\right)
=
\exp
\left[
-\frac{\delta}{\mathcal J_0}
\sum_{k=1}^{n}W(X_k)
\right].
```

ready条件下の残量は再び同じ指数分布なので、capacityを消去した古典生存核は

```math
\mathsf P_\delta
=
K_\delta D_\delta,
\qquad
D_\delta
=
\exp
\left(
-\frac{\delta W}{\mathcal J_0}
\right)
```

で閉じる。連続極限は $\exp[-\int_0^tW(X_s)ds/\mathcal J_0]$ である。物理resetを毎周期行う結果ではない。M45の有限energy指数近似から理想指数分布への誤差はR130の厳密部分と分ける。

### R131 条件付き線形半減衰と対称共分散核

- 導出状態：**補助線形応答を仮定した厳密結果**
- 性質：$D\mapsto D^{1/2}$・確率核と共分散核の分離
- 対象モデル：M46から保持する補助応答

phase-preserving線形散逸portを

```math
\dot b_x
=
-\frac{W(x)}{2\mathcal J_0}b_x
```

と採用すると、

```math
A_\delta
=
\exp
\left(
-\frac{\delta W}{2\mathcal J_0}
\right)
=
D_\delta^{1/2}.
```

複素共分散核は

```math
\mathsf S_\delta
=
A_\delta K_\delta A_\delta
```

であり、古典生存核とは $\mathsf S_\delta=A_\delta\mathsf P_\delta A_\delta^{-1}$ の相似関係にある。$K_\delta=\exp(-\delta\nu L_{\mathcal G})$、$\mathcal J_0=2m\nu$ なら

```math
\mathsf S_\delta
=
I
-
\frac{\delta}{\mathcal J_0}
\left(
H_V-V_{\rm ref}
\right)
+O(\delta^2).
```

同じ指数thresholdの二方向生存だけでは $D^{1/2}$ は導けず、$\exp[-\max(c_x,c_y)/\mathcal J_0]$ になる。R131は線形散逸portを必要とする。具体的回路から純散逸係数を導く部分は未解決である。

### R132 準臨界主共分散選択

- 導出状態：**線形準臨界極限での条件付き厳密結果**
- 性質：主固有射影・bath共分散・非線形飽和誤差
- 対象モデル：M28、M47の準備指針

$\mathsf S_\delta h_0=\lambda_0h_0$、$h_0>0$ とし、準備モードの線形化を

```math
Y_{n+1}
=
g\mathsf S_\delta Y_n+\eta_n,
\qquad
E[\eta_n\eta_n^\dagger]=Q
```

とする。主固有値が単純で $\langle h_0,Qh_0\rangle>0$ なら、$g\lambda_0<1$ の定常共分散は

```math
C_g
=
\sum_{k=0}^{\infty}
g^{2k}
\mathsf S_\delta^kQ\mathsf S_\delta^k
```

であり、

```math
\frac{C_g}{\operatorname{tr}C_g}
\longrightarrow
\frac{|h_0\rangle\langle h_0|}
{\langle h_0,h_0\rangle}
```

が $g\lambda_0\uparrow1$ で成り立つ。これはbath共分散の線形主モード選択であり、実現配置周辺が $h_0^2$ になることを単独では導かない。未来probeまたは二つの独立浴を必要としないが、$X$ とのmatching、有限振幅のRayleigh飽和を含む非線形定常測度の集中誤差、一般複素相対位相、切断後保存は未評価である。

### R133 局所current transducerと前後drift

- 導出状態：**不採用模型内では有限グラフ恒等式が厳密、連続極限は条件付き**
- 性質：非負rate・非最小往復流・正の台上のNelson drift
- 対象モデル：不採用M46

$h_{ij}=|h_{ij}|e^{i\gamma_{ij}}$、$\rho_i=|\psi_i|^2>0$ とし、

```math
q_{i\to j}^{\rm ct}
=
\frac{|h_{ij}|}
{2\mathcal J_0\rho_i}
\left|
\psi_i
+
i e^{i\gamma_{ij}}\psi_j
\right|^2
```

と定める。全rateは非負であり、

```math
\rho_iq_{i\to j}^{\rm ct}
-
\rho_jq_{j\to i}^{\rm ct}
=
\frac{2}{\mathcal J_0}
\operatorname{Im}
\left(
\psi_j^*h_{ji}\psi_i
\right)
```

が厳密に成り立つ。このrateはR113の最小率ではないが、同じ等変性を持つ。標準等間隔格子で $h_{i,i+1}=-\mathcal J_0\nu/a^2$ とし、$\psi=\sqrt\rho\exp(iS/\mathcal J_0)$ が滑らかで正なら、

```math
q_\pm
=
\frac{\nu}{a^2}
\pm
\frac{v+u}{2a}
+O(1),
```

```math
b_+=v+u,
\qquad
b_-=v-u,
\qquad
v=\frac{\nabla S}{m},
\qquad
u=\nu\nabla\log\rho
```

を得る。nodeでは率が特異になるため、R115型正則化またはsector分離が必要である。

この恒等式をM47の因果律には使わない。M47では複素振幅を $X$--bath共同統計から後に定義するため、このrateで再び $X$ を動かすと循環する。R133は `notes/rejected_m46_current_transducer.md` と版履歴で管理する。

### R134 一般時間依存Nelson流の時間対称Newton則

- 導出状態：**不採用M46の前提下での条件付き解析結果**
- 性質：非零current velocity・複素位相・時間対称加速度
- 対象モデル：不採用M46

```math
i\mathcal J_0\partial_t\psi
=
\left[
-\frac{\mathcal J_0^2}{2m}\Delta+V
\right]\psi,
\qquad
\psi
=
\sqrt\rho
\exp
\left(
\frac{iS}{\mathcal J_0}
\right),
```

$\mathcal J_0=2m\nu$ とする。R133の $b_\pm=v\pm u$ について

```math
a_N
=
\frac12
\left(
D_+D_-+D_-D_+
\right)X
=
\partial_tv
+
(v\cdot\nabla)v
-
(u\cdot\nabla)u
-
\nu\Delta u
```

であり、Madelung方程式から

```math
m a_N=-\nabla V
```

を得る。これはR129の $v=0$ 定常式を、任意の正則な非固有rank-one初期状態の時間依存sectorへ拡張する形式的結果である。Schrödinger型伝播、Madelung表示、R133の連続極限を前提とし、有限古典ミクロ系から一般時間依存Nelson流を因果的に導出した結果ではない。M47はR134を使用しない。

### R135 古典作用角共分散のcommutator回転

- 導出状態：**厳密結果**
- 性質：閉鎖古典Hamiltonian・rank保存・2モード統計核
- 対象モデル：M47

W型最低2モードのbath作用角対を $(I_n,\theta_n)$ とし、

```math
H_{\rm rot}
=
\sum_{n=0}^1
\frac{E_n}{\mathcal J_0}I_n,
\qquad
Z_n
=
\sqrt{\frac{I_n}{\mathcal J_0}}e^{-i\theta_n}
```

とする。規格化bath共分散

```math
C_{mn}
=
\frac{E[Z_mZ_n^*]}{E[Z^\dagger Z]}
```

は

```math
i\mathcal J_0\dot C
=
[D_W,C],
\qquad
D_W=\operatorname{diag}(E_0,E_1)
```

を満たす。従ってtrace、正値性、rankが保存される。$C(t_0)=c_0c_0^\dagger$ なら

```math
c(t)
=
\exp
\left[
-\frac{iD_W(t-t_0)}{\mathcal J_0}
\right]c_0
```

をrank-one因子に選べる。この式は二つの古典rotorのHamilton方程式から導き、Schrödinger方程式を入力しない。R135だけでは実現配置周辺が統計核の対角へ一致することを導かない。

### R136 W型2モード共同統計の左右占有振動

- 導出状態：**厳密結果**
- 性質：非定常重ね合わせ・相対位相回転・current・左右占有率
- 対象モデル：M47

対称W型生成子の最低2モードを実偶 $\phi_0$、実奇 $\phi_1$ とし、rank-one因子を

```math
\psi_W^{\rm stat}(x,t)
=
a_0\phi_0(x)e^{-i\theta_0(t)}
+
a_1\phi_1(x)e^{-i\theta_1(t)},
\qquad
a_0^2+a_1^2=1
```

とする。相対角は

```math
\delta(t)
=
\delta(t_0)
+
\frac{E_1-E_0}{\mathcal J_0}(t-t_0)
```

である。matching条件 $P(X=x)=|\psi_W^{\rm stat}(x,t)|^2$ の下で

```math
\rho(x,t)
=
a_0^2\phi_0^2
+
a_1^2\phi_1^2
+
2a_0a_1\phi_0\phi_1\cos\delta(t)
```

となる。$B_W=\langle\phi_0,\Pi_L\phi_1\rangle$ とすれば

```math
P_L(t)
=
\frac12
+
2a_0a_1B_W\cos\delta(t).
```

等重みの周期は $2\pi\mathcal J_0/(E_1-E_0)$ である。連続表示のWronskianから $\partial_t\rho+\partial_xj=0$ も直接成り立つ。固有モード単独では密度が定常なので、重ね合わせの占有振動を閉鎖伝播の中心検査とする。

### R137 matching保存を仮定したW型統計閉包

- 導出状態：**条件付き厳密結果**
- 性質：共同測度・rank-one核・閉鎖伝播
- 対象モデル：M47

共同測度 $\mu(dX\,d\xi)$ から

```math
\rho_i[\mu]=\mu(X=i),
\qquad
K_W[\mu]=\Phi C[\mu]\Phi^\dagger
```

を作り、matching多様体を

```math
\mathcal M_W
=
\left\{
\mu:
\operatorname{rank}C[\mu]=1,
\quad
\rho_i[\mu]=K_{W,ii}[\mu]
\right\}
```

とする。切断時に $\mu_{t_0}\in\mathcal M_W$ で、$\lambda_{\rm prep}=0$ の完全な閉鎖古典流が観測時間中に $\mathcal M_W$ を保存し、2モード外漏れ、散逸、雑音、記憶がないと仮定する。このときrank-one因子 $\psi_W^{\rm stat}$ は

```math
i\mathcal J_0\partial_t\psi_W^{\rm stat}
=
h_W\psi_W^{\rm stat}
```

を満たし、

```math
P(X=i)
=
|\psi_{W,i}^{\rm stat}|^2
```

が保たれる。未導出なのは、自然な局所Hopf--bath方程式からmatching多様体への吸引と閉鎖流による保存を証明する部分である。

### R138 逆累積分布による古典共同測度の存在

- 導出状態：**逆設計による厳密存在結果**
- 性質：決定論的古典ラベル・周辺分布・非局所構成
- 対象モデル：M47

R135から得る有限配置密度を $\rho_i(t)$ とし、一様な古典ラベル $U\in[0,1)$ と累積境界

```math
F_k(t)
=
\sum_{i\leq k}\rho_i(t)
```

を使って

```math
X_t
=
\min
\left\{
k:
U<F_k(t)
\right\}
```

と定める。このとき $P(X_t=i)=\rho_i(t)$ が各時刻で厳密に成り立つ。$U$ と共役作用を自明なHamiltoniansectorへ埋め込めるため、量子力学を使わない古典存在構成である。

ただし全密度 $\rho(t)$ を先に使う逆設計であり、局所慣性軌道、有限伝播速度、bath反作用、自然なHopf準備を導かない。R138をR137のmatching保存の物理的証明とは扱わない。

### R139 M47階数1共分散のBloch縮約

- 導出状態：**厳密結果**
- 性質：共通位相縮約・一般時間依存2モード生成子・rank保存
- 対象モデル：M47

trace 1の正半定値2次共分散は

```math
C
=
\frac12
\left(
I_2+\boldsymbol r\cdot\boldsymbol\sigma
\right)
```

と書け、階数1条件は $|\boldsymbol r|=1$ と同値である。従って階数1共分散の集合は共通位相を除いて $\mathbb{CP}^1\simeq S^2$ である。古典Hamiltonian $H_G=Z^\dagger G(t)Z$ は

```math
i\mathcal J_0\dot C=[G(t),C]
```

を与え、trace、正値性、階数を保存する。R139は統計状態の可逆操作を与えるが、実現配置周辺のmatching保存を単独では導かない。

### R140 W型傾斜制御による2モード可制御性

- 導出状態：**2モード射影内の厳密結果**
- 性質：任意の $SU(2)$・離調Rabi式・有限定傾斜列
- 対象モデル：M47

左右局在基底で、制御されたW型生成子は共通エネルギーを除いて

```math
G_F(t)
=
-J\sigma_x
+
\frac{\varepsilon(t)}2\sigma_z
```

となる。$J>0$ で傾斜 $\varepsilon(t)$ を正負の2値以上へ区分的に制御できれば、$\sigma_x$、$\sigma_z$ とそのcommutatorから $\mathfrak{su}(2)$ 全体を生成し、有限個の定傾斜区間で任意の $U\in SU(2)$ を実現できる。一定傾斜での左右遷移は

```math
P_{L\to R}(t)
=
\frac{4J^2}{\varepsilon^2+4J^2}
\sin^2
\left(
\frac{\sqrt{\varepsilon^2+4J^2}}{2\mathcal J_0}t
\right)
```

である。全W型系への適用ではR141の2モード外漏れと切替誤差を別に加える。

### R141 傾斜による左右占有分布の固定

- 導出状態：**明示誤差付き結果**
- 性質：高速対トンネル・低速対高モードの尺度階層
- 対象モデル：M47

最低2モードと高モードのgapを $G=E_2-E_1$ とする。測定傾斜と切替時間を

```math
J\ll|\varepsilon_m|\ll G,
\qquad
\frac{\mathcal J_0}{G}
\ll\tau_q\ll
\frac{\mathcal J_0}{J}
```

と選ぶ。任意の規格化共分散について、保持中の左占有率変化と残留結合を

```math
\varepsilon_{\rm lock}
\leq
\frac{2|J|}{\sqrt{\varepsilon_m^2+4J^2}}
+
\varepsilon_{\rm hold}
```

で抑える。局在射影から開始した場合の反対井戸遷移には、さらに強い $4J^2/(\varepsilon_m^2+4J^2)$ 上界が成り立つ。全W型系の固定中分布誤差は、別に評価した高モード漏れを加えて $\varepsilon_{2m}+\varepsilon_{\rm lock}$ 以下である。深いW型族 $J/G\to0$ では両者を任意に小さくできる。ただし傾斜は一般入力を測定固有状態へ収縮させず、周辺分布の固定だけでは単一試行の $X$ が記録中に同じ井戸へ滞在することも従わない。

### R142 W型左右読出しのBorn型有限誤差

- 導出状態：**matching条件下の明示誤差付き結果**
- 性質：有限コントラスト位置効果・無反応込み
- 対象モデル：M47

左半空間射影の最低2モード効果は、局在基底で

```math
E_L
=
(1-\eta_W)|L\rangle\langle L|
+
\eta_W|R\rangle\langle R|,
\qquad
0\leq\eta_W\leq\frac12
```

となる。従って理想軸重み $p_+$ に対し $|P_L-p_+|\leq\eta_W$ である。分析器、2モード漏れ、matching、固定、境界無反応、局所記録を加えた実分布と理想Born分布の全変動距離は、対応する誤差と $\eta_W$ の和で抑えられる。有限障壁の生の左右位置読出しは一般に厳密射影でなく、深いW型極限で任意精度となる有限コントラスト測定である。

### R143 M47実現配置を傾斜で固定し局所記録する有限誤差instrument

- 導出状態：**有限衝突再平衡化に条件付けた明示構成**
- 性質：transducerなし・操作面再平衡化・辺閉鎖・局所記録・枝別正準SWAP・逐次測定
- 対象モデル：M47

R145の信号bath方向準備、R164の条件付き作用殻準備、R161、R162の操作面再平衡化、R140の有限精度制御、R141の周辺固定、R162の辺閉鎖、安全枝での局所記録、結果別テンプレート交換、測定後作用殻準備・再平衡化、境界無反応を順に作用させる。このとき結果集合 $\{+1,-1,\varnothing\}$ を持つ有限正準・弱開放装置を構成でき、理想Born分布との全変動距離は

```math
\varepsilon_{\rm inst}
\leq
\eta_W
+\varepsilon_{\rm ctrl}
+\varepsilon_{2m}
+\varepsilon_{\rm Hopf}
+2\varepsilon_{\rm eq}
+\varepsilon_{\rm lock}
+\varepsilon_{\rm res}
+\varepsilon_{\rm guard}
+\varepsilon_{\rm rec}
+\varepsilon_{\rm br}
+\varepsilon_{\rm post}
```

で抑えられる。ここで $\varepsilon_{\rm eq}$ は有限混合、正則化、有限セル、信号bath保持、$\varepsilon_{\rm res}$ は辺閉鎖後に単一試行の $X$ が安全井戸を離れる経路確率の上界である。安全枝の出力共分散は対応固有射影から有限trace距離内にあり、条件付き配置はtemplate方向のGibbs分布へ戻る。記録は各試行の実現配置 $X$ の局所関数だけを読み、M42/R113、統計振幅、全密度、確率流、current transducerを入力にしない。R137の全時刻matching保存を仮定しない。

### R144 M47傾斜測定の固定有限弱開放周期

- 導出状態：**条件付き明示構成**
- 性質：固定有限段・永久記録・内部逆計算・交換reset
- 対象モデル：M47

固定純粋入力、有限個の傾斜制御、固定された2測定軸、任意の有限周期数について、R143の条件が各段・各周期で一様に成立するとする。Hopf準備、操作面再平衡化、任意軸操作、辺閉鎖、傾斜分離固定、局所記録、枝別テンプレート交換、測定後再平衡化、2段逐次測定、永久記録、内部逆計算、外部fresh-cell交換を有限正準・弱開放構成へ合成できる。分布誤差は各段の $\varepsilon_{\rm inst}$ の和、周期末偏差はR99の交換reset上界で抑えられる。連続matching保存は不要になったが、周期総収支を閉じていないためQ1-3は部分達成である。

### R145 M47単一Hopf準備の吸引位相円と有限時間率

- 導出状態：**厳密結果**
- 性質：採用開放方程式後・雑音零・有限時間指数率・seed条件
- 対象モデル：M47

回転座標で $\widetilde z=ac_*+p$、$c_*^\dagger p=0$ と分ける。雑音零の採用Hopf方程式は

```math
\frac{\|p(\tau)\|}{|a(\tau)|}
=
\frac{\|p_0\|}{|a_0|}e^{-\kappa\tau}
```

と、$y=|a|^{-2}$ の閉形式を与える。$a_0\neq0$ なら目標位相円へ収束し、有界seed集合では

```math
\operatorname{dist}
\left(
\widetilde z(\tau),
\{e^{i\alpha}c_*\}
\right)
\leq
K_{47}e^{-\gamma_{47}\tau},
\qquad
\gamma_{47}
=
\min\{2g,\kappa\}
```

を得る。規格化bath共分散も同じ率で $c_*c_*^\dagger$ へ近づく。$a_0=0$ の直交超平面、雑音付き定常測度、実現配置周辺、条件付きbath分布、切断後matching保存はこの定理に含まない。

### R161 任意の有限信号方向に対する配置再平衡化

- 導出状態：**採用局所生成子後の厳密結果**
- 性質：任意ray・共通位相不変・明示一様混合率・node no-go
- 対象モデル：M50、M47、M48/R152

単一試行の信号bath座標 $z\neq0$ について

```math
\pi_i^\delta(z)
=
\frac{|(\Phi z)_i|^2/(z^\dagger z)+\delta q_i}{1+\delta},
```

```math
k_{i\to j}^\delta(z)
=
\kappa_Xa_{ij}
\sqrt{
\frac{\pi_j^\delta(z)}{\pi_i^\delta(z)}
}
```

と置く。有限連結W型グラフ上で $\pi^\delta(z)$ は唯一の定常分布であり、$m_\delta=\delta q_{\min}/(1+\delta)$、無重みグラフギャップ $\lambda_G$ を使って

```math
D_{\rm TV}
\left(
\operatorname{Law}(X_T\mid z),
\pi^\delta(z)
\right)
\leq
\frac12
\sqrt{m_\delta^{-1}-1}
e^{-\kappa_Xa_{\min}m_\delta\lambda_GT}
```

を全bath rayに一様に得る。理想W型対角との差は $\delta/(1+\delta)$ 以下である。$\delta=0$ の零占有頂点が切断点なら、局所詳細釣合いを保つ既約生成子は存在しない。

### R162 局所詳細釣合い率の有限衝突熱浴実現

- 導出状態：**有限時間・有限セルのHamiltonian散乱実現**
- 性質：粗視化有効自由エネルギー保存・時間反転・位相体積保存・overflow込み
- 対象モデル：M50、M47、M48/R152

R164の作用殻fiberを消去した条件付き配置有効自由エネルギーと対称辺障壁を

```math
E_i^\delta(z)
=
-\Theta\log\pi_i^\delta(z),
```

```math
B_{ij}^\delta(z)
=
B_{ij}^0
+
\frac12
\left[
E_i^\delta(z)+E_j^\delta(z)
\right]
```

とする。到着断面上の入射エネルギー分布 $\beta e^{-\beta\epsilon}$ と粗視化有効自由エネルギー保存散乱から

```math
k_{i\to j}^{\rm coll}(z)
=
\nu_{ij}e^{-\beta B_{ij}^0}
\sqrt{
\frac{\pi_j^\delta(z)}{\pi_i^\delta(z)}
}
```

を得る。有限観測時間では有限セル、有限エネルギー、滑らかな閾値、履歴保存へ持ち上げられる。保存式は条件付き配置中間状態の有効自由エネルギーとセルエネルギーの粗視化和に対するものであり、作用殻fiberを含む全微視的仕事・熱を同定しない。セル超過と閾値帯は無反応へ含める。記録前に入射を止めて辺ゲートを閉じることで、R143の経路滞在誤差を有限障壁裾と時計誤差から評価する。

### R163 有限信号制御切替・配置再平衡化の経路ゆらぎ関係

- 導出状態：**厳密結果**
- 性質：正逆経路比・積分ゆらぎ関係・有効散逸仕事と相対エントロピー
- 対象モデル：M50、M47、M48/R152の局所配置応答

局所詳細釣合いを満たす正逆protocolについて

```math
\frac{\mathcal P_F[\omega]}
{\mathcal P_R[\omega^\dagger]}
=
e^{\Sigma[\omega]},
\qquad
\left\langle e^{-\Sigma}\right\rangle
=1,
\qquad
\langle\Sigma\rangle
\geq0
```

を得る。条件付きGibbs平衡 $\pi^\delta(c)$ から $c'$ への瞬間quenchでは

```math
W_i^{\rm rel}
=
\Theta
\log
\frac{\pi_i^\delta(c)}{\pi_i^\delta(c')},
\qquad
\left\langle e^{-\beta W^{\rm rel}}\right\rangle
=1,
```

```math
\langle W^{\rm rel}\rangle
=
\Theta
D_{\rm KL}
\left(
\pi^\delta(c)
\|
\pi^\delta(c')
\right).
```

作用殻明示表示では $W_i^{\rm sh}=\Delta F_i^{\rm sh}$、消去表示では $W_i^{\rm rel}=\Delta E_i=W_i^{\rm sh}-\Delta F_{\rm eq}^{\rm sh}$ を使う。経路確率比は粗視化跳躍過程で厳密だが、これらを全微視的仕事・熱と同一視しない。

### R164 有限信号作用のBorn型殻状態数

- 導出状態：**条件付き厳密結果＋滑らかな有限幅近似**
- 性質：単一母測度・2作用殻線形容量・Born型状態数・有効自由エネルギー・次元剛性
- 対象モデル：M50、M47、M49中央4枝、M48各翼局所状態数

一般形は $v\in\mathbb C^m$、$\Psi^\dagger\Psi=I_m$ である。以下の $z\in\mathbb C^2$、$\Phi$ はM47特殊化の記法であり、M49中央4枝では $m=L=4$、$\Psi=I_4$ とする。

単一試行の信号bath座標 $z\neq0$ から

```math
J_{\rm sig}(z)
=
\mathcal J_0z^\dagger z,
\qquad
J_i(z)
=
\mathcal J_0|(\Phi z)_i|^2,
```

```math
A_i^\delta(z)
=
J_i(z)+\delta q_iJ_{\rm sig}(z)
```

を作る。各排他的枝に $K_i+I_i=A_i^\delta$ の2作用殻fiberを置き、同じLiouville母測度で数えると

```math
\Omega_i^\delta(z)
=
\frac{(2\pi)^2}{J_{\rm ref}}
A_i^\delta(z),
```

```math
\frac{\Omega_i^\delta(z)}
{\sum_j\Omega_j^\delta(z)}
=
\frac{|(\Phi z)_i|^2/(z^\dagger z)+\delta q_i}{1+\delta}
=
\pi_i^\delta(z).
```

従って

```math
E_i^\delta(z)
=
-\Theta\log\pi_i^\delta(z)
```

は、作用殻を消去した条件付き中間状態有効自由エネルギーとして得られる。状態数明示表示と消去表示を同じ分配関数で二重計数しない。直接作用を受け取る明反応方向が $q$ 本なら $\Omega\propto A^q$ なので、線形Born型重みを全容量族で保つのは $q=1$ だけである。$\delta\downarrow0$ の一様精度には剛性 $\Omega(\delta^{-2})$ が必要で、$\Theta(\delta^{-2})$ は代表的な選択である。

未導出なのは、枝容量結合、fiber内平衡化、枝対称性、信号bath保持反作用をM47測定周期の有限局所Hamiltonianとして統合することである。旧M15の位置入口周期、等方混合、標本化後再埋込みは復活させない。R164だけで一般Born則、全微視的仕事・熱、paired-Hopf周期を導出済みと扱わない。

## 5. 有限基底標本化と明示周期

### R69 統一母測度の将来目標

- 導出状態：**予想・未解決**
- 性質：統一目標
- 対象モデル：M31

相関観測、可変調製、可変基底、設定生成器、 Bell 二側条件、外部記録まで含む単一の開始面、周期写像、統一母測度 $\mu_*$ は未構成である。固定プログラムごとの不変測度を混合しても、一般に全体のエルゴード性は従わない。現行定理は $\mu_*$ の存在またはエルゴード性を仮定に使わない。

### R70 正準作用選択器の一般確率式

- 導出状態：**厳密結果**
- 性質：仮説依存・成立条件
- 対象モデル：M32

選択器角が $(b,W,\mathcal P)$ の下で条件付き一様なら、累積作用区間の排他的結果は

```math
P(k\mid W,\mathcal P)
=
\mathbb E
\left[
\frac{I_k}{I_{\rm ph}}
\middle|
W,\mathcal P
\right]
```

を満たす。階数1と共通入口流束は不要である。

### R71 固定作用下の有限基底 Born 型則

- 導出状態：**厳密結果**
- 性質：仮説依存・成立条件
- 対象モデル：M28、M32

$I_{\rm ph}$ が試行ごとに固定されるなら、

```math
P(k\mid W,\mathcal P)
=
\frac{
\left(
WC_WW^\dagger
\right)_{kk}
}{
\operatorname{tr}C_W
}
```

を得る。$C_W=C$ が許容基底で共通なら、同じ状態 $C$ に対する任意有限直交基底の Born 型則となる。高階数相関行列を含む。

### R72 全作用変動の共分散補正

- 導出状態：**厳密結果**
- 性質：誤差恒等式
- 対象モデル：M32

$r_k=I_k/I_{\rm ph}$ とすると、

```math
P_k
-
\frac{
\mathbb E[I_k]
}{
\mathbb E[I_{\rm ph}]
}
=
-
\frac{
\operatorname{Cov}
\left(
I_{\rm ph},r_k
\right)
}{
\mathbb E[I_{\rm ph}]
}
```

である。固定全作用または無相関が平均比との一致条件になる。

### R75 正準レジスターの理想読出し

- 導出状態：**厳密結果**
- 性質：理想パルス・成立条件
- 対象モデル：M34

$G_{\rm read}=\sum_kP_kI_k+P_UI_{\rm ph}f(\vartheta)$ は $P_k=P_U=0$ の理想入口で入力へ反作用せず、作用と閾値を正準レジスターへ移す。分岐生成子の保存量を含めて共役運動量の反作用を追跡する必要がある。固定有限基底への周期統合はR76--R79で扱う。

### R76 固定有限基底の自律内部測定周期

- 導出状態：**厳密結果**
- 性質：固定プログラム・有限自律周期
- 対象モデル：M35

任意の有限 $L$、固定純粋準備 $\chi$、固定基底変換 $W$ に対し、$3L+4$ 正準対を使って準備、作用読出し、累積比較、テンプレート準備、正準 SWAP 、内部記録、保持、逆計算、選択器ドリフトを1本の有限自律 Hamiltonian へまとめる。基底回路の局所実装はR89、滑らかな結果形成はR90で与える。

### R77 無理数回転 Poincaré 写像と Haar 不変測度

- 導出状態：**厳密結果**
- 対象モデル：M35

内部準備値を固定した不変集合上で、1周期後に選択器角だけが

```math
\vartheta
\longmapsto
\vartheta+2\pi\alpha
\pmod{2\pi},
\qquad
\alpha\notin\mathbb Q
```

と進む。円周 Haar 測度は唯一の不変確率測度であり、写像は一意エルゴード的である。固定プログラムでは不変性とエルゴード性を仮定せず導出する。

### R78 零幅極限の Born 型長期頻度と条件付き測定後状態

- 導出状態：**厳密結果**
- 性質：固定純粋準備・固定基底
- 対象モデル：M35

比較幅を零にした理想累積区間では、結果 $k$ の長期頻度は $|\langle u_k|\chi\rangle|^2$ である。有限幅の安全セクターでは正準 SWAP 後の信号は厳密に $|u_k\rangle$、測定前情報はテンプレートの $-W\chi$ へ退避する。新しい空テンプレートを持つ第2装置では同じ結果が確率1で反復される。同じ装置の逆計算前の再使用は含まない。滑らかな有限幅分布はR91で扱う。

### R79 内部逆計算と永久記録の限界

- 導出状態：**厳密結果**
- 性質：内部逆計算・誤差上界・否定的結果
- 対象モデル：M35

滑らかな比較、無反応領域、中間テンプレートを含め、前向き写像全体を逆順・逆符号で実行すると、全内部正準変数と時計運動量が周期末に準備値へ戻る。永久外部記録まで結果非依存の同一点へ戻すことは Hamiltonian 流の1対1性と両立しない。無理数回転は混合的でなく、二項分布型有限標本揺らぎを与えない。

### R89 任意有限基底の隣接2モード正準回路

- 導出状態：**厳密結果**
- 性質：局所構成・資源上界
- 対象モデル：M35

局所位相回転

```math
G_{Z,j}
=
\frac{Q_j^2+P_j^2}{2}
```

と隣接交換

```math
G_{X,j}
=
Q_jQ_{j+1}+P_jP_{j+1}
```

の有限列は任意の隣接 $U(2)$ を生成する。隣接 Givens 消去により、任意の $W\in U(L)$ を $L(L-1)/2$ 個以下の隣接2モード混合と対角位相へ分解できる。全ゲートは作用と正準性を厳密に保存し、逆回路は順序と角度の反転で得る。$G_{X,j}$ は運動量間結合を含むため、M37の位置ばね網と同じハードウェアではない。

### R90 滑らかな累積比較ポインターと完全結果集合

- 導出状態：**厳密結果**
- 性質：滑らかな結果形成・成立条件
- 対象モデル：M35

作用レジスターを隣接剪断して $Q_j=S_j-u$ を作り、双曲型増幅後に平坦部を持つ滑らかな段差関数を適用する。安全セクター $\mathcal O_k$ は互いに素であり、その内部ではテンプレートが隣接経路により厳密に $e_k$、内部記録が $k$ となる。補集合を正式な無反応結果 $\mathcal O_\varnothing$ とすることで、$\{1,\ldots,L,\varnothing\}$ が全入力を覆う。結果判定は比較ポインターセクターで行い、遷移領域で偶然整数になり得る記録座標だけを使わない。

### R91 無反応込みの任意精度 Born 型長期頻度

- 導出状態：**近似のもとで導出済み**
- 性質：明示誤差上界・固定純粋準備・固定有限基底
- 対象モデル：M35

入力換算比較幅を $w=Xe^{-\Lambda}$、角切断接続領域の Haar 質量を $\varepsilon_{\rm cut}$ とすると、実長期分布

```math
p^{\rm cyc}
=
\left(
p_1^{\rm cyc},\ldots,p_L^{\rm cyc},p_\varnothing^{\rm cyc}
\right)
```

と理想分布の距離は

```math
D_{\rm TV}
\left(
p^{\rm cyc},
\left(
|\langle u_1|\chi\rangle|^2,
\ldots,
|\langle u_L|\chi\rangle|^2,
0
\right)
\right)
=
p_\varnothing^{\rm cyc}
\leq
2(L-1)
\frac{Xe^{-\Lambda}}{I_{\rm ph}}
+
\varepsilon_{\rm cut}
```

を満たす。有限 $L$ では $\Lambda$ と切断幅を選び、任意の $\epsilon>0$ に対して右辺を $\epsilon$ 未満にできる。無反応試行を除いて再規格化しない。

## 6. Bell側の導出結果

置換済みM41のR107--R111は状態と参照先だけをここに残し、式、証明、誤差分解は `notes/superseded_m41_bell_cycle.md` と `notes/superseded_m41_cycle_proofs.md` で管理する。現行Q2-2の根拠には使わない。

| 結果 | 導出状態 | 対象モデル | 置換済み内容 |
|---|---|---|---|
| R107 | 置換済み模型内の厳密式と明示誤差付き結果 | M39、M41、M42 | A分析器後の共同実現配置読出し |
| R108 | 置換済み模型内の厳密結果と明示構成 | M41、M42 | 共同実現配置に条件付けた2端準備 |
| R109 | 置換済み模型内の厳密式と明示誤差付き結果 | M41、M42 | 局所分析器後の実現配置共同分布 |
| R110 | 置換済み模型内の厳密な因果監査 | M41 | 前向き因果順序と設定条件付き測定開始面 |
| R111 | 置換済み模型内の厳密式と明示誤差付き結果 | M41 | singlet余弦統計、非信号性、CHSH値、Bell前提監査 |

### R146 積bath標本からの直接singlet階数1共分散の不可能性

- 導出状態：**厳密結果**
- 性質：否定的結果・直接標本共分散と交差共分散の区別
- 対象モデル：M48

$Z=z_A\otimes z_B$ の規格化共分散が $c_{\rm s}c_{\rm s}^\dagger$ なら、rank-one支持性から非零標本 $Z$ はほとんど確実にsinglet直線へ属する必要がある。しかし積標本の $2\times2$ 係数行列は階数1、singlet係数行列は階数2なので不可能である。M48は直接共分散でなく $\mathbb E[z_Az_B^{\mathsf T}]$ の規格化射影を使う。

### R147 M48の2枝paired-Hopf吸引多様体と有限時間率

- 導出状態：**厳密結果**
- 性質：採用開放方程式後・雑音零・盆余裕・無反応込み
- 対象モデル：M48

bright変数 $m$、dark変数 $d$ は

```math
\frac{d\|m\|^2}{d\tau}
=
2g(1-\|m\|^2)\|m\|^2,
```

```math
\frac{dh_x}{d\tau}
=
2\kappa h_x(1-h_x^2),
\qquad
d(\tau)
=
e^{-\kappa_{\rm p}\tau}d_0
```

を満たす。$h_x(m_0)\neq0$ なら符号で選ばれた固有枝へ収束し、$|h_x(m_0)|\geq h_*$ の有界seed集合では率 $\gamma_{48}=\min\{2g,2\kappa,\kappa_{\rm p}\}$ を得る。Haar方向の盆境界質量 $h_*$ は無反応として残す。

### R148 設定前共通基準測度からのsinglet交差共分散準備

- 導出状態：**厳密結果と明示誤差付き結果**
- 性質：有限設定族・初期共通原因・設定依存開放準備・singlet射影
- 対象モデル：M48

1つの設定前Haar方向基準測度を各A設定 $x$ のM48流で押し出す。安全2枝が等重みなら

```math
M_{AB}^{G}(\infty\mid x)
=
-\frac{1-h_*}{2}\mathsf E,
\qquad
B_{AB}(\infty\mid x)
=
-\frac{\mathsf E}{\sqrt2}
```

であり、ベクトル化した階数1射影は $c_{\rm s}c_{\rm s}^\dagger$ となる。右辺は有限設定族の全ての $x$ で同じである。有限時間誤差は $K_\times e^{-\gamma_{48}\tau}$、枝非対称、切断残差で抑える。交差共分散は集団統計であり単一試行結果ではない。

### R149 M48準備と局所M47分析器からの余弦共同分布

- 導出状態：**厳密結果**
- 性質：抽象的完全matching仮説依存・局所instrument・平面内2出力。固定Bell周期ではR153、R154が仮説を充足
- 対象モデル：M47、M48

2翼実現配置と条件付きbath分布がR148の各枝へ完全matchingされ、切断後に局所M47分析器がそのmatchingを保存すると仮定する。このとき

```math
P(A=a,B=b\mid x,y)
=
\frac14
\left(
1-ab\,n_x\cdot n_y
\right)
```

を得る。平面設定では相関は $-\cos(x-y)$、一側周辺は $1/2$ である。R147、R148だけから実現配置頻度が出たとは扱わない。

### R150 M48経路の有限誤差非信号性、CHSH破れ、Bell前提監査

- 導出状態：**厳密結果**
- 性質：抽象的R149仮説依存・無反応込み・有限全変動距離・Bell監査。固定Bell周期ではR155へ具体化
- 対象モデル：M47、M48

各設定対の完全結果分布がR149の理想分布から全変動距離 $\varepsilon_{\rm Bell}^{48}$ 以下なら、一側周辺の設定差は $2\varepsilon_{\rm Bell}^{48}$ 以下、CHSH値のずれは $8\varepsilon_{\rm Bell}^{48}$ 以下である。$\varepsilon_{\rm Bell}^{48}<({\sqrt2-1})/4$ ならCHSH破れが残る。切断後の局所応答は因子化するが、測定開始面の分布 $\mu_x$ がA設定に依存するため測定設定独立性は成立しない。Bellの定理を否定しない。

### R151 setting-pre等重みseedの履歴付き安全盆routing

- 導出状態：**明示構成・有限誤差上界**
- 性質：M48内部の設定前fair seed・履歴不活性・安全盆への有限routing
- 対象モデル：M48

設定前の枝seed $S_0\in\{+1,-1\}$ を

```math
P(S_0=+1)=P(S_0=-1)=\frac12
```

として固定し、A設定生成後に各 $(S_0,x)$ を $S_0h_x(m)\geq h_*$ のpaired-Hopf安全盆へ明示的に写す。有限seed biasを $\varepsilon_{\rm seed}$、routing失敗を $\varepsilon_{\rm route}$ とする。provenance履歴は保存してもよいが、許された履歴値で条件付けた結果法則を変えない。M48単独では内部seedを使い、接続周期ではR160によりM49の01、10共同配置から $S_0=(-1)^{X_A}$ を受ける。

### R152 有限設定用の局所matching生成子

- 導出状態：**厳密結果・採用開放生成子**
- 性質：有限連結配置グラフ・詳細釣合い・一意不変分布・有限時間収束・共通位相不変
- 対象モデル：M48

有限連結グラフの翼 $X\in\{A,B\}$ で、paired-Hopf方向 $z_X$ から

```math
w_i(z_X)
=
\frac{|(\Phi_Xz_X)_i|^2}{z_X^\dagger z_X},
\qquad
\pi_i^\delta(z_X)
=
\frac{w_i(z_X)+\delta q_i}{1+\delta}
```

を定め、辺上のrateを

```math
k_{i\to j}^{X,\delta}(z_X)
=
\kappa_Xa_{ij}
\sqrt{
\frac{\pi_j^\delta(z_X)}{\pi_i^\delta(z_X)}
}
```

とする。この有限状態連続時間Markov生成子は $\pi_X^\delta$ に関して詳細釣合いを満たし、一意不変分布を持つ。固定有限設定族の安全compact集合では、ある $C_X<\infty$、$\lambda_X^\delta>0$ により全変動距離が $C_Xe^{-\lambda_X^\delta T_X}$ 以下となる。$\pi_X^\delta(e^{i\theta}z_X)=\pi_X^\delta(z_X)$ であり、正則化誤差は $\delta/(1+\delta)$ 以下である。R152はR161のBell限定形であり、R161の全ray一様ギャップ下界とR162の有限衝突bath実現を利用できる。ただしpaired-Hopf、seed routing、2翼controllerまで同じ最小Hamiltonianへ統合したことにはならない。

### R153 中央切断面の2翼strong matching

- 導出状態：**明示構成・有限誤差上界**
- 性質：有限設定族・条件付きbath fiberを含むstrong matching・準備先行・非空間分離
- 対象モデル：M48

R151のseed routing、R147のpaired-Hopf吸引、R152の2翼配置緩和を順に走らせ、切断面で各安全枝の実現配置周辺と条件付きbath分布を目標fiberへ近づける。連続bath方向にはpaired位相を保つprojective cost、離散配置には最適couplingの不一致確率を使うfiber距離について

```math
\varepsilon_{\rm match}^{48}
\leq
\varepsilon_{\rm seed}
+
\varepsilon_{\rm route}
+
K_{48}e^{-\gamma_{48}T_{\rm PH}}
+
\frac{2\delta}{1+\delta}
+
2C_Xe^{-\lambda_X^\delta T_X}
+
\varepsilon_{\rm cut}
```

で抑えられる。これを完全状態の全変動距離とは呼ばない。中央切断器の幅と有限準備時間を含めて、任意の目標精度に対する有限パラメータを順に選べる。

### R154 切断後の局所分析器、再matching、傾斜固定、局所記録

- 導出状態：**明示構成・有限誤差上界**
- 性質：切断後局所因子化・Bell固有instrument・無反応込み・永久記録
- 対象モデル：M47、M48

中央結合を切った後は、A、B各翼の生成子を局所項へ分ける。各翼でM47型分析器、R152型短時間再matching、傾斜固定、既存実現配置の局所記録を順に行う。安全領域の外は無反応として残し、除外して再規格化しない。これは固定Bell族の結果形成だけを扱い、結果別bathテンプレート交換、逐次測定後状態、M47の一般Q1 instrumentを含まない。

### R155 M48単独完全周期の余弦共同分布とBell監査

- 導出状態：**明示構成・制御された任意精度**
- 性質：M48単独周期の条件付き達成を与える主定理・有限設定・固定singlet型・測定設定独立性の破れ
- 対象モデル：M47、M48

R151--R154の周期が出す無反応込み共同分布 $P_{\rm Bell}^{48,{\rm cyc}}$ は、理想分布

```math
P_{\rm s}(a,b\mid x,y)
=
\frac14
\left[
1-ab\cos(x-y)
\right]
```

から設定対一様に全変動距離 $\varepsilon_{\rm Bell}^{48,{\rm cyc}}$ 以下にある。一側周辺の反対側設定差は $2\varepsilon_{\rm Bell}^{48,{\rm cyc}}$ 以下、CHSH値のずれは $8\varepsilon_{\rm Bell}^{48,{\rm cyc}}$ 以下であり、$\varepsilon_{\rm Bell}^{48,{\rm cyc}}<(\sqrt2-1)/4$ ならCHSH破れが残る。切断後応答は局所因子化するが、測定開始面がA設定に依存するため測定設定独立性は成立しない。標準的な空間分離Bell実験またはBellの定理の否定ではない。本定理はM48単独上界であり、付録Kの接続誤差はR160で別に加える。

### R156 外部空セル交換による弱開放帰還

- 導出状態：**厳密結果・収縮上界**
- 性質：永久記録を保持した次周期入口への帰還・有限試行またはfresh-cell流
- 対象モデル：M48

局所記録後、内部作業レジスターを逆計算し、残る装置偏差を外部空セルと交換する。周期写像が $\|R_{\rm ret}\|\leq r_{\rm ret}<1$、fresh-cell残差が $\|\eta_n\|\leq\sigma_{\rm ret}$ を満たすと

```math
\limsup_{n\to\infty}
\|\Delta Z_n\|
\leq
\frac{
\sigma_{\rm ret}
}{
1-r_{\rm ret}
}
```

を得る。正準部分交換では $r_{\rm ret}=|\cos\phi|$、$\sigma_{\rm ret}=\varepsilon_{\rm cyc}^{48}+|\sin\phi|\sigma_E$ と置けばR99の式に一致する。旧状態と散逸生成物は使用済み外部自由度へ移り、消去されない。有限試行は有限セル列、無期限反復はfresh-cell流を必要とする。

### R157 M49中央4枝状態数の有限Hamiltonian準備

- 導出状態：**厳密結果・有限Hamiltonian任意精度近似**
- 性質：M49の共同bath準備定理
- 対象モデル：M49

規格化係数行列 $C$ の行重み $\rho_a=\sum_b|C_{ab}|^2$ を枝確率とし、

```math
z_A=\rho_a^{-1/4}e^{i\theta}e_a,
\qquad
z_B=\rho_a^{-3/4}e^{-i\theta}C_{a\bullet}^{\mathsf T}
```

と置く。$X_A=a$、$P(X_B=b\mid a)=|C_{ab}|^2/\rho_a$ なら、$\mathbb E[z_Az_B^{\mathsf T}]=C$ と $P(X_A=a,X_B=b)=|C_{ab}|^2$ が同時に成立する。各端のbath条件付き配置法則も理想局所核と一致する。固定有限プログラムでは校正済みtemplate bank、M35作用区間、可逆decode、canonical SWAPにより有限Hamiltonian任意精度化できる。

### R158 担体・bath・配置へ同期する同一試行CNOT

- 導出状態：**厳密結果・有限Hamiltonian構成**
- 性質：M49の結合ゲート定理
- 対象モデル：M49

行枝 $a=X_A$ に対して $z_B\mapsto(\sigma_x)^az_B$、$X_B\mapsto X_B\oplus X_A$ とすると、bath交差モーメントと共同配置分布は同じCNOT行作用で更新される。4モードプログラム担体にはR105の面積 $\pi$ 流を同期させる。制御井戸、B側bath交換、B配置SWAP、履歴cellを含む拡大系上で1対1な有限Hamiltonian写像にできる。

### R159 固定有限入力、入力頻度、固定積出力基底の共同入力--出力統計

- 導出状態：**明示構成・制御された任意精度**
- 性質：Q2-1の現行主定理
- 対象モデル：M49、M35

任意の有限固定入力、固定積出力基底、入力頻度について、program選択 $u_S$、入力配置 $u_X$、出力読出し $u_Y$ に相異なる選択器を使う。M49の準備、CNOT、積分析器を実際に通過した単一試行program担体をM35作用区間selectorへ入れ、結果をfreshな2配置registerへ可逆decodeする。無反応込みの入力label・2ビット共同分布を理想CNOT分布へ任意精度で近づけられる。M42/R113、ensemble交差モーメントの再注入、未知入力、独立同分布型有限標本は使わない。

### R160 M49固定singlet providerからM48へのsetting-free同一register受渡し

- 導出状態：**厳密結果・明示誤差上界**
- 性質：Q2-1--Q2-2の固定singlet接続定理
- 対象モデル：M49、M48

積入力

```math
C_{\rm in}^{\rm s}
=\frac1{\sqrt2}
\begin{pmatrix}0&-1\\0&1\end{pmatrix}
```

はCNOTで $-\mathsf E/\sqrt2$ へ移る。R157の2行枝ではCNOT後に各試行で $z_B=\mathsf E\overline{z_A}$、$d=0$、共同配置 $01,10$ の等重みとなる。同じ $z_A,z_B,X_A,X_B$ をsetting-free面で恒等搬送する $T_{\rm link}$ は付録Kのcross、配置、program matchingと1対1性を満たし、cross projector感度と枝biasを保存する。接続Bell誤差は

```math
\varepsilon_{\rm Bell}^{49\to48}
\leq
\varepsilon_{\rm Q2-link}
+\varepsilon_{\rm Bell}^{48,{\rm cyc}}
```

である。一般Q2-1出力の一般状態Bell測定は含まない。

### R165 M49中央4枝作用殻とM35作用区間標本化の同値性

- 導出状態：**条件付き厳密結果・有限Hamiltonian任意精度近似**
- 性質：共同Born状態数・行周辺整合・測度押し出し
- 対象モデル：M50、M49、M35

規格化出力係数 $D$ に対し $m=L=4$、$\Psi=I_4$、$\delta=0$ とすると

```math
\Omega_{ab}(D)
=
\frac{(2\pi)^2\mathcal J_0}{J_{\rm ref}}
|D_{ab}|^2,
\qquad
P(a,b\mid D)=|D_{ab}|^2.
```

中央2作用殻の枝周辺を平坦化した座標をM35作用区間へ押し出すと同じ共同分布を得る。M35は有限Hamiltonian標本器であり、作用殻の熱化証明ではない。配置混合と衝突熱浴はこの直接標本化定理の必須項でない。

### R166 M48切断後局所作用殻の条件付き積因子化

- 導出状態：**採用局所生成子後の厳密結果・有限偏差上界**
- 性質：fresh局所殻・条件付き積測度・局所詳細釣合い・経路エントロピー加法性
- 対象モデル：M50、M48

完全共通原因 $\Lambda$ に条件付け、切断後生成子とfresh局所装置がA、Bに分離すると

```math
\mu_{\rm sh}^{AB}(d\gamma_A,d\gamma_B\mid\Lambda,x,y)
=
\mu_{{\rm sh},A}^x(d\gamma_A\mid\Lambda)
\otimes
\mu_{{\rm sh},B}^y(d\gamma_B\mid\Lambda),
```

```math
\Sigma_{\rm post}=\Sigma_A+\Sigma_B.
```

残留結合と共通雑音の偏差を $\varepsilon_{\rm prod}$ とする。$\Lambda$ を積分した結果は相関してよい。$-\Theta\log P(a,b\mid x,y)$ を切断後の物理的大域ポテンシャルへ戻さない。

## 7. 置換済み結果IDの扱い

draft-19までのR1--R52は版履歴上の結果IDとして保持するが、現行主張の根拠には使わない。draft-28での位置・測定・結合ゲート・Bell確率機構の扱いは次の通りである。

| 旧結果 | 現在の扱い |
|---|---|
| R1--R12 | 配置拡散、 Fisher 項、線形 Gauss 型作用の補助理論。現行主線に不採用 |
| R13--R20 | 旧3モード Bell モデルと一般測定周期の結果。研究メモまたは未解決問題として保存 |
| R21、R22、R25--R28 | 実在2成分場、位相接続、密度同期、循環量子化。現行主張から撤回 |
| R23、R24 | 作用殻容量と位置入口頻度。旧R24の一般作用殻容量と2作用殻線形性だけをR164へ再導入した。R23の位置入口頻度と旧M15測定周期は不活性のまま |
| R29--R38 | 旧Bell余弦集団計算。M30--M36の不採用記録とともに研究メモへ移し、現行頻度機構には使わない |
| R39--R52 | 旧2成分場の準備、交換、弱縮約。現行主線に不採用 |
| R61 | 2モード作用殻による位置入口頻度。R70、R71へ置換 |
| R63、R66、R67、R73、R74、R80--R82 | M30、M33、M36の集団余弦重み、二側境界測度、逆計算。現行根拠から外し、いったんR107--R111のM41周期へ、draft-45BでR151--R156のM48周期へ置換 |
| R64 | 比較作用と2モード殻容量。補助的幾何結果として版履歴に保存し、現行Bell確率機構には使わない |
| R65 | 共通境界測度下のBell共同確率。M36の不採用記録へ移し、R110、R111へ置換 |
| R92--R94 | 旧M38のBloch縮約、任意の $SU(2)$、Rabi式。現行Q1ではM47共分散とW型傾斜制御のR139、R140へ置換 |
| R95、R96 | 旧M38/M42の測定後状態と逐次測定。現行Q1では傾斜固定、有限コントラスト読出し、局所記録、枝別交換のR141--R143へ置換 |
| R100、R119 | 旧M38/M42のQ1完全周期。現行Q1ではR164の作用殻準備とR161、R162の操作面再平衡化を用いるM47周期R144へ置換。周期総収支が残るためQ1-3は部分達成 |
| R101--R103 | 旧M38/M42の有限Zeno履歴・装置・主定理。Q1-4を未達として凍結したため現行根拠から外し、`notes/frozen_q1_zeno.md` とGit履歴で保存 |
| R126 | M44の捕捉エントロピー型準備作用素と定常Nelson則。有限Hamiltonian模型から未導出の橋が多いため現行根拠から外し、直接開放捕捉器のR127・R128と条件付き作用素のR129へ分解して置換 |
| R133 | 旧M46の局所current transducer。有限グラフ恒等式は旧模型内で正しいが、M47の統計場解釈では $X$--bath統計から振幅を作り、その振幅で再び $X$ を動かす循環になるため現行因果鎖から外した |
| R134 | 旧M46でSchrödinger--Madelung式とR133連続極限を仮定した後のNelson恒等式。有限古典ミクロ系から一般時間依存流を導出した結果ではなく、M47は使用しない |

旧結果の完全な式は過去版で保存する。現行検索結果へ旧主張を混在させないため、完全な旧章を `notes/` へ複製しない。

## 8. 否定的結果と限界

1. 閉鎖線形 Hamiltonian 発展は相関行列を純化しない。
2. 一般非線形 Hamiltonian では相関行列方程式が高次モーメントへ結合する。
3. M37は実対称 $h_L$ の局所結合形を位置ばね網へ対応させるが、$\mathcal J_0$ と有効質量 $m$ の普遍値を導かない。
4. 古典振動子による有限次元 Schrödinger 方程式の表示自体は既知である。
5. 相関行列と干渉節だけでは粒子の連続運動を決めない。
6. 選択器角の周辺一様性だけでは Born 型則に足りず、作用分配に対する条件付き一様性が必要である。
7. 一般には比の平均と平均の比は一致しない。
8. 同じ状態の任意基底測定には、単粒子側の調製相関が基底に依存しないことが必要である。
9. M37複素振幅場の空間セル作用比とM35による直接標本化だけでは、粒子の局所位置検出を自動的に与えない。M42は独立粒子変数と局所検出器を追加する。
10. 旧2モード作用殻を位置入口標本化、等方混合、標本化後再埋込みと組み合わせる旧周期は現行Born型機構に使わない。R164は一般作用殻容量と2作用殻線形性だけを、単一母測度、正則化、枝流束誤差を明示して現行M47へ再導入する。
11. M49は4モードプログラム担体を2物理担体へ複製しない。行分解共同原因として $(z_A,z_B,X_A,X_B)$ を同時準備し、交差モーメントと共同配置周辺だけを係数行列へmatchingする。
12. M48の2翼branch seedは、単独周期では内部fair seed、接続周期ではM49 singlet型の01、10成分を等重みで使う。paired-Hopf盆が設定生成後に枝をroutingし、目的枝ラベルを設定前基準測度へ直接書かない。
13. R160はM49の固定singlet出力だけをM48へ接続する。一般M49出力の状態情報は受渡し面に残るが、それを一般Bell結果へ変換するM48 receiverは未構成である。
14. 可変作用B測定の無条件誤差上界は正の枝作用下限を必要としないが、各稀な枝の条件付き精度を一様保証する場合は作用下限が必要である。
15. 無反応試行を除いて共同分布またはCHSH値を再規格化すれば事後選別になる。
16. Bellの定理は否定されず、現行M48では測定設定独立性が成立しない。置換済みM41も同じ前提を破る別構成だった。
17. M48の準備条件付き設定分布と、中央切断後にA設定だけを変更した介入分布は一般に同じでない。
18. 異なる結果履歴を外部記録ごと同じ位相点へ押しつぶす完全再初期化は Hamiltonian ではない。
19. 現M48周期は固定有限設定族の平面内2出力に限定され、一般Tsirelson原理を与えない。
20. 旧2成分場の循環量子化を外したため、Wallstrom 問題への限定的回答も未達である。密度と流れだけを基本変数とする一般的な解決は現行固定目標に含めない。
21. 無理数円回転は一意エルゴード的だが混合的でなく、二項分布型有限標本揺らぎを与えない。
22. 同じ測定装置のテンプレートは測定前状態を保持するため、内部逆計算前の直後再使用には空テンプレートを追加する必要がある。
23. R97により、滑らかな有限時間Hamiltonian 流は、無反応領域なしに連結した初期領域全体を厳密な離散基底状態だけへ写すことができない。
24. 位置ばね網の局所包絡には反回転項があり、局所作用は一般に厳密保存されない。
25. 厳密正常モード包絡は一般に非局所であり、局所物理場として追加できない。
26. 現M48は交差共分散を単一試行で読まず、R152--R154でmatchingされた実現配置を局所記録して共同頻度を作る。置換済みM41は別の2段作用区間を使っていた。
27. M35の厳密な局所交換回路は運動量間結合を含み、M37の通常の位置ばねだけからは導かれない。
28. M37が導く時間独立W型生成子とそのgapだけから、R140の時間依存傾斜制御、切替波形、駆動源の仕事は自動的に従わない。
29. 有限閉鎖系の固定容量では永久記録と使用済みreset 状態を無期限に保存できず、$K$ 実験周期の単純資源上界は $O(K)$ で増える。
30. Q1-1の達成はW型最低2モードと制御可能な傾斜に限定される。深いW型極限では誤差を小さくできるが、$J$ の減少により零傾斜回転時間 $O(\mathcal J_0/J)$ が長くなる。
31. Q1-2はR164でBorn型状態数と有効自由エネルギーの条件付き起源を導出したが、作用容量結合、fiber内平衡化、枝対称性、信号bath反作用の有限局所Hamiltonian統合が未導出なので部分達成である。Q1-3はHopf pumpからresetまでの周期総収支を未導出とするため部分達成である。連続matching保存、経路滞在、枝別整合、周期間帰還はR161--R164を使う有限時間プロトコルへ置き換えた。
32. 傾斜による遷移抑制は左右エネルギー差による離調であり、反復測定間隔に依存するZeno効果ではない。
33. Q1-4は未達として凍結する。旧R101--R103の有限Zeno結果をM47の現行証拠として再利用しない。
34. 観測後の逆計算とreset は、既に記録された同じ観測の分布を遡って変えない。これらは次周期の準備誤差として扱う。
35. M49の操作誘導テンソル積は1つの4頂点プログラム担体内の論理分解であり、2つの独立した古典位相空間または物理担体を与えない。2端の物理変数は別に準備する行分解bath対と配置registerである。
36. M49単独の論理相関は空間的に分離した測定翼のBell統計を示さない。固定singlet接続後のM48も準備先行・非空間分離であり、Bellの定理を否定しない。
37. M49の差モード射影生成子は明示的だが、通常の位置ばねまたは自然なミクロ相互作用から導出していない。
38. M49内部の直接モード符号化を一般化すると $n$ 論理量子ビットに $2^n$ 複素モードを要し、Q2-3の隠れた指数コストを解消しない。
39. 5正準対はM49の4モード担体CNOTと共有時計だけの上界である。一programのR157準備にはM35入力配置選択器16対、2配置register 4対、active bath 4対、2行template bank 8対からなる単純上界32対が必要である。外側program選択器、benchmark出力選択器、履歴、永久記録、resetを含まず、最小性も主張しない。
40. M48単独周期と固定目標Q2-2全体の条件付き達成は、固定singlet型、固定有限設定族、準備先行、非空間分離、採用開放法則に限定する。R160はこのsinglet接続を閉じるが、一般Q2-1出力に対する一般状態Bell receiverは構成しない。
41. M48の切断後局所生成子の可換性は装置変数の因子化であり、空間的に隔たった設定選択または有限伝播円錐を与えない。
42. R152はR161のBell限定形であり、各翼の局所状態数にはR164、局所率の有限衝突近似にはR162を利用できる。ただし独立同分布型有限標本揺らぎ、作用容量fiber、paired-Hopf準備、衝突bathの同一装置統合を導出しない。
43. M48の観測後逆計算とresetは、既に外部記録された同じ周期の共同分布を遡って変えない。
44. M42のBorn型等変性は初期分布 $P(X_0=i)=|b_i(0)|^2$ を必要とし、任意の初期実現配置分布からの収束を与えない。初期作用比準備を独立の物理過程として置く。
45. 局所連続方程式だけでは跳躍率は一意でない。M42の最小率は余分な対称往復流を加えないというモデル選択であり、M37だけから必然的に強制されない。
46. 一様有界な脱出率を持つ連続時間Markov跳躍過程は、正の頂点占有を有限時間で厳密零へ送れない。有限正則化装置では節占有を任意に小さくするが、厳密零にはしない。
47. M42の実現配置がM37担体の全エネルギー、慣性質量、電荷を運ぶことは示していない。有限幅辺チャネル内の1粒子軌道と、格子細分化による連続空間極限は別である。
48. 固定有限 $K$ の完全局所M42構成は $O(K(L+|E|))$ の単純資源上界を持つ。$O(L+K)$ には移動式制御器または明示的な局所通信路が別に必要である。
49. M42の固定時刻局所検出から、初回到達時刻分布、吸収確率、時間積分流束は従わない。Q3-4とQ3-5の改訂後の固定判定は有限時刻の位置分布への接続だけを要求し、これらの到達・吸収量や源、シャッター、全検出器のHamiltonian構成は要求しない。
50. M45の能動項は熱浴と別の非平衡自由エネルギー源を消費する。全局所エネルギー保存、無消費の準備、平衡雑音だけによる準臨界殻を主張しない。
51. M45の直接Langevin軌道は捕捉器 $(s,r)$ だけを動かす。粒子位置 $X$、物理ポテンシャル $V(X)$、無偏向位置拡散核を直接積分していない。
52. $W(X)$ の局所エネルギー帳簿、完全接触選別の時間比例切片則、捕捉器消去後の周辺可逆性は未導出である。条件付き作用素監査を3条件の導出証拠としない。
53. 正で既約なM45条件付き核は正の主固有関数を選ぶため、一般の符号変化する励起固有関数を安定選択しない。励起状態には不変対称sector、物理的節障壁、符号または位相を運ぶ別の縮約量が必要である。
54. R129が与えるのは定常基底状態の $v=0$ Nelson流であり、一般の時間依存密度、非零現在速度、複素位相、Schrödinger時間発展を直接開放模型から導かない。
55. M46の場からrateを作る三角形因果律は、複素振幅を $X$--bath共同統計から後に定義するM47と両立しないため不採用である。旧M46の完全な式を現行因果鎖へ混ぜない。
56. 同じ指数thresholdを二方向で共有するbinary ready条件だけでは、複素振幅の半減衰 $A=D^{1/2}$ は出ない。R131はphase-preserving線形散逸を補助条件として必要とし、M47の統計振幅を導かない。
57. 古典生存核 $\mathsf P_\delta=K_\delta D_\delta$ と補助共分散核 $\mathsf S_\delta=A_\delta K_\delta A_\delta$ は相似でも同じ物理量ではない。一方向の生存終端分布を実現配置周辺またはrank-one因子と同一視しない。
58. R132の線形主共分散選択だけでは、実現配置周辺、一般複素相対位相、有限振幅Hopf定常測度、切断後matchingを準備しない。
59. R133のcurrent-transducer rateは旧M46内の有限グラフ恒等式として残るが、M47の因果律には使用しない。nodeの特異性と自然なミクロrate導出も未解決である。
60. R134はSchrödinger--Madelung式とR133連続極限を仮定した後の形式的恒等式であり、有限古典ミクロ系から一般時間依存Nelson流を導出した結果ではない。
61. M47の準備中は $\lambda_{\rm prep}>0$ の開放系、伝播中は $\lambda_{\rm prep}=0$ の閉鎖系とする。切替は外部操作であり、内部反応座標またはlatchによる完全自律切替を主張しない。
62. R145はQ1の一般M47について信号bath 2モード方向と規格化共分散を目標位相円へ吸引する。R164は操作面ごとの条件付き作用殻状態数、R161、R162は実現配置の条件付きGibbs再平衡化を与える。残るのは作用容量結合、fiber内平衡化、枝対称性、信号bath反作用の有限局所Hamiltonian統合である。
63. R137は閉鎖流による連続matching保存を仮定する旧比較結果である。現行R143、R144は解析器中のmatching破れを許し、操作面ごとのR164作用殻準備、R161再平衡化、R162辺閉鎖を使うためR137を前提にしない。
64. R138は逆累積分布による古典存在構成であり、全密度を先に使う。局所慣性軌道、有限伝播、bath反作用、自然なHopf準備の説明として使わない。
65. 大域共分散のrank-one性は、結果で条件付けた浴共分散が左右固有射影になることを意味しない。R143は結果別テンプレートとの正準交換を別の物理段階として必要とする。
66. 有限障壁ではW型左右位置効果のコントラスト誤差 $\eta_W$ は一般に零でない。生の左右位置を有限パラメータで厳密射影測定と呼ばない。
67. 測定傾斜は $J\ll|\varepsilon_m|\ll G$、切替時間は $\mathcal J_0/G\ll\tau_q\ll\mathcal J_0/J$ を同時に満たす必要がある。この尺度窓が閉じるW型族へR141を適用しない。
68. R143の局所記録は実現配置 $X$ だけを読む。統計振幅、共分散、全密度、確率流またはcurrent rateから結果を生成する模型へ読み替えない。
69. R146により積標本 $z_A\otimes z_B$ の直接共分散をsinglet階数1射影へできない。M48の交差共分散射影を直接標本共分散と呼ばない。
70. M48の規格化交差共分散行列 $B_{AB}$ 自体は階数2である。階数1なのはベクトル化した $B_{AB}$ の外積 $C_{AB}^{\times}$ である。
71. R147の吸引率は雑音零の採用開放方程式に対する結果である。白色雑音を加えた盆横断、枝反転、定常測度、位相拡散へ拡張しない。
72. $h_x=0$ はM48の不変盆境界である。有限時間一様率のため $|h_x|<h_*$ を無反応へ送り、その質量を事後選別で除かない。
73. R148のsinglet交差共分散は集団統計であり、単一試行の $A,B$ 結果を生成しない。結果頻度にはR152--R154の2翼strong matchingと局所instrumentが別に必要である。
74. M48のA設定 $x$ は中央準備流へ入る。B設定 $y$ は切断後に局所的に選べるが、標準的な2側自由設定・空間分離Bell実験を再現したとはいえない。
75. 旧R151の反対称filterは非零入力をglobal phaseを除いて同じ $\mathsf E$ へ潰し、内部fair seedで代替できるため、M39状態を運ぶ物理的受渡しではない。現行R151はM48内部のseed routingである。
76. M48の局所作用様帳簿はbright pumpとdark散逸を示すが、pump、設定controller、sink、切断器を含む総エネルギー・総エントロピー収支を閉じない。
77. R151--R156のM48単独周期とR160で接続した固定目標Q2-2全体は、固定singlet型、固定有限設定族、準備先行、非空間分離、採用開放法則という解釈で条件付き達成である。M41の既存定理は置換済み模型内の結果として保存する。

## 9. 物理的解釈

- 各試行で実在する候補は、局所古典振動子、実現配置、bath作用角またはbath対、源、選択器、装置、記録、外部自由度である。M47の複素振幅とM48の交差共分散はこの一覧へ独立実体として加えない。
- 相関集団測度 $\mu_{\mathcal P,M}$、M35周期の $\mu_{\chi,W}^{\rm M35}$、M47の $X$--bath共同測度、M49のprogram・行分解共同測度、M48設定前基準測度 $\nu_0$、Q2-3・Q3に限定したM42周期の $\mu_{b_0,K}^{\rm M42}$ は役割が異なる。全てを統合する $\mu_*$ は将来目標である。M41の旧基準測度は置換済み研究メモだけで扱う。
- $b$ は局所振動子から作る広がった複素振幅場、$\widetilde b$ は解析用の正常モード包絡、$d$ は補助有効層の正準担体、$X$ はM42が独立に追加する局在粒子位置であり、同じ確立度または役割の変数ではない。
- M42では $|b_i|^2$ は担体の局所作用比であり、初期作用比準備後にはQ2・Q3の実現配置集団の等変分布でもある。1試行の粒子が作用比に従って複数セルへ分裂しているとは解釈しない。Q1にはこの因果律を使わない。
- M42の固定時刻位置検出器はQ3の既存 $X$ を局所的に読む。Born型位置重みは初期作用比準備と等変保存から生じ、検出器が測定時に新しく作るものではない。
- M43は注目系のモード作用とエネルギー占有率を保存したまま環境との位相相関を作る。環境を読まない縮約が開放性であり、エネルギー交換または摩擦を開放系の必要条件としない。
- M45の熱雑音は連続的な揺らぎを供給するが、準臨界殻を維持する能動仕事は別の非平衡自由エネルギー源から来る。熱雑音だけを準備再生機構としない。
- M45の形成方向と保持方向は二つの浴でなく、一つの準安定捕捉器の安定方向と不安定方向である。左右因子が一致するためには、能動自由度を消去した位置周辺核の可逆性が別に必要である。
- M45で準備されるのは未来の雑音波形でなく、定常時刻に準備領域へある局所状態で条件付けた現在の共同測度である。位相すべり後の帰還は外部リセットでなく連続軌道から測る。
- M46の三角形current transducerは不採用である。R130--R132の指数capacity、条件付き線形半減衰、線形主共分散選択だけを補助計算として残す。
- M47では共同測度 $\mu(dX\,d\xi)$ から実現配置周辺 $\rho$ とbath共分散 $C$ を作り、rank-one性と対角matchingが同時に成立する場合だけ $K_W=|\psi_W^{\rm stat}\rangle\langle\psi_W^{\rm stat}|$ と因数分解する。$\psi_W^{\rm stat}$ は共通位相を除く統計記法である。
- M47の信号bath準備と閉鎖伝播は外部 $\lambda_{\rm prep}(t)$ で切り替える。準備中はreservoir、能動供給、散逸、位相基準を許し、伝播中は2作用角のHamiltonian回転を残す。R145は採用方程式の信号bath方向を有限時間評価する。実現配置の熱化には別の有限衝突bathを使う。
- M47の左右占有振動は、W型偶奇2モード間の相対角が $(E_1-E_0)/\mathcal J_0$ で回る交差項から生じる。局所位相比較器またはrate transducerで相対位相を位置へ変換しない。
- M47のmatching多様体は旧連続閉包R137を監査する比較対象である。現行Q1周期は全時刻でそのfiberを保存せず、単一試行bath座標に条件付けたR164の作用殻状態数を操作面ごとに準備し、R161のGibbs分布へ戻す。
- M47の任意軸操作は、W型の零傾斜トンネル項と制御された1次傾斜項が生成する古典2モード正準流である。複素振幅を独立した駆動場として置かない。
- 測定時の傾斜は、分析器後の実現配置を左右井戸に分離固定する。結果は井戸ごとの局所記録セルが既存の $X$ を読み、統計量から新しく標本化しない。
- 安全結果後の浴共分散は、結果別に準備した局在テンプレートとの正準SWAPで更新する。結果条件付けだけから枝別固有状態が生じるとは扱わない。
- R124、R125の位置確率はM42の実現配置集団で読む。単一試行の複数位置への分裂、初回到達、吸収、連続運転スクリーンを意味しない。
- $C$ は指定した調製集団の2次相関であり、独立した物質または単一試行の変数ではない。旧Bell交差相関 $\Xi$ は不採用モデルの研究記録に限る。
- 階数1因子 $\chi$ は共通位相を除いて相対振幅と相対位相を表す。
- M47のBloch球は、rank-one規格化共分散を共通位相について縮約した有効統計状態空間であり、単一試行に物理的な複素振幅場が実在することを意味しない。
- M47のBorn型結果頻度は、任意軸分析器後の信号bath方向に対しR164で条件付き作用殻状態数、R161、R162で実現配置の条件付きGibbs再平衡化を作り、有限コントラスト位置効果として局所的に読むことで生じる。残るのは作用容量結合、fiber内平衡化、枝対称性、信号bath反作用の有限局所Hamiltonian統合である。
- 条件付き測定後状態は、局所記録結果で分岐させた信号bathと準備済み局在テンプレートの正準SWAPを信号部分へ縮約して得る。
- 永久記録は滑らかな比較ポインターを外部セルへ正準コピーして得る。内部逆計算後も結果情報は外部セルに残り、消去されない。
- 弱開放 reset は装置偏差を外部空モードと交換する。旧状態は使用済みセルへ移り、結合強度を弱くする場合は接触時間が長くなる。
- Q1-4のZeno効果は未達として凍結する。傾斜による離調固定を測定間隔依存のZeno抑制と同一視せず、旧M38の有限Zeno周期を現行M47へ接続しない。
- M49の2つの論理部分系は、4モードprogram担体上の2つの相互可換な局所操作代数によって定義される。局所操作は係数行列の階数を保存し、差モード射影流だけが積入力を非因子化する。これは1担体内の論理分解であり、2物理端への分配ではない。
- M49のCNOT流は1つの装置内の論理結合である。同じ担体の作用区間から2配置registerをdecodeし、行templateから2端bathを準備した後、担体、bath、配置へ同じCNOTを同期する。固定有限共同統計にはprogram、入力配置、出力読出しで相異なる選択器を使う。
- M48は設定前共通基準測度から、A設定生成後の開放paired-Hopf流で2翼bath方向を準備する。branch符号は初期 $h_x$ の盆から動力学的に選ばれ、設定依存枝ラベルを初期測度へ直接書かない。
- M48のsinglet型対象は規格化交差共分散をベクトル化した階数1射影である。積bath標本の直接4次元共分散ではなく、単一試行の結果でもない。
- M48の余弦共同頻度は、R152、R153の2翼strong matchingが実現配置周辺と条件付きbath分布を各吸引枝へ接続し、R154の局所分析器が既存の $X_A,X_B$ を記録することで得る。
- M48では切断後の局所応答は因子化するが、測定開始面の分布がA設定に依存する。測定設定独立性の破れと観測周辺の非信号性を別に扱う。
- M48/R151はsetting-pre等重みseedを安全なpaired-Hopf盆へroutingする。M48単独では内部seedを使い、M49接続ではR160が固定singletの01、10配置と同じbath registerをsetting-free面から渡す。旧M39 adapterはM48の必須入力でもstate-carrying受渡しでもない。
- R152のmatching rateはR161のBell限定形であり、各翼の条件付き状態数はR164、局所率はR162の有限衝突bathと粗視化有効自由エネルギー保存散乱から有限誤差で実現できる。作用容量fiberとpaired-Hopf準備を同じ最小Hamiltonianへ統合したことにはならない。
- M48の設定依存性は、設定前共通基準測度から設定生成後の前向き準備流で生じる。目的共同分布を設定条件付き初期測度へ直接書かない。
- M48の測定段階は中央切断後にA、B局所生成子へ因子化するが、測定開始面の完全状態がA設定に依存するため測定設定独立性を満たさない。
- M41の旧周期はR107--R111、R121とともに置換済み研究メモへ残し、現行Q2-2の物理解釈または誤差根拠へ流用しない。
- singlet型の非選択B相関行列は $I_2/2$ でA設定に依存しない。完全な古典分解の設定依存性と観測周辺の非信号性は両立する。

## 10. 最重要の未解決問題

1. R160の固定singlet providerを一般Q2-1出力へ拡張し、一般cross projectorと単一試行配置matchingを結果法則へ変換する一般状態M48 receiverを構成する。
2. Q3-2の位相量子化は、判定基準を保持したまま当面凍結する。
3. R123の有限環境純位相緩和を、連続環境極限、不可逆時間窓、雑音摂動へ拡張できるか判定する。Q3-3の固定目標自体は達成済みである。
4. R124、R125を、散乱透過率、初回到達、吸収、幾何学的開口、源、シャッター、連続運転スクリーンへ拡張できるかを固定目標と分けて判定する。Q3-4、Q3-5の固定目標自体は達成済みである。
5. R164の作用容量結合、fiber内平衡化、枝対称性をW型装置の有限局所Hamiltonianから導き、信号bath保持反作用まで同じ操作面へ統合する。
6. 可変調製、可変基底、M47のQ1装置、M49--M48のQ2周期、M42のQ2-3・Q3装置、M35、外部記録を含むM0と統一Poincaré写像 $\mu_*$ を構成する。
7. M48を準備後の設定変更、空間的に隔たった設定選択、長距離空間分離、有限伝播速度を持つ時計配線へ拡張できるか判定する。
8. 一般非singlet状態でB作用側チャネルを除去し、可変作用測定の条件付き精度と資源を評価する。
9. 高階数相関行列を1本の軌道から生成する源状態用エルゴード自由度を構成する。
10. 無理数回転を混合的なシンプレクティック写像へ強化し、有限標本統計を評価する。
11. Q1-4は未達として凍結する。再開時にはM47の局所記録instrumentを用い、継続駆動、測定窓、有限誤差、永久記録を同じ前向き周期へ接続してからZeno・反Zeno境界を再評価する。
12. M42実現配置の慣性質量、電荷、担体エネルギーとの関係、格子細分化による連続空間極限、多粒子拡張を構成する。
13. M45へ粒子 $X$ と一般の $V(X)$ を局所接続し、$W(X)$ のエネルギー帳簿、時間比例切片則、位置周辺核の可逆性を同じ直接結合計算から導けるか判定する。条件付きNelson加速度と直接結合模型の条件付き加速度の差も評価する。
14. M47について、R145の採用開放方程式を具体的なHopf--bath相互作用から導き、外部 $\lambda_{\rm prep}(t)$ の切断仕事、信号bath保持controllerへの反作用、残留相関を評価する。
15. R164の作用殻fiber、R162の衝突散乱、容量・障壁controller、信号bath保持、R143の局所記録、template交換、R144のfresh-cell resetを同じ有限時間Hamiltonian台帳へ統合する。全時刻matching保存は要求しない。
16. M42を全面退役させる場合は、Q3の固定時刻位置読出し、R118、R124、R125に対応する有限装置を別の単一試行模型で再導出し、達成判定と既存回帰を再評価する。Q1はM47、Q2-1はM49、Q2-2はM48へ移行済みである。
17. R163の粗視化配置過程のゆらぎ関係と有効仕事・熱を、作用殻fiber、Hopf pump、paired-Hopf準備、記録、使用済みセル、resetまで含む周期全体の微視的仕事・熱・情報流へ拡張する。
18. M49の $2^n$ 直接モードコストを監査し、M48を一般状態、準備後の自由設定変更、空間分離、有限伝播時計、独立同分布型有限標本統計へどこまで拡張できるかを、現行の固定singlet接続周期と分けて判定する。

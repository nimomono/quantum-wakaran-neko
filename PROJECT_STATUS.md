# 固定長期目標、モデル運用状態、結果の導出状態、理論の境界

この文書は、固定長期目標とその現在地に加え、モデル、導出結果、物理的解釈を分けて管理する。固定目標と可変の進捗、モデルの運用状態、結果の導出状態、付記情報を混同しない。

## 固定長期目標

以下の目標ID、名称、意味、達成判定基準は、通常の論文更新では変更しない。個々の論文や現行モデルが変わっても一覧から削除せず、追加、削除、統合、分割または判定基準の変更が必要な場合は、独立した方針変更として扱う。

### A. 理論目標

| ID | 固定目標 | 達成判定の中心 |
|---|---|---|
| Q1 | Schrödinger 型有効力学 | 明示的な古典ミクロモデルから、適用範囲と誤差を伴う有効時間発展として導く |
| Q2 | Born 則 | 確率重みを外から置かず、準備、力学、測定過程から導く |
| Q3 | 位相量子化 | 循環量子化、節、単価性を含めて Wallstrom 問題へ回答する |
| Q4 | 重ね合わせと干渉 | 位相差に依存する干渉項を明示的なミクロモデルから導く |
| Q5 | 複合系の状態構成 | テンソル積に相当する状態構造と局所測定を古典自由度から構成する |
| Q6 | 束縛状態 | 井戸型・調和型ポテンシャルで固有エネルギー、確率密度、節、安定性を再現する |
| Q7 | トンネル効果 | 障壁高、幅、入射エネルギーに対する透過率を再現し、古典的熱活性化と区別する |
| Q8 | Rabi 振動 | 駆動2準位系の振動数、振幅、離調依存性を再現する |
| Q9 | 2重スリット干渉 | 準備から検出までの明示的なモデルで位置分布を再現する |
| Q10 | Bell 型測定統計 | 余弦共同確率、CHSH不等式の破れ、Tsirelson 限界、非信号性、Bell の前提違反を一貫して示す |
| Q11 | 完全な測定周期 | 準備、設定生成、結果形成、増幅、記録、消去、再初期化を1周期として構成する |
| Q12 | 一般性と頑健性 | 特定モデルの偶然ではなく、一定範囲の古典系に共通する機構として示す |

### B. 工学的実証目標

工学的実証では、論文で導出した縮約機構を実際に利用する。古典的な振幅ベクトルへ量子ゲートの行列を作用させるだけの既知のアナログ模擬とは区別する。

| ID | 固定目標 | 達成判定の中心 |
|---|---|---|
| D1 | 古典自由度への状態符号化 | 量子コヒーレンスを前提としない巨視的な古典変数だけで状態と位相を操作できる |
| D2 | 単一量子ビット型ゲート | 反転、位相、Hadamard 型操作を同じ装置原理で実現する |
| D3 | 2量子ビット型操作 | 複合状態とCNOT または制御位相型操作を実現する |
| D4 | 完全な装置周期 | 準備、ゲート列、測定、消去、再初期化を連続動作させる |
| D5 | 性能と資源増加の評価 | 忠実度、雑音耐性、エネルギー収支、必要自由度の規模依存性を評価する |

### 現在地

この表は論文と現行モデルの更新に合わせて変更する。「達成」は達成判定基準を満たした状態、「部分達成」は限定されたモデルまたは一部の構成要素で達成した状態、「未達」は理論上の作業対象だが基準を満たしていない状態、「未着手」は具体的なモデルまたは検証がまだない状態を表す。達成した目標も固定一覧から削除せず、達成状態と根拠を保存する。

| 目標ID | 現在地 | 根拠となる結果 | 残る課題 |
|---|---|---|---|
| Q1 | 部分達成 | R26、R39–R41 | coherent集中と密度同期の準備・維持、一様な特異極限、M0からの停留点選択、節を含む領域での制御 |
| Q2 | 部分達成 | R24 | 任意基底、一般測定器、共通流束因子のミクロ導出、標本化後の完全周期 |
| Q3 | 部分達成 | R27 | 節の生成・消滅、正則化極限、全ての物理的流れの単価な場からの準備 |
| Q4 | 未達 | — | 一般状態の重ね合わせ構造と干渉項の導出 |
| Q5 | 部分達成 | R29、R30 | 低ランク対モードを超える一般的な複合状態の構成と局所測定への拡張 |
| Q6 | 未達 | — | 2種の標準ポテンシャルに対する定量検証と状態選択 |
| Q7 | 未達 | — | 透過率の定量導出と熱活性化との識別 |
| Q8 | 未達 | — | 駆動2準位系の明示モデルと定量検証 |
| Q9 | 未達 | — | 準備、伝播、検出を含む明示モデル |
| Q10 | 部分達成 | R30、R32–R36 | 共通境界測度の物理的準備、一般 Tsirelson 原理、非対称準備での非信号性、完全周期 |
| Q11 | 未達 | R20、R28、R37–R41 | 結果形成、境界測度準備、外部記録、準備浴再初期化、再埋め込み、長期resetの統合 |
| Q12 | 未達 | R23、R29、R34 | 固定モード分離、共通境界密度、交差誤差を含む成立範囲の証明 |
| D1 | 未着手 | — | 装置原理と状態符号化の設計 |
| D2 | 未着手 | — | 単一量子ビット型操作の実装 |
| D3 | 未着手 | — | 複合状態と結合操作の実装 |
| D4 | 未着手 | — | 完全な連続動作周期の実装 |
| D5 | 未着手 | — | 性能指標と必要資源の測定 |

## 1. モデルの運用状態

| 識別 | モデル | 運用状態 | 役割と限界 |
|---|---|---|---|
| M0 | 粒子・装置・構造化2成分誘導場・準備浴・共通未来比較領域・外部流路を持つ弱開放系 | 現行モデル | 位相接続を力学の主線とし、その前段に局所作用整合・動径低速化の準備窓を置く。Born 入口殻、Bell 対モード、2モード比較器を固定部分空間へ置く。全周期と一様縮約定理は未完成 |
| M1 | 1試行内の有限閉鎖系 | 補助モデル | 正準写像、Liouville モーメント、保存量、作用殻体積を厳密に計算する |
| M2 | 運動量結合した線形誘導場 | 補助理論・M0非採用 | 配置速度揺らぎと反作用記憶項を正確に分ける。付録D |
| M3 | 配置変数の二側 Markov 拡散 | 補助理論・M0非採用 | 前進・後退流れと Fisher 項を整理する。M0からの導出は未達 |
| M4 | 線形 Fourier–Gauss 型確率表示 | 補助理論・M0非採用 | 作用形式の $C^1$ 収束を検証する |
| M5 | Gauss 幅の Routh 縮約 | 補助理論・M0非採用 | 固定内部作用と Fisher 項の一致を示す可解例 |
| M6 | 最小結果符号化器 | 補助モデル | 既存の2値符号を伝達ベクトルと固定指針へ写す。一般測定器ではない |
| M7 | 3モード境界作用殻 | 不採用モデル | 旧 Bell 模型。数学的結果は `notes/superseded_three_mode_bell_shell.md` に保存 |
| M8 | U(3) 等方殻拡散 | 不採用モデル | 旧3モード全殻の準備模型。現行2モード比較殻には使わない |
| M9 | 残余2モードの U(2) 混合 | 不採用模型の否定的補助結果 | 旧模型でファイバー内部の一様性だけでは不足することを示す |
| M10 | OU 位相表示 | 補助モデル | 弱開放部分系の緩和と回転の例。詳細は `notes/gaussian_nelson_examples.md` |
| M11 | 終端関数、中央比較器、相補時計による Bell 重み | 不採用モデル | Bell 固有の終端統計入力を必要とする。`notes/superseded_terminal_function_model.md` |
| M12 | 順時間的共有浴、結果依存滞在時間、相補終端半空間 | 不採用モデル | 試行頻度を得ない、事後選別になる、または余弦項を消す。詳細は `notes/` |
| M13 | 位置結合から力密度を Fisher 応力へ閉じる経路 | 不採用モデル | Fisher 項へ遠回りで、未証明の構成則を必要とする。`notes/superseded_position_coupling_fisher_closure.md` |
| M14 | 位相接続2成分活性場 | 補助モデル | coherent縮約多様体上で Madelung 作用と条件付き循環量子化を与える。多様体の準備は未達 |
| M15 | 2モード境界作用殻による位置入口標本化 | 補助モデル | 共通流束因子の下で Born 型位置重みを与える。一般測定器と再埋め込みは未達 |
| M16 | 同じ2成分場の低ランク反対称対モード | 補助モデル | 2つの固定源チャンネルと局所回転から Bell 余弦作用を与える |
| M17 | 共通未来の2モード境界比較器 | 補助モデル | 相関振幅の実部・虚部を比較作用へ理想的に転送する |
| M18 | 2モード Bell 共通境界流束 | 補助モデル | 共通境界密度の下で Bell 共同確率を与える。物理的準備は未達 |
| M19 | 比較、記録コピー、逆計算周期 | 補助モデル | 比較器の理想的逆計算を与える。長期resetと一般結果形成は未達 |
| M20 | 局所作用整合・動径低速 Hamiltonian | 補助モデル | 有限特異パラメータの低速枝として $P_i$ と $J_i-\mathcal J_\phi q_i$ を小さくする。一般初期値からの吸引は含まない |
| M21 | 時計制御された有限振幅浴・作用交換浴 | 補助モデル | 準備窓で欠陥エネルギーを有限浴へ移す。全作用保存は厳密だが、欠陥減衰は短記憶・固定振幅・低温近似に依存する |

M0が現在の主モデルである。M0の固定射影、位相接続、対モード源、局所測定器、戻り伝播、比較器、境界測度、記録、逆計算、再初期化を1本の有限幅 Hamiltonian へ統合した完成モデルはない。

M1、M6、M10、M14からM21の厳密結果は、それぞれの補助モデルまたは縮約方程式の内部での結果である。M2からM5は現行M0に採用しない補助理論、M7からM9は不採用の旧 Bell 模型である。M0から各補助モデルへの接続が未完成なら、M0全体から導出済みとは呼ばない。

## 2. 共通ミクロ構成の導出結果

### R1 固定した明・暗モード基底

- 導出状態：**厳密結果**
- 対象モデル：M0、M1、M2

Fisher 側の運動量結合方向 $\operatorname{Ran}C_N^{\mathsf T}$ と、Bell 側の装置座標結合方向 $\operatorname{Ran}B_q$ を含む固定明部分空間を選べる。直交行列 $O_N$ を場座標と場運動量へ同時に作用させる変換は正準である。

暗モードを直接駆動しない条件は

```math
P_{\rm D}C_N^{\mathsf T}
=
0,
\qquad
P_{\rm D}B_q
=
0
```

である。$K_N$ と $M_N^{-1}$ による間接伝播は一般に残る。

### R2 固定位相整合射影

- 導出状態：**厳密結果**
- 性質：成立条件
- 対象モデル：M0、M1、M2

位相整合射影 $P_{\rm c}$ と欠陥射影 $P_\perp$ は、$K_N$ と $M_N$ の固定スペクトル帯、$C_N^{\mathsf T}$ と $B_q$ が生成する Krylov 部分空間、保存作用、装置構造から事前に定める。

得られた密度または目標量子状態を見て射影を選ぶ構成は採用しない。射影の正準性は、欠陥成分だけの減衰、配置 Markov 性、局所セクターの分離を意味しない。

### R3 選択的な欠陥減衰

- 導出状態：**予想・未解決**
- 性質：中心課題
- 対象モデル：M0

外部流路を $P_\perp$ へ強く、$P_{\rm c}$ へ弱く結合し、欠陥成分だけを速く除去する候補を示す。R40とR41は局所作用欠陥と動径欠陥に限ってこの候補を具体化するが、一般の $P_\perp$、coherent集中、密度同期を扱わない。指数減衰率、流入補正、位相整合成分の寿命を、特定の外部スペクトルから一様に導く定理はない。

## 3. Fisher 側の導出結果

### R4 運動量結合 Hamiltonian の成立条件

- 導出状態：**厳密結果**
- 性質：成立条件
- 対象モデル：M0、M1、M2

運動量2次形式

```math
\frac12
\begin{pmatrix}
P\\
\Pi
\end{pmatrix}^{\mathsf T}
\begin{pmatrix}
m^{-1}I_d & C_N\\
C_N^{\mathsf T} & M_N^{-1}
\end{pmatrix}
\begin{pmatrix}
P\\
\Pi
\end{pmatrix}
```

は

```math
M_N^{-1}
-
mC_N^{\mathsf T}C_N
>
0
```

のとき正定値である。運動量の同時反転で偶なので、他の項も時間反転不変なら有限閉鎖核は時間反転対称である。

### R5 配置流束と1次モーメント

- 導出状態：**厳密結果**
- 対象モデル：M0、M1、M2

線形核の配置速度を

```math
U_N
=
\frac Pm
+
C_N\Pi
```

とすると、全 Liouville 密度から

```math
\partial_t\rho_N
+
\nabla\cdot
\left(
\rho_Nv_N
\right)
=
0,
\qquad
v_N
=
\mathbb E_N
\left[
U_N
\mid
X=x
\right]
```

を得る。正準運動量の輸送流束は $\mathbb E_N[P\otimes U_N\mid X=x]$ を含む。配置速度の1次モーメントには、場の加速度項と条件付き速度共分散が現れる。$P/m$ だけを配置速度とする旧式は使わない。

### R6 線形誘導場の正確な消去

- 導出状態：**厳密結果**
- 性質：指定初期値または指定境界条件の下で成立
- 対象モデル：M2

質量規格化した線形誘導場では、

```math
Y_N(t)
=
Y_N^{\rm free}(t)
-
\int_0^t
C_N\Omega_N
\sin
\left[
\Omega_N(t-s)
\right]
C_N^{\mathsf T}P(s)
\,\mathrm ds
```

と分けられる。第1項は自由速度揺らぎ、第2項は反作用速度記憶である。指定した Gauss 型初期集団では、

```math
R_N(t-s)
=
\Theta_NC_N
\cos
\left[
\Omega_N(t-s)
\right]
C_N^{\mathsf T}
```

を得る。有限 $N$ では相関は余弦関数の有限和であり、厳密な OU 相関ではない。自己共役な二側境界条件では Green 核が時間交換対称になる。

### R7 配置変位の Brown 極限

- 導出状態：**予想・未解決**
- 性質：中心課題・近似結果として示すべき対象
- 対象モデル：M0から有効位相空間過程への接続

目標は、再帰前の窓で

```math
\int_0^t
Y_N^{\rm free}(s)
\,\mathrm ds
\Longrightarrow
\sqrt{2\nu}\,W_t
```

を示すことである。多数モード、短記憶、スペクトル包絡、異方性、弱開放補正、有限再帰の評価は未完成である。反作用記憶項を質量繰り込み、局所ドリフト、制御可能な残差へ分ける必要もある。

### R8 配置変数の Markov 閉鎖

- 導出状態：**予想・未解決**
- 性質：中心課題
- 対象モデル：M0からM3への接続

配置雑音を持つ $(X,P)$ の位相空間過程が得られても、$X$ 射影は一般に Markov ではない。配置変数だけの前進・後退 Markov 拡散、共通拡散係数 $\nu$、小さい非 Markov 残差を同じ極限から示す必要がある。条件付き速度分散が余分な古典圧力を残さない条件も未完成である。

### R9 二側配置拡散の Fisher 構造

- 導出状態：**厳密結果**
- 性質：仮説依存
- 対象モデル：M3

共通の正の密度と共通の等方拡散係数 $\nu$ を持つ前進・後退 Markov 拡散では、

```math
v
=
\frac{b_++b_-}{2},
\qquad
u
=
\frac{b_+-b_-}{2}
=
\nu\nabla\log\rho
```

が成立する。従って

```math
\frac m2
\int
\rho|u|^2
\,\mathrm dx
=
\frac{m\nu^2}{2}
\mathcal I[\rho]
```

である。量子ポテンシャルと Fisher 応力は

```math
Q[\rho]
=
-2m\nu^2
\frac{\Delta\sqrt\rho}{\sqrt\rho},
```

```math
P_F[\rho]
=
-m\nu^2\rho
\,\nabla\nabla\log\rho
```

であり、

```math
-\nabla\cdot P_F[\rho]
=
-\rho\nabla Q[\rho]
```

が厳密に成立する。

### R10 時間対称動力学

- 導出状態：**予想・未解決**
- 性質：中心課題
- 対象モデル：M0からM3への接続

配置拡散だけから

```math
ma_{\rm ts}
=
-\nabla V
```

は従わない。時間対称 Green 応答、反作用記憶、条件付き変分からこの Newton 則へ収束することを示す必要がある。これが成立し、$v=\nabla S/m$ と書ける領域では $\hbar_{\rm eff}=2m\nu$ の局所 Schrödinger 表示を得る。Wallstrom 問題は含まない。

### R11 Gauss 幅の Routh–Fisher 一致

- 導出状態：**厳密結果**
- 性質：成立条件・補助検証
- 対象モデル：M5

1次元 Gauss 密度では $\mathcal I[\rho_\sigma]=1/\sigma^2$ である。2次元内部座標の循環作用を $J=m\nu$ に固定して Routh 縮約すると、

```math
-\frac{J^2}{2m\sigma^2}
=
-\frac{m\nu^2}{2}
\mathcal I[\rho_\sigma]
```

となる。配置拡散極限または時間対称 Newton 則を導くものではない。

### R12 線形 Gauss 型作用の <i>C</i><sup>1</sup> 収束

- 導出状態：**厳密結果**
- 性質：成立条件・補助検証
- 対象モデル：M4

有限 Fourier–Gauss 型駆動、線形流れ、Gauss 初期分布、正定値の有限分解能終端記録、2次ポテンシャル、滑らかな有限次元パラメータ族について、

```math
\left\|
\mathcal A_{N,h}^{R,U}
-
\mathcal A_{\rm GM}^{R,U}
\right\|_{C^1(K)}
\leq
C_K
\left(
\frac hT
+
\frac{T^2}{Nh^2}
\right)
```

が成立する。Guerra--Morato 表示と Nelson 表示は正の密度領域で厳密に一致する。

M0からM4を導くこと、微視的運動が作用停留点を選ぶこと、R7、R8、R10を証明することは含まない。

## 4. 旧3モード Bell 模型の保存結果

R13からR19は draft-15 までの旧3モード模型に属する。数学的結果と否定的結果を保存するが、現行M0の Bell 経路には用いない。現行2モード模型の結果は R29以降で管理する。

### R13 局所セクターの近似分離

- 導出状態：**近似結果**
- 性質：成立条件・適用限界
- 対象モデル：M0、M1

測定窓の全交差応答比 $\varepsilon_{\rm loc}\ll1$ なら、左右局所記録を $O(\varepsilon_{\rm loc})$ の範囲で分離して扱う。全応答には座標–座標核と運動量–座標混合核を含む。結合ベクトルの直交性だけでは十分でない。

### R14 和・差作用の余弦幾何

- 導出状態：**厳密結果**
- 性質：不採用模型内の保存結果
- 対象モデル：M1、M6、M7

位相同期した実2次元伝達ベクトルについて、

```math
I_\pm^{AB}
=
I_0
\left[
1
\pm
ABV\cos\Delta_{ab}
\right],
\qquad
I_+^{AB}
+
I_-^{AB}
=
2I_0
```

を得る。

### R15 U(2) 等方性の不足

- 導出状態：**厳密結果**
- 性質：否定的結果
- 対象モデル：M9

残余2モードの $U(2)$ 不変性は、固定 $J_+$ のファイバー内部を一様化するだけで、異なる $J_+$ のファイバー間質量を決めない。

### R16 U(3) 共通殻測度と等方拡散

- 導出状態：**厳密結果**
- 性質：不採用模型内の保存結果・仮説依存
- 対象モデル：M7、M8

$U(3)$ 不変な固定作用殻測度は一意である。縮約方程式

```math
\partial_tf
=
D_\partial\Delta_{S^5}f
```

の一意な定常確率密度は共通殻上の定数密度である。

M0からこの生成子を導くことは未完成である。常時の弱漏れは殻方向の等方化ではなく、欠陥除去、再帰抑制、半径分布の維持を担う。

### R17 共通殻の周辺と残余ファイバー

- 導出状態：**厳密結果**
- 性質：不採用模型内の保存結果
- 対象モデル：M7

共通作用殻 $J_++J_s+J_r=C_0$ の正規化 Liouville 測度について、

```math
p_+(x)
=
\frac{2(C_0-x)}{C_0^2}
```

を得る。共通 Jacobian で $J_+=I_+^{AB}$ を課した残余ファイバー体積は

```math
W_{AB}
\propto
C_0-I_+^{AB}
=
J_*+I_-^{AB}
```

となる。

### R18 Bell 型共同確率

- 導出状態：**厳密結果**
- 性質：不採用模型内の保存結果・仮説依存
- 対象モデル：M6、M7、M8

共通作用殻、等しい結果セクター基準質量、共通の境界分解能と coarea Jacobian、固定総入力作用の下で、

```math
P(A,B\mid a,b)
=
\frac14
\left[
1
-
V_{\rm eff}AB\cos\Delta_{ab}
\right],
\qquad
V_{\rm eff}
=
\frac{I_0}{J_*+I_0}V
```

を得る。

### R19 履歴測度と Bell 前提監査

- 導出状態：**厳密結果**
- 性質：不採用模型内の保存結果・Bell 前提監査
- 対象モデル：M7

履歴測度は、全境界正準位相空間の Liouville 測度を作用保存と境界適合条件で制限し、解写像で許容履歴空間へ押し出す。解空間へ直接 Liouville 測度を置くものではない。

境界適合条件が $a,b$ に依存するため、完全履歴分布は一般に測定設定独立性を満たさない。対称セクターでは一側周辺は $1/2$ である。偏った準備での一般的な非信号性は未証明である。

### R20 一般測定器と試行周期

- 導出状態：**予想・未解決**
- 性質：中心課題
- 対象モデル：M0

現在の明示装置は既存の2値符号を記録する最小結果符号化器である。結果形成、増幅、安定記録、境界適合、消去、再初期化を、開始数と記録数を減らさない1周期として統合していない。

## 5. 位相接続と Born 型入口標本化の導出結果

### R21 位相接続 Hamiltonian と保存位相作用

- 導出状態：**厳密結果**
- 性質：成立条件
- 対象モデル：M14

有限セル2成分場では、

```math
\sum_i
\boldsymbol\Pi_i\cdot d\boldsymbol\Phi_i
=
\sum_i
\left(
p_{r,i}\,dr_i+j_i\,d\theta_i
\right)
```

が成立する。共通内部回転の生成子

```math
\mathcal J_\phi
=
\sum_i j_i\Delta V
```

は、全 Hamiltonian が共通回転不変なら保存される。正則化接続

```math
\mathbf a_\varepsilon
=
\frac{
\Phi_1\nabla\Phi_2
-
\Phi_2\nabla\Phi_1
}{
|\boldsymbol\Phi|^2+\varepsilon^2
}
```

と

```math
H_{\rm p}
=
\frac{
\left|
P-\mathcal J_\phi\mathbf a_\varepsilon(X)
\right|^2
}{
2m
}
+
V(X)
```

は共通内部回転に不変である。標準時間反転では $P$ と $\mathcal J_\phi$ が同時に反転するため、両符号sectorを含む全理論は時間反転対称である。

### R22 固定作用下の局所作用分配

- 導出状態：**厳密結果**
- 性質：エネルギー最小配置
- 対象モデル：M14

規格化 $\sum_i r_i^2\Delta V=1$ と固定全作用 $\sum_i j_i\Delta V=\mathcal J_\phi$ の下で、

```math
E_{\rm rot}
=
\frac{\mathcal J_\phi^2}{2I}
+
\sum_i
\frac{
\left(
j_i-\mathcal J_\phi r_i^2
\right)^2
}{
2Ir_i^2
}
\Delta V.
```

従って一意な最小配置は

```math
j_i
=
\mathcal J_\phi r_i^2.
```

閉鎖 Hamiltonian 流がこの配置へ吸引されることは含まない。

R39は同じ欠陥平方項を高速化した有限 Hamiltonian の低速枝を与えるが、R22の静的最小化結果を置き換えない。

### R23 一般作用殻容量と作用分配次元の剛性

- 導出状態：**厳密結果**
- 性質：成立条件・否定的結果
- 対象モデル：M1、M7、M15

$n$個の複素作用モードの固定総作用殻容量は

```math
\Omega_n(A)
=
\frac{
(2\pi)^n
}{
(n-1)!
}
A^{n-1}.
```

活性モードへ作用を直接分配する明反応方向が $q$ 個なら、入口容量は $A^q$ に比例する。Born 型の線形重みには $q=1$ が必要である。

### R24 境界 Liouville 流束による Born 型入口密度

- 導出状態：**厳密結果**
- 性質：仮説依存・成立条件
- 対象モデル：M15

局所作用

```math
A_i
=
A_{\rm tot}r_i^2\Delta V
```

を、選択された活性モードと1つの共有明反応座標が分配する。入口チャンネルが排他的で、法線速度、障壁、coarea Jacobian、spectator因子が共通なら、

```math
P_i
=
\frac{A_i}{A_{\rm tot}}
=
r_i^2\Delta V,
```

```math
\rho_{{\rm in},i}
=
r_i^2.
```

これは位置入口分布に限定された Born 型結果であり、任意基底の一般 Born 則ではない。

### R25 coherent多様体上の密度同期差保存

- 導出状態：**厳密結果**
- 性質：仮説依存
- 対象モデル：M14

位相変分から

```math
\partial_tj
+
\mathcal J_\phi
\nabla\cdot(\rho v)
=
0
```

を得る。$j=\mathcal J_\phi r^2$ と粒子の連続の式の下で、

```math
\partial_t
\left(
r^2-\rho
\right)
=
0.
```

これは入口で作られた同期差の中立的保存であり、同期多様体への吸引ではない。

### R26 縮約多様体上の Madelung 作用

- 導出状態：**厳密結果**
- 性質：仮説依存・成立条件
- 対象モデル：M14

coherent集中、局所作用分配、密度同期、単流束化、動径断熱化、節から離れた接続極限の下で、

```math
\mathcal A_{\rm red}
=
\int
\left[
\frac m2\rho|v|^2
-
\rho V
-
\rho
\left(
\partial_tS+v\cdot\nabla S
\right)
-
\kappa
\left|
\nabla\sqrt\rho
\right|^2
\right]
\,dx\,dt,
```

```math
S=-\mathcal J_\phi\theta.
```

係数整合

```math
\kappa
=
\frac{\mathcal J_\phi^2}{2m}
```

の下で Madelung 作用に一致し、

```math
\hbar_{\rm eff}
=
|\mathcal J_\phi|
```

の局所 Schrödinger 型方程式を得る。M0から縮約多様体を準備し、この作用の停留点を選ぶことは未完成である。

### R27 単価な2成分場による条件付き循環量子化

- 導出状態：**厳密結果**
- 性質：仮説依存・部分達成
- 対象モデル：M14

場が閉曲線上で非零かつ単価なら、

```math
\oint
\nabla\theta\cdot d\ell
=
2\pi n,
\qquad
n\in\mathbb Z.
```

従って

```math
\oint
\nabla S\cdot d\ell
=
-2\pi\mathcal J_\phi n.
```

節の生成・消滅、正則化極限、全ての物理的流れの準備は未完成であり、Wallstrom 問題の全面解決ではない。

### R28 coherent準備、等方化、再埋め込み、再初期化

- 導出状態：**予想・未解決**
- 性質：中心課題
- 対象モデル：M0、M14、M15

2モード入口殻の等方準備、全チャンネルでの共通流束因子、標本化後の結果記録、活性場の coherent部分空間への再埋め込み、共有明反応座標の復元、garbage自由度の処理を、事後選別なしの有限 Hamiltonian 周期へ統合していない。M21は再埋め込み後の局所作用欠陥と動径欠陥を小さくする候補になるが、元の振幅、記録、明反応座標、garbageを復元しない。

## 6. 現行2モード Bell 模型の導出結果

### R29 同じ場の固定対モードと低ランク相関

- 導出状態：**厳密結果**
- 性質：成立条件
- 対象モデル：M0、M1、M16

同じ2成分場の固定直交モードから、左右の2出力モードと2つの源チャンネルを正準に切り出せる。低ランク相関を

```math
C_{\mu\nu}
=
\sum_{r=1}^{2}
\eta_r
z^A_{\mu r}
\left(
z^B_{\nu r}
\right)^*
```

と定める。$C$ は独立した正準場でなく派生量であり、共通内部回転に不変である。位相、対、比較部分空間の交差応答を小さくする動力学は未完成である。

### R30 反対称源からの Bell 余弦作用

- 導出状態：**厳密結果**
- 性質：準備した源模型内部
- 対象モデル：M16

2つの直交源チャンネルが

```math
C_0
=
\sqrt{\frac{\mathcal K}{2}}
\begin{pmatrix}
0&1\\
-1&0
\end{pmatrix}
```

を準備し、左右分析器が局所出力モードを回すと、

```math
K_{AB}
=
\frac{\mathcal K}{4}
\left[
1-AB\cos\Delta_{ab}
\right]
```

を得る。さらに $\sum_{A,B}K_{AB}=\mathcal K$、$\sum_BK_{AB}=\mathcal K/2$ が成立する。反対称源の動力学的準備と長時間の相対位相保持は未完成である。

### R31 局所分析器と有限伝播

- 導出状態：**近似の下で導出済み**
- 性質：局所性条件
- 対象モデル：M0、M16

左右分析器は $H_A(a)+H_B(b)$ の局所和で出力モードを回転する。戻りモードが共通未来へ有限速度で伝播し、損失、分散、交差応答が $O(\varepsilon_{\rm ret})$ なら、比較窓の入力相関は $C_{AB}+O(\varepsilon_{\rm ret})$ である。

### R32 理想比較読み出しと作用転送

- 導出状態：**厳密結果**
- 性質：理想パルス
- 対象モデル：M17

```math
H_{\rm read}
=
g_{\rm read}(\vartheta)
\left[
P_{\rm R}\operatorname{Re}C
+
P_{\rm I}\operatorname{Im}C
\right]
```

とし、比較正準対を零から始める。比較中の他の自由発展を無視できるなら、入力モードを乱さず

```math
Q_{\rm R}^{\rm out}
=
\Gamma\operatorname{Re}C_{AB},
\qquad
Q_{\rm I}^{\rm out}
=
\Gamma\operatorname{Im}C_{AB}
```

を得る。比較総作用は

```math
A_\partial^{AB}
=
\frac{\Gamma^2}{2}K_{AB}.
```

有限パルスでは $O(\varepsilon_{\rm pulse})$ の補正がある。

### R33 2モード比較殻の作用保存と線形容量

- 導出状態：**厳密結果**
- 対象モデル：M1、M17

Hermitian な $u(2)$ 生成子による混合は

```math
\left\{
A_\partial,
H_{\rm mix}
\right\}
=
0
```

を満たす。比較2モード殻容量は

```math
\Omega_2
\left(
A_\partial^{AB}
\right)
=
(2\pi)^2
A_\partial^{AB}
\propto
K_{AB}.
```

等方な初期集団の準備は別の仮定である。

### R34 正準混合によるsector質量保存

- 導出状態：**厳密結果**
- 性質：否定的結果
- 対象モデル：M1、M15、M17

正規化されたsector測度 $\mu_{AB}$ を読み出しと $U(2)$ 混合で押し出しても、

```math
\left(
\mathcal U_{AB}
\right)_*
\mu_{AB}
\left(
\Gamma^{AB}
\right)
=
\mu_{AB}
\left(
\Gamma^{AB}
\right)
```

である。従って Hamiltonian 読み出しと殻内混合だけでは、結果sector総質量を $\Omega_2(A_\partial^{AB})$ に比例させられない。Born 側でも同じ制限がある。

### R35 共通境界測度下の Bell 共同確率

- 導出状態：**厳密結果**
- 性質：仮説依存・成立条件
- 対象モデル：M18

4結果sectorの基準多重度が等しく、境界密度、時計流束、coarea Jacobian、spectator体積、解多重度が共通なら、

```math
P(A,B\mid a,b)
=
\frac14
\left[
1-AB\cos\Delta_{ab}
\right]
```

を得る。共通境界測度を実験周期が準備することは未完成である。

### R36 非信号性と Bell 前提監査

- 導出状態：**厳密結果**
- 性質：仮説依存・Bell 前提監査
- 対象モデル：M18

R35の対称条件の下で、一側周辺は $1/2$ であり非信号性が成立する。境界測度を生成側へ引き戻すと一般に

```math
\rho(\Lambda\mid a,b)
\neq
\rho(\Lambda).
```

Bell の前提違反は測定設定独立性にある。非対称準備と結果依存誤差を含む一般的な非信号条件は未証明である。

### R37 比較器の理想逆計算

- 導出状態：**厳密結果**
- 性質：理想周期
- 対象モデル：M19

結果を空の外部記録へ可逆にコピーした後、$U(2)$ 混合、比較読み出し、戻り伝播、局所分析を逆実行すれば、理想比較器を基準状態へ戻せる。入力戻りモードは理想読み出しで乱されない。

外部記録を含む全自由度を同じ1点へ戻すことはできない。異なる結果情報を外部記録、garbage、仕事源、環境のいずれかへ残す必要がある。

### R38 共通境界測度と一般測定周期

- 導出状態：**予想・未解決**
- 性質：中心課題
- 対象モデル：M0

共通境界密度を事後選別なしで準備し、設定生成、唯一結果形成、増幅、局所記録、比較、外部記録、逆計算、再初期化を、開始数と完了数を結果別に減らさない1周期へ統合していない。

## 7. 局所作用整合と動径低速化の導出結果

### R39 有限特異 Hamiltonian の低速枝

- 導出状態：**厳密結果**
- 性質：成立条件・形式的特異極限
- 対象モデル：M20

セル体積を吸収した $R_i$、$P_i$、$J_i$ と

```math
N
=
\sum_iR_i^2,
\qquad
\delta J_i
=
J_i
-
\frac{
\mathcal J_\phi R_i^2
}{
N
}
```

について、

```math
\sum_i
\frac{
\delta J_i^2
}{
R_i^2
}
=
\sum_i
\frac{
J_i^2
}{
R_i^2
}
-
\frac{
\mathcal J_\phi^2
}{
N
}
```

が厳密に成立する。この欠陥項と動径運動項を $1/\epsilon_{\rm s}$ で高速化した有限 Hamiltonian の Legendre 変換から、正規化単体上で

```math
P_i
=
\frac{
\epsilon_{\rm s}M
}{
2\sqrt{q_i}
}
\dot q_i,
```

```math
\delta J_i
=
\epsilon_{\rm s}Iq_i
\left(
\dot\theta_i
-
\bar\omega
\right)
```

を得る。有界な低速枝では両者は $O(\epsilon_{\rm s})$ である。有限エネルギーだけなら一般に $O(\sqrt{\epsilon_{\rm s}})$ であり、一般初期値に対する解の一様収束は含まない。

### R40 有限作用交換浴の全位相作用保存

- 導出状態：**厳密結果**
- 性質：保存則・成立条件
- 対象モデル：M21

各グラフ辺の位相差 $\phi_e=\theta_i-\theta_k$ に対し、同じスペクトルを持つ $\cos\phi_e$ と $\sin\phi_e$ の有限調和浴を対にして結合する。全 Hamiltonian は共通位相回転に不変なので、時計窓の立ち上がりと立ち下がりを含め、

```math
\left\{
\mathcal J_\phi,
H_{\rm all}
\right\}
=
0.
```

余弦・正弦対の逆項は $\cos^2\phi_e+\sin^2\phi_e=1$ により位相差に依存せず、不要な位相固定ポテンシャルを作らない。

### R41 準備近似での局所作用欠陥減衰

- 導出状態：**近似の下で導出済み**
- 性質：固定振幅・短記憶・低温・再帰前
- 対象モデル：M21

準備中の $q_i>0$ を固定し、連結グラフの Laplacian を $L_G$、$D_q=\operatorname{diag}(q_i)$ とする。作用交換浴を短記憶化し、$H_0$ の位相力と雑音を無視すると、

```math
\dot{\boldsymbol{\delta J}}
=
-
\frac{
\eta_J
}{
\epsilon_{\rm s}I
}
L_GD_q^{-1}
\boldsymbol{\delta J}.
```

従って欠陥エネルギーは

```math
\frac{
dE_{\delta J}
}{
dt
}
=
-
\frac{
\eta_J
}{
\epsilon_{\rm s}^2I^2
}
\left(
D_q^{-1}
\boldsymbol{\delta J}
\right)^{\mathsf T}
L_G
\left(
D_q^{-1}
\boldsymbol{\delta J}
\right)
\leq
0.
```

連結グラフと $\sum_i\delta J_i=0$ の下で、零減衰状態は $\boldsymbol{\delta J}=0$ だけである。動的振幅、有限温度、記憶尾、有限浴再帰、浴切断後の再成長を含む一様な評価は未完成である。作用交換浴は観測中の相対位相運動も変え得るため、準備後に切り離す。

## 8. 否定的結果と限界

1. 運動量結合が配置速度揺らぎを作っても、その積分が Brown 運動へ収束するとは限らない。
2. $(X,P)$ が Markov でも、$X$ だけの射影は一般に Markov ではない。
3. 二側配置拡散から Fisher 項が得られても、時間対称 Newton 則は自動的に従わない。
4. 配置拡散経路は現行M0に採用せず、位相接続縮約の前提にしない。
5. 閉鎖 Hamiltonian 流は、固定作用のエネルギー最小配置または coherent縮約多様体へ一般に永久吸引しない。有限浴を含む全系も Hamiltonian であり、対象部分の減衰は準備窓でのエネルギー移送である。
6. 有限特異 Hamiltonian の低速枝は、それだけで一般初期値を準備しない。作用交換浴を観測中も作動させると、物理的な相対位相運動を変え得る。
7. rank-one 2次モーメントだけでは、非線形位相接続の標本平均を閉じない。
8. $\kappa=\mathcal J_\phi^2/(2m)$ は内部回転対称性だけから従わない。
9. 密度同期差保存は、同期多様体への復元力ではない。
10. 条件付き循環量子化は Wallstrom 問題の全面解決ではない。
11. 位置入口の Born 型流束は、任意基底の一般 Born 則ではない。
12. 直接作用分配方向が複数なら、入口容量は作用の高いべきになる。
13. 低ランク相関は独立した配置空間場ではない。
14. 低ランク相関と局所回転だけでは Bell 頻度は生じない。
15. 共通未来比較器だけでは、前向き因果として過去の結果頻度を変更できない。
16. Hamiltonian 読み出しと $U(2)$ 混合だけでは殻容量に比例するsector質量を生成しない。
17. Bell 重みには共通境界測度の準備が必要である。
18. 現行局所装置は既存の2値sectorを符号化するが、一般的な唯一結果形成を与えない。
19. 異なる結果履歴を同じ全位相点へ押しつぶす完全resetは Hamiltonian ではない。
20. Bell 余弦則は一般 Born 則、Wallstrom 問題、一般 Tsirelson 原理を単独では解かない。
21. 現Bell模型は平面内の2出力分析設定に限定される。

## 9. 物理的解釈

- 構造化2成分誘導場は、位相接続では保存位相作用と物質微分結合、Born 側では入口作用分配、Bell 側では対モード、戻り伝播、境界比較を担う。
- これらは別の実在場でなく、同じ場の固定直交モード部分空間である。
- Schrödinger 型PDEは、位相接続模型を coherent多様体へ制限した作用変分から生じる。実在的な Markov 経路の導出を必要としない。
- 有限特異 Hamiltonian と準備浴は、coherent多様体全体ではなく、局所作用欠陥と動径欠陥の法線方向だけを部分的に準備する。
- Born 型位置重みは、2モード作用殻の容量と排他的入口面の Liouville 流束から生じる。
- Bell 余弦作用は、反対称低ランク源と左右の局所モード回転から生じる。
- 比較器は相関振幅を共通未来で2モード作用へ転送するが、結果頻度を前向きに作らない。
- Bell 共同頻度は、共通境界密度を持つ二側完全履歴測度から生じる。
- 二側条件付けは終端から過去へ制御力を送る記述ではなく、初期・終端の双方に適合する完結履歴の条件付き集団を表す。
- 時計制御された準備浴は欠陥除去を担い、試行間の弱い外部交換は浴再初期化、再帰抑制、記録安定化、garbage処理を担う。coherent集中、密度同期、共通境界密度を直接生む入力ではない。

これらは R38 の完全周期を証明しない。

## 10. 最重要の未解決問題

1. 2モード比較殻へ容量に比例する共通境界密度を、事後選別なしの実験周期として準備する。
2. 設定生成器、局所結果形成、増幅、記録を同じ周期へ含める。
3. 比較後の結果を外部へ保持しながら、比較、戻り伝播、分析器、場を可逆に再初期化する。
4. 位置入口側の標本化後に、活性場を coherent部分空間へ再埋め込みする。
5. 局所作用整合準備を、動的振幅、有限温度、有限浴再帰、浴切断後の長時間安定性まで含む一様な誤差評価へ進める。
6. coherent集中と密度同期を有限 Hamiltonian 時間発展から準備・維持し、縮約作用の停留点選択を示す。
7. 節を含む位相接続、循環量子化、密度同期を制御する。
8. 反対称対モードを生成する有限 Hamiltonian 源と長時間の位相保持を構成する。
9. 位相、対、比較部分空間の交差応答を長時間一様に小さくする。
10. 平面内2出力を超える一般測定器と複合状態構成へ拡張する。
11. 非対称準備と有限誤差を含む一般的な非信号条件を示す。

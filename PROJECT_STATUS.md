# モデル運用状態、結果の導出状態、理論の境界

この文書は、モデル、導出結果、物理的解釈を分けて管理する。モデルの運用状態、結果の導出状態、付記情報を混同しない。

## 1. モデルの運用状態

| 識別 | モデル | 運用状態 | 役割と限界 |
|---|---|---|---|
| M0 | 有限 Hamiltonian 部分を持つ弱開放系 | 現行モデル | 外部との流入・流出、制御仕事、測定、記録、消去、再初期化を含む。全周期は未完成 |
| M1 | 1試行内の有限閉鎖系 | 補助モデル | 正準写像、保存量、位相体積を厳密に計算する。$\varepsilon_{\rm open}\ll1$ の測定窓だけで用いる |
| M2 | 線形 Gauss 型確率表示 | 補助モデル | 第I部の作用収束定理の対象。M0からの一般導出は未達 |
| M3 | OU 位相表示 | 補助モデル | 弱開放部分系の緩和と回転を具体化する。詳細は `notes/gaussian_nelson_examples.md` |
| M4 | 最小結果符号化器 | 補助モデル | 既存の2値符号を応答モードと固定指針へ写す。一般測定器ではない |
| M5 | 2モード作用分配系と相補時計 | 補助モデル | 一様なしきい値密度と終端比較を明示する |
| M6 | 局所記録後の順時間的共有浴による結果重みづけ | 不採用モデル | 全試行を1回ずつ数える結果領域質量を変えない。詳細は `notes/rejected_forward_weighting_models.md` |
| M7 | 結果依存滞在時間による結果重みづけ | 不採用モデル | 試行頻度ではなく時刻占有率または完了率を変える。詳細は `notes/rejected_forward_weighting_models.md` |
| M8 | 多数の残余作用モードによるしきい値分布 | 補助モデル | 2モード条件の必要性を比較し、非線形累積分布と高次調波を示す |
| M9 | 2つの相補終端半空間の等重み平均 | 不採用モデル | Bell 余弦項を消す。詳細は `notes/complementary_terminal_halfspaces.md` |

M0が現在の主モデルである。ただしM0の全 Hamiltonian、外部結合、仕事貯蔵系、記録・消去・再初期化を統合した1周期は未構成である。M1からM5の厳密計算はM0の一部を切り出した結果であり、そのままM0全体の厳密結果とは呼ばない。M8はM5の2モード条件を限定する比較にだけ用いる。

## 2. 第I部の導出結果

### R1 線形 Gauss 型作用のパラメータ収束

- 導出状態：**厳密結果**
- 性質：**肯定的結果・成立条件**
- 対象モデル：M2

次の範囲で、繰り込み済み粗視化作用形式の収束を証明している。

- 有限 Fourier–Gauss 型駆動。
- 時間依存係数を許す線形流れ。
- Gauss 初期分布。
- 正定値の有限分解能終端記録。
- 2次以下の外部ポテンシャル。
- コンパクトで滑らかな有限次元パラメータ族。
- $h_N\to0$ かつ $N(h_N/T)^2\to\infty$。

この条件で

```math
\left\|
\mathcal A_{N,h}^{R,U}
-
\mathcal A_{\rm GM}^{R,U}
\right\|_{C^1(K)}
\leq
C_K
\left(
\frac hT
+
\frac{T^2}{Nh^2}
\right)
```

が成り立つ。$h_N=TN^{-1/3}$ なら誤差は $O(N^{-1/3})$ である。

$C^1$ は指定した有限次元パラメータ集合 $K$ 上のノルムであり、任意の無限次元経路変分についての主張ではない。

### R2 Guerra--Morato 表示と Nelson 表示の一致

- 導出状態：**厳密結果**
- 性質：**肯定的結果・成立条件**
- 対象モデル：M2

正の Gauss 型密度と、境界項が消える条件の下で、2つの作用表示は厳密に一致する。

### R3 弱開放ミクロモデルから線形表示への縮約

- 導出状態：**予想・未解決**
- 性質：中心課題
- 対象モデル：M0からM2への接続

弱結合、線形化、時間尺度分離、Markov 近似、白色雑音近似を用いる候補経路は示している。しかし、M0の明示 Hamiltonian と外部結合から、M2の一般の時間依存平均・共分散を一様誤差付きで導く定理はない。

### R4 微視的運動による停留点選択

- 導出状態：**予想・未解決**
- 性質：中心課題

微視的 Hamiltonian 運動が粗視化作用の停留点を選ぶこと、任意の Nelson 停留点が有限モデルの停留点列から得られることは未導出である。

## 3. 第II部の導出結果

### R5 比較器と相補時計

- 導出状態：**厳密結果**
- 性質：**肯定的結果**
- 対象モデル：M1、M5

閉鎖比較窓

```math
H_{\rm win}
=
H_{\rm or}
+
\omega_-I_-
+
\omega_\ell(J_s+J_0)
+
\Omega J_c
+
\Omega f_R(\vartheta)
Y_R(h-\kappa I_-)
```

では、

```math
\{I_-,H_{\rm win}\}
=
\{h,H_{\rm win}\}
=
0
```

であり、内部時計の自由運動を含む有限幅パルス後に

```math
\Pi_R(T)
=
E_*+\kappa I_- -h
```

を厳密に得る。終端半空間と順序付き時計向き保存条件も同値である。

### R6 2モード作用分配系の一様周辺

- 導出状態：**厳密結果**
- 性質：**肯定的結果・成立条件**
- 対象モデル：M5

固定総作用殻の正規化 Liouville 測度について、

```math
p(h)
=
\frac1{E_\ell}
\mathbf1_{\{0\leq h\leq E_\ell\}}
```

が成り立つ。固定殻の入口測度を準備する機構と、弱開放時の漏れはこの定理に含まれない。

### R7 差動作用の余弦幾何

- 導出状態：**厳密結果**
- 性質：**肯定的結果**

位相同期した2つの実2次元伝達ベクトルについて、

```math
\overline I_-^{AB}
=
I_0
\left[
1-ABV\cos\Delta_{ab}
\right]
```

を得る。

### R8 `[R]` の下での Bell 型共同確率

- 導出状態：**厳密結果**
- 性質：**肯定的結果・成立条件**
- 付記：**仮説依存**
- 対象モデル：M1、M4、M5

`[H,P,S,M,R]` と

```math
0
\leq
E_*+\kappa I_-
\leq
E_\ell
```

の下で、終端整合体積を積分すると

```math
P_R(A,B\mid a,b)
=
\frac14
\left[
1-ABV_{\rm eff}\cos\Delta_{ab}
\right],
```

```math
V_{\rm eff}
=
\frac{\kappa I_0}{E_*+\kappa I_0}V
```

を得る。この結果の厳密性は `[R]` の成立を証明するものではない。`[R]` を仮定した後の積分と規格化が厳密である。

### R9 実験頻度としての Bell 型共同確率

- 導出状態：**予想・未解決**
- 性質：肯定的主張
- 付記：**仮説依存**
- 依存する仮説：`[R]`、一般測定器、結果非依存の試行完了と再初期化

実験の無条件試行頻度がR8に従うという物理的主張は、M0から未導出の `[R]` に本質的に依存する。

### R10 弱開放補正

- 導出状態：**近似結果**
- 性質：**成立条件・適用限界**
- 対象モデル：M0とM1、M5の接続

次の小さい量を用いる。

```math
\varepsilon_{\rm Bell}
=
\varepsilon_{\rm open}
+
\varepsilon_\ell
+
\varepsilon_{\rm mix}
+
\varepsilon_{\rm pulse}
+
\varepsilon_R.
```

しきい値が作用殻の端点から離れ、誤差が結果と測定設定に一様なら、共同法則の補正を $O(\varepsilon_{\rm Bell})$ と整理する。全係数を持つ一様上界は未完成である。各小さい量を変えた収束試験と、元の有限モデルとの比較が必要である。

結果または設定に相関した漏れとリセット失敗は、規格化で消えず、高次調波、設定頻度の変化、非信号性残差を生み得る。

### R11 対称準備での非信号性

- 導出状態：**厳密結果**
- 性質：**肯定的結果・成立条件**
- 依存する入力：`[S]`、`[R]`

`[S]` の独立符号反転対称性の下で一側周辺は $1/2$ になる。任意の偏った準備については未証明である。

### R12 一般測定器

- 導出状態：**予想・未解決**
- 対象モデル：M0

現在の明示モデルでは

```math
A=\sigma(s_A),
\qquad
B=\sigma(s_B)
```

であり、既存の2値符号を記録するだけである。一般の

```math
A=\mathscr A(a,\lambda_A),
\qquad
B=\mathscr B(b,\lambda_B)
```

を設定、到来変数、装置微視状態の相互作用から形成し、増幅、安定記録、消去、再初期化まで統合する Hamiltonian は未構成である。

### R13 `[R]` の物理的生成

- 導出状態：**予想・未解決**
- 対象モデル：M0

`[R]` は Hamilton 方程式、有限性、再帰、時間反転対称性、相補時計、通常の順時間的リセットだけからは導いていない。相補時計は終端半空間へ意味を与えるが、その半空間だけを物理的試行として数える原理を与えない。

## 4. 否定的結果

### N1 順時間的共有浴

- 導出状態：**厳密結果**
- 性質：**否定的結果**
- 対象モデル：M6

局所記録後の Hamiltonian 共有浴結合は、全試行を1回ずつ数えた結果領域質量を変えない。

詳細は `notes/rejected_forward_weighting_models.md` に置く。

### N2 滞在時間と完了時間

- 導出状態：**厳密結果**
- 性質：**否定的結果**
- 対象モデル：M7

結果依存の滞在時間は試行番号頻度を変えない。時間切れまたは未完了試行の除外は事後選別を生む。

詳細は `notes/rejected_forward_weighting_models.md` に置く。

### N3 多数の残余作用モード

- 導出状態：**厳密結果**
- 性質：**否定的結果**
- 対象モデル：M8

```math
F_N(x)
=
1-
\left(
1-\frac{x}{E_\ell}
\right)^N
```

は $N>1$ で非線形であり、高次調波を生む。

### N4 相補半空間の等重み平均

- 導出状態：**厳密結果**
- 性質：**否定的結果**
- 対象モデル：M9

同じ読出しに対する2つの相補的終端半空間を等重みで平均すると、余弦項は消える。

詳細は `notes/complementary_terminal_halfspaces.md` に置く。

### N5 Wallstrom 問題

- 導出状態：**厳密結果**
- 性質：**否定的結果**

Bell 型余弦則は配置空間位相の循環量子化を含意しない。

## 5. 物理的解釈

次は結果そのものではなく、結果に基づく解釈である。

- 相補時計の反対向きは、粒子が実験室時刻を逆向きに進むことではなく、内部時計または境界情報の向きが相補的であることを表す。
- 終端条件による初期事後分布の変化は、終端装置から過去への力を意味せず、完結履歴の条件付き測度を表す。
- Bell の前提違反は、`[R]` 後の完全微視状態分布が測定設定に依存することに位置する。
- OU の摩擦は弱開放部分系の有効緩和を表す候補であり、有限 Hamiltonian 部分の基本法則が非可逆であることを意味しない。

これらの解釈は `[R]` の物理的生成を証明しない。

## 6. 最重要の未解決問題

1. 弱開放な準備、測定、記録、消去、再初期化を1周期の明示 Hamiltonian と外部結合で構成し、エネルギー収支を閉じる。
2. その試行周期またはより大きな境界値理論から `[R]` を導く。
3. 設定、到来変数、装置微視状態から結果を形成する一般測定相互作用を構成する。
4. M0からM2への縮約を導き、弱結合、線形化、Markov、白色雑音の誤差を評価する。
5. 微視的時間発展による Nelson 停留点選択を示す。
6. 有限混合器の混合時間、再帰時間、総作用漏れを定量化する。
7. 偏った準備装置まで含めても非信号性が回復するか判定する。
8. 2次比較器と Tsirelson 限界を追加原理から選ぶ。
9. Nelson 位相と生成源の作用角位相を結び、Wallstrom 量子化へ進む。

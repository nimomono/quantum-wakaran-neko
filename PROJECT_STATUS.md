# モデル運用状態、結果の導出状態、理論の境界

この文書は、モデル、導出結果、物理的解釈を分けて管理する。モデルの運用状態、結果の導出状態、付記情報を混同しない。

## 1. モデルの運用状態

| 識別 | モデル | 運用状態 | 役割と限界 |
|---|---|---|---|
| M0 | 粒子・装置・構造化誘導場・外部流路を持つ弱開放系 | 現行モデル | Fisher 側と Bell 側の共通ミクロ構成。全周期と一様縮約定理は未完成 |
| M1 | 1試行内の有限閉鎖系 | 補助モデル | 正準写像、Liouville モーメント、保存量、作用殻体積を厳密に計算する |
| M2 | 線形誘導場 | 補助モデル | 指定した初期条件または二側境界条件の下で Green 作用素により消去する |
| M3 | 二側 Markov 拡散 | 補助モデル | 前進・後退流れ、時間対称平均加速度、Fisher 応力を整理する。M0からの導出は未達 |
| M4 | 線形 Fourier–Gauss 型確率表示 | 補助モデル | 作用形式の $C^1$ 収束を検証する。M0の中心モデルではない |
| M5 | Gauss 幅の Routh 縮約 | 補助モデル | 固定内部作用と Fisher 項の一致を示す可解例 |
| M6 | 最小結果符号化器 | 補助モデル | 既存の2値符号を伝達ベクトルと固定指針へ写す。一般測定器ではない |
| M7 | 3モード境界作用殻 | 補助モデル | 共通作用殻、和モード周辺、残余ファイバー体積を厳密に計算する |
| M8 | U(3) 等方殻拡散 | 補助モデル | 共通殻 Liouville 測度を一意な定常分布として与える。M0からのミクロ導出は未達 |
| M9 | 残余2モードの U(2) 混合 | 補助モデル | ファイバー内部の一様作用分配を示し、全殻測度には不足することを比較する |
| M10 | OU 位相表示 | 補助モデル | 弱開放部分系の緩和と回転の例。詳細は `notes/gaussian_nelson_examples.md` |
| M11 | 終端関数、中央比較器、相補時計による Bell 重み | 不採用モデル | Bell 固有の終端統計入力を必要とする。`notes/superseded_terminal_function_model.md` |
| M12 | 順時間的共有浴、結果依存滞在時間、相補終端半空間 | 不採用モデル | 試行頻度を得ない、事後選別になる、または余弦項を消す。詳細は `notes/` |

M0が現在の主モデルである。M0の固定射影、外部流路、Fisher 縮約、局所測定器、境界殻準備、境界適合、消去、再初期化を1本の有限幅 Hamiltonian へ統合した完成モデルはない。

M1からM9の厳密結果は、それぞれの補助モデルまたは縮約方程式の内部での結果である。M0から各補助モデルへの接続が未完成なら、M0全体から導出済みとは呼ばない。

## 2. 共通ミクロ構成の導出結果

### R1 固定した明・暗モード基底

- 導出状態：**厳密結果**
- 対象モデル：M0、M1、M2

直接結合行列 $B$ の像を含む明部分空間と、その直交補空間である暗部分空間を固定できる。直交行列 $O$ を座標と運動量へ同時に作用させる変換は正準である。

暗モードを直接駆動しない条件は

```math
P_{\rm D}B=0
```

である。$P_{\rm D}K_NP_{\rm B}$ による間接伝播は一般に残る。

### R2 固定位相整合射影

- 導出状態：**厳密結果**
- 性質：成立条件
- 対象モデル：M0、M1、M2

位相整合射影 $P_{\rm c}$ と欠陥射影 $P_\perp$ は、$K_N$ の固定スペクトル帯、$B$ が生成する Krylov 部分空間、保存作用、装置構造から事前に定める。

得られた密度または目標量子状態を見て射影を選ぶ構成は採用しない。射影の正準性は、欠陥成分だけの減衰を意味しない。

### R3 選択的な欠陥減衰

- 導出状態：**予想・未解決**
- 性質：中心課題
- 対象モデル：M0

外部流路を $P_\perp$ へ強く、$P_{\rm c}$ へ弱く結合し、欠陥成分だけを速く除去する候補を示す。指数減衰率、流入補正、位相整合成分の寿命を、特定の外部スペクトルから一様に導く定理はない。

## 3. Fisher 側の導出結果

### R4 Liouville 0次・1次モーメント

- 導出状態：**厳密結果**
- 対象モデル：M0、M1

外部自由度まで含む全 Liouville 密度から、

```math
\partial_t\rho_N
+
\nabla\cdot(\rho_Nv_N)
=0,
```

```math
m\rho_N
\left(
\partial_t+v_N\cdot\nabla
\right)v_N
=
-\rho_N\nabla V
+
\rho_N\overline F_{{\rm G},N}
-
\frac1m
\nabla\cdot
\left(
\rho_N\Sigma_{p,N}
\right)
```

を得る。閉鎖近似は使わない。

### R5 条件付き全分散分解

- 導出状態：**厳密結果**
- 対象モデル：M0、M1

枝指標 $\alpha$ を用い、運動量共分散を枝内部幅と枝中心間流束へ正確に分ける。場の射影 $P_{\rm c},P_\perp$ と、この運動量分散分解は自動的には一致しない。

### R6 線形誘導場の Green 消去

- 導出状態：**厳密結果**
- 性質：指定境界条件の下で成立
- 対象モデル：M2

線形誘導場は、初期値問題または一意可解な二側境界値問題の下で Green 作用素により消去できる。自己共役境界条件では Green 核が時間交換対称になる。

自己共役性だけでは Markov 性、共通拡散係数、Nelson 平均加速度は導けない。

### R7 二側拡散の浸透速度

- 導出状態：**厳密結果**
- 性質：仮説依存
- 対象モデル：M3

共通の正の密度と共通の拡散係数 $\nu$ を持つ前進・後退 Markov 拡散では、

```math
u
=
\frac{b_+-b_-}{2}
=
\nu\nabla\log\rho
```

が厳密に成立する。

### R8 Fisher 応力恒等式

- 導出状態：**厳密結果**
- 対象モデル：M3

```math
P_F[\rho]
=
-m\nu^2\rho\,\nabla\nabla\log\rho
```

と定めれば、

```math
-\nabla\cdot P_F[\rho]
=
2m\nu^2\rho\,
\nabla
\left(
\frac{\Delta\sqrt\rho}{\sqrt\rho}
\right)
```

が成立する。

### R9 ミクロ誘導場から二側 Markov 拡散への縮約

- 導出状態：**予想・未解決**
- 性質：中心課題
- 対象モデル：M0からM3への接続

短記憶化、条件付き均質化、前進・後退の共通拡散係数、非 Markov 残差、境界層、有限再帰の一様評価は未完成である。

### R10 Fisher 閉鎖

- 導出状態：**予想・未解決**
- 性質：最重要の中心課題
- 対象モデル：M0からM3への接続

目標は

```math
\rho_N\overline F_{{\rm G},N}
-
\frac1m
\nabla\cdot
\left(
\rho_N\Sigma_{p,N}
\right)
\longrightarrow
-\nabla\cdot P_F[\rho]
```

を、次元の揃った時空間ノルムと一様誤差評価で証明することである。

### R11 Gauss 幅の Routh–Fisher 一致

- 導出状態：**厳密結果**
- 性質：成立条件・補助検証
- 対象モデル：M5

1次元 Gauss 密度では $\mathcal I[\rho_\sigma]=1/\sigma^2$ である。2次元内部座標の循環作用を $J=m\nu$ に固定して Routh 縮約すると、

```math
-\frac{J^2}{2m\sigma^2}
=
-\frac{m\nu^2}{2}
\mathcal I[\rho_\sigma]
```

となる。一般密度の Fisher 閉鎖は含まない。

### R12 線形 Gauss 型作用の $C^1$ 収束

- 導出状態：**厳密結果**
- 性質：成立条件・補助検証
- 対象モデル：M4

有限 Fourier–Gauss 型駆動、線形流れ、Gauss 初期分布、正定値の有限分解能終端記録、2次ポテンシャル、滑らかな有限次元パラメータ族について、

```math
\left\|
\mathcal A_{N,h}^{R,U}
-
\mathcal A_{\rm GM}^{R,U}
\right\|_{C^1(K)}
\leq
C_K
\left(
\frac hT
+
\frac{T^2}{Nh^2}
\right)
```

が成立する。Guerra--Morato 表示と Nelson 表示は正の密度領域で厳密に一致する。

M0からM4を導くこと、微視的運動が作用停留点を選ぶこと、R10を証明することは含まない。

## 4. Bell 側の導出結果

### R13 局所セクターの近似分離

- 導出状態：**近似結果**
- 性質：成立条件・適用限界
- 対象モデル：M0、M1

測定窓の左右交差応答比 $\varepsilon_{\rm loc}\ll1$ なら、左右局所記録を $O(\varepsilon_{\rm loc})$ の範囲で分離して扱う。結合ベクトルの直交性だけでは十分でない。

### R14 和・差作用の余弦幾何

- 導出状態：**厳密結果**
- 対象モデル：M1、M6、M7

位相同期した実2次元伝達ベクトルについて、

```math
I_\pm^{AB}
=
I_0
\left[
1
\pm
ABV\cos\Delta_{ab}
\right],
\qquad
I_+^{AB}+I_-^{AB}=2I_0
```

を得る。

### R15 U(2) 等方性の不足

- 導出状態：**厳密結果**
- 性質：否定的結果
- 対象モデル：M9

残余2モードの $U(2)$ 不変性は、固定 $J_+$ のファイバー内部を一様化するだけで、異なる $J_+$ のファイバー間質量を決めない。

### R16 U(3) 共通殻測度と等方拡散

- 導出状態：**厳密結果**
- 性質：仮説依存
- 対象モデル：M7、M8

$U(3)$ 不変な固定作用殻測度は一意である。縮約方程式

```math
\partial_tf
=
D_\partial\Delta_{S^5}f
```

の一意な定常確率密度は共通殻上の定数密度である。

M0からこの生成子を導くことは未完成である。常時の弱漏れは殻方向の等方化ではなく、欠陥除去、再帰抑制、半径分布の維持を担う。

### R17 共通殻の周辺と残余ファイバー

- 導出状態：**厳密結果**
- 性質：成立条件
- 対象モデル：M7

共通作用殻 $J_++J_s+J_r=C_0$ の正規化 Liouville 測度について、

```math
p_+(x)
=
\frac{2(C_0-x)}{C_0^2}
```

を得る。共通 Jacobian で $J_+=I_+^{AB}$ を課した残余ファイバー体積は

```math
W_{AB}
\propto
C_0-I_+^{AB}
=
J_*+I_-^{AB}
```

となる。

### R18 Bell 型共同確率

- 導出状態：**厳密結果**
- 性質：仮説依存・成立条件
- 対象モデル：M6、M7、M8

共通作用殻、等しい結果セクター基準質量、共通の境界分解能と coarea Jacobian、固定総入力作用の下で、

```math
P(A,B\mid a,b)
=
\frac14
\left[
1
-
V_{\rm eff}AB\cos\Delta_{ab}
\right],
\qquad
V_{\rm eff}
=
\frac{I_0}{J_*+I_0}V
```

を得る。

### R19 履歴測度と Bell 前提監査

- 導出状態：**厳密結果**
- 性質：Bell 前提監査
- 対象モデル：M7

履歴測度は、全境界正準位相空間の Liouville 測度を作用保存と境界適合条件で制限し、解写像で許容履歴空間へ押し出す。解空間へ直接 Liouville 測度を置くものではない。

境界適合条件が $a,b$ に依存するため、完全履歴分布は一般に測定設定独立性を満たさない。対称セクターでは一側周辺は $1/2$ である。偏った準備での一般的な非信号性は未証明である。

### R20 一般測定器と試行周期

- 導出状態：**予想・未解決**
- 性質：中心課題
- 対象モデル：M0

現在の明示装置は既存の2値符号を記録する最小結果符号化器である。結果形成、増幅、安定記録、境界適合、消去、再初期化を、開始数と記録数を減らさない1周期として統合していない。

## 5. 否定的結果と限界

1. 自己共役 Green 核だけでは Nelson の時間対称平均加速度を導けない。
2. 選択的な弱漏れだけでは Fisher 応力を導けない。
3. 場の固定射影と粒子運動量分散の分解は自動的に一致しない。
4. 残余2モードの U(2) 等方性だけでは、異なる $J_+$ のファイバー間質量を決められない。
5. 固定された正準接合部は Liouville 測度を保存するが、単一状態から一様殻測度を生成しない。
6. 純粋な一方向漏れは $C_0>0$ の一様作用殻を準備しない。
7. 有限閉鎖 Hamiltonian 流れは微細 Liouville 密度を一般に強収束させない。
8. 左右局所セクターを同じ誘導場へ置くだけでは、共通未来の境界条件を除けない。
9. 局所記録後の順時間的共有浴結合と結果依存滞在時間は、全試行を1回ずつ数えた結果頻度を変えない。
10. Bell 型余弦則は Born 則、Tsirelson 限界、Wallstrom 量子化を単独では導かない。

## 6. 物理的解釈

- 構造化誘導場は、第I部では粒子の条件付き反作用と運動量流束の源、第II部では局所反応座標と境界殻混合の源として使う。
- 常時の弱い漏れは、欠陥除去、再帰抑制、記録安定化を担う。Fisher 応力または殻方向等方性を直接生む入力ではない。
- 二側条件付けは終端から過去へ力を送る記述ではなく、初期・終端の双方に適合する完結履歴の条件付き集団を表す。
- Fisher 応力は、誘導場反作用だけでなく、粒子運動量共分散の発散と合わせて閉じるべき対象である。
- Bell 重みは結果依存終端関数でなく、同じ共通作用殻を境界適合条件で切った残余ファイバー体積から生じる。

これらは R9、R10、R16 のミクロ導出や R20 の試行周期を証明しない。

## 7. 最重要の未解決問題

1. 構造化誘導場から二側 Markov 拡散を導き、前後の拡散係数と非 Markov 残差を評価する。
2. 誘導場反作用と運動量流束の Fisher 閉鎖を、一様な時空間誤差評価として証明する。
3. 固定位相整合射影を保ったまま、欠陥成分だけの減衰と再帰抑制を外部スペクトルから導く。
4. 同じ誘導場から U(3) 等方な殻接方向生成子を導き、異方誤差、混合時間、再帰時間を定量化する。
5. 常時の弱い漏れと流入から、狭い作用殻の半径分布とエネルギー収支を導く。
6. 設定、到来変数、装置微視状態から結果を形成する一般測定相互作用を構成する。
7. 境界適合、測定、記録、消去、再初期化を1周期へ統合し、事後選別がないことを示す。
8. 偏った準備装置まで含めても非信号性が回復する条件を判定する。
9. 微視的時間発展による Nelson 停留点選択を示す。
10. Born 則、Tsirelson 限界、Wallstrom 量子化へ進む追加構造を特定する。

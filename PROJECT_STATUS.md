# モデル運用状態、結果の導出状態、理論の境界

この文書は、モデル、導出結果、物理的解釈を分けて管理する。モデルの運用状態、結果の導出状態、付記情報を混同しない。

## 1. モデルの運用状態

| 識別 | モデル | 運用状態 | 役割と限界 |
|---|---|---|---|
| M0 | 1つの構造化有限浴を持つ弱開放系 | 現行モデル | 局所反応座標、静的な明・暗モード分解、共通境界3モード、常時の微小な流入・流出を含む。全周期は未完成 |
| M1 | 1試行内の有限閉鎖系 | 補助モデル | 正準写像、保存量、位相体積を厳密に計算する。$\varepsilon_{\rm open}\ll1$ の短い窓だけで用いる |
| M2 | 線形 Gauss 型確率表示 | 補助モデル | 第I部の作用収束定理の対象。M0からの一般導出は未達 |
| M3 | OU 位相表示 | 補助モデル | 弱開放部分系の緩和と回転を具体化する。詳細は `notes/gaussian_nelson_examples.md` |
| M4 | 最小結果符号化器 | 補助モデル | 既存の2値符号を伝達ベクトルと固定指針へ写す。一般測定器ではない |
| M5 | 3モード境界作用殻 | 補助モデル | 共通作用殻、和モード周辺、残余ファイバー体積を厳密に計算する |
| M6 | U(3) 等方殻拡散 | 補助モデル | 共通殻 Liouville 測度を一意な定常分布として与える。M0からのミクロ導出は未達 |
| M7 | 残余2モードの U(2) 混合 | 補助モデル | ファイバー内部の一様作用分配を示し、全殻測度には不足することを比較する |
| M8 | 終端関数、中央比較器、相補時計による Bell 重み | 不採用モデル | Bell 固有の終端統計入力を必要とするため、共通作用殻モデルで置き換えた。整理は `notes/superseded_terminal_function_model.md` |
| M9 | 局所記録後の順時間的共有浴による結果重みづけ | 不採用モデル | 全試行を1回ずつ数える結果領域質量を変えない。詳細は `notes/rejected_forward_weighting_models.md` |
| M10 | 結果依存滞在時間による結果重みづけ | 不採用モデル | 試行頻度ではなく時刻占有率または完了率を変える。詳細は `notes/rejected_forward_weighting_models.md` |
| M11 | 2つの相補終端半空間の等重み平均 | 不採用モデル | Bell 余弦項を消す。詳細は `notes/complementary_terminal_halfspaces.md` |

M0が現在の主モデルである。ただし、M0の全 Hamiltonian、外部結合、仕事貯蔵系、全殻拡散、境界適合、記録・消去・再初期化を統合した1周期は未構成である。

M1、M4、M5の厳密計算はM0の一部を切り出した結果であり、そのままM0全体の厳密結果とは呼ばない。M6の定常測度定理は縮約拡散方程式の内部で厳密だが、M0からM6への導出は未完成である。

## 2. 第I部の導出結果

### R1 線形 Gauss 型作用のパラメータ収束

- 導出状態：**厳密結果**
- 性質：**肯定的結果・成立条件**
- 対象モデル：M2

次の範囲で、繰り込み済み粗視化作用形式の収束を証明している。

- 有限 Fourier–Gauss 型駆動。
- 時間依存係数を許す線形流れ。
- Gauss 初期分布。
- 正定値の有限分解能終端記録。
- 2次以下の外部ポテンシャル。
- コンパクトで滑らかな有限次元パラメータ族。
- $h_N\to0$ かつ $N(h_N/T)^2\to\infty$。

この条件で

```math
\left\|
\mathcal A_{N,h}^{R,U}
-
\mathcal A_{\rm GM}^{R,U}
\right\|_{C^1(K)}
\leq
C_K
\left(
\frac hT
+
\frac{T^2}{Nh^2}
\right)
```

が成り立つ。$h_N=TN^{-1/3}$ なら誤差は $O(N^{-1/3})$ である。

$C^1$ は指定した有限次元パラメータ集合 $K$ 上のノルムであり、任意の無限次元経路変分についての主張ではない。

### R2 Guerra--Morato 表示と Nelson 表示の一致

- 導出状態：**厳密結果**
- 性質：**肯定的結果・成立条件**
- 対象モデル：M2

正の Gauss 型密度と、境界項が消える条件の下で、2つの作用表示は厳密に一致する。

### R3 弱開放ミクロモデルから線形表示への縮約

- 導出状態：**予想・未解決**
- 性質：中心課題
- 対象モデル：M0からM2への接続

弱結合、線形化、時間尺度分離、Markov 近似、白色雑音近似を用いる候補経路は示している。しかし、M0の明示 Hamiltonian と外部結合から、M2の一般の時間依存平均・共分散を一様誤差付きで導く定理はない。

### R4 微視的運動による停留点選択

- 導出状態：**予想・未解決**
- 性質：中心課題

微視的 Hamiltonian 運動が粗視化作用の停留点を選ぶこと、任意の Nelson 停留点が有限モデルの停留点列から得られることは未導出である。

## 3. 第II部の導出結果

### R5 構造化有限浴の静的基底

- 導出状態：**厳密結果**
- 性質：**肯定的結果・成立条件**
- 対象モデル：M0、M1

浴の結合ベクトルが張る明部分空間と、その直交補空間である暗部分空間を固定できる。直交行列 $O$ を浴の座標と運動量へ同時に作用させる変換

```math
\widetilde Q=OQ,
\qquad
\widetilde P=OP
```

は正準である。

ただし、変換後の浴 Hamiltonian が局所A、局所B、共通境界、暗モードの厳密な直和になるとは限らない。局所分離の動力学的条件は、交差応答 $\varepsilon_{\rm loc}\ll1$ として別に置く。

### R6 局所セクターの近似分離

- 導出状態：**近似結果**
- 性質：**成立条件・適用限界**
- 対象モデル：M0とM1の接続

測定窓 $0\leq t\leq T_{\rm meas}$ で、

```math
\varepsilon_{\rm loc}
=
\frac{
\sup_t
\max
\left(
|\chi_{AB}(t)|,
|\chi_{BA}(t)|
\right)
}{
\sup_t
\min
\left(
|\chi_{AA}(t)|,
|\chi_{BB}(t)|
\right)
}
\ll1
```

なら、左右の局所記録を $O(\varepsilon_{\rm loc})$ の範囲で分離して扱う。非線形浴と外部結合を含む一様上界は未完成である。

### R7 和・差作用の余弦幾何

- 導出状態：**厳密結果**
- 性質：**肯定的結果**
- 対象モデル：M1、M4、M5

位相同期した2つの実2次元伝達ベクトルについて、

```math
I_\pm^{AB}
=
I_0
\left[
1
\pm
ABV\cos\Delta_{ab}
\right],
```

```math
I_+^{AB}+I_-^{AB}=2I_0
```

を得る。有限の位相雑音は、設定・結果と独立で重みが作用に線形なら、可視度 $V$ にまとめられる。

### R8 U(2) 等方性の不足

- 導出状態：**厳密結果**
- 性質：**否定的結果**
- 対象モデル：M7

残余2モードの $U(2)$ 不変性は、固定 $J_+$ のファイバー内部を一様化するだけである。任意の非負関数 $f$ に対し、

```math
d\mu_f
\propto
f(J_+)
\delta
\left(
C_0-J_+-J_s-J_r
\right)
d\Gamma
```

は $U(2)$ 不変だが、ファイバー質量は

```math
W_f(x)
\propto
f(x)(C_0-x)
```

となる。従って U(2) 等方性だけでは Bell 重みを決められない。

### R9 U(3) 不変な共通殻測度

- 導出状態：**厳密結果**
- 性質：**肯定的結果・成立条件**
- 付記：**仮説依存**
- 対象モデル：M5、M6

$U(3)$ は3モード固定作用殻

```math
S_{C_0}^5
=
\left\{
a\in\mathbb C^3:
a^\dagger a=C_0
\right\}
```

上で推移的に作用する。従って正規化された $U(3)$ 不変測度は一意で、固定殻 Liouville 測度に等しい。

### R10 等方殻拡散の定常測度

- 導出状態：**厳密結果**
- 性質：**肯定的結果・成立条件**
- 付記：**仮説依存**
- 対象モデル：M6

縮約方程式

```math
\partial_t f
=
D_\partial\Delta_{S^5}f,
\qquad
D_\partial>0
```

の一意な定常確率密度は共通殻上の定数密度である。$L^2$ 初期密度はスペクトルギャップにより指数的に定常密度へ収束する。

この結果の厳密性は、M0からこの拡散方程式を導いたことを意味しない。

### R11 有限浴から全殻拡散への縮約

- 導出状態：**予想・未解決**
- 性質：中心課題
- 対象モデル：M0からM6への接続

$u(3)$ の Hamiltonian 生成子を、等方な相関行列を持つ浴変数へ弱く結合する候補を示している。弱結合・短相関時間極限で Casimir 生成子を得ることが期待される。

特定の有限非線形浴と外部結合について、非退化性、等方性、混合時間、再帰時間、異方誤差を一様に評価する定理はない。

### R12 3モード共通殻の周辺と残余ファイバー

- 導出状態：**厳密結果**
- 性質：**肯定的結果・成立条件**
- 対象モデル：M5

共通作用殻

```math
J_++J_s+J_r=C_0
```

の正規化 Liouville 測度について、

```math
p_+(x)
=
\frac{
2(C_0-x)
}{
C_0^2
},
\qquad
0\leq x\leq C_0
```

を得る。共通 Jacobian で $J_+=I_+^{AB}$ を課した残余ファイバー体積は、

```math
W_{AB}
\propto
C_0-I_+^{AB}
=
J_*+I_-^{AB}
```

となる。

### R13 共通作用殻の下での Bell 型共同確率

- 導出状態：**厳密結果**
- 性質：**肯定的結果・成立条件**
- 付記：**仮説依存**
- 対象モデル：M4、M5、M6

共通作用殻、等しい結果セクター基準質量、共通の境界分解能と coarea Jacobian、固定総入力作用の下で、

```math
P(A,B\mid a,b)
=
\frac14
\left[
1
-
V_{\rm eff}
AB\cos\Delta_{ab}
\right],
```

```math
V_{\rm eff}
=
\frac{
I_0
}{
J_*+I_0
}V
```

を得る。$J_*=0$ かつ $V=1$ なら完全な余弦共同確率、CHSH 超過には $V_{\rm eff}>1/\sqrt2$ が必要である。

この結果は、M6の定常測度または同じ共通殻測度を準備できることに仮説依存する。

### R14 対称準備での非信号性

- 導出状態：**厳密結果**
- 性質：**肯定的結果・成立条件**
- 付記：**仮説依存**

4つの結果セクターの基準質量、境界分解能、Jacobian が左右符号反転対称なら、一側周辺は $1/2$ となる。任意の偏った準備については未証明である。

### R15 測定設定独立性の破れ

- 導出状態：**厳密結果**
- 性質：**Bell 前提監査**
- 対象モデル：M5

境界適合条件

```math
J_+=I_+^{AB}(a,b)
```

により許容履歴ファイバーが設定に依存するため、完全履歴分布は一般に

```math
\rho_{\rm hist}(\Lambda\mid a,b)
\neq
\rho_{\rm hist}(\Lambda)
```

となる。Bell の前提違反は測定設定独立性にある。

### R16 弱開放補正

- 導出状態：**近似結果**
- 性質：**成立条件・適用限界**
- 対象モデル：M0とM1、M5、M6の接続

主要誤差を

```math
\varepsilon_{\rm Bell}
=
\varepsilon_{\rm loc}
+
\varepsilon_{\rm pulse}
+
\varepsilon_{\rm aniso}
+
\varepsilon_{\rm mix}
+
\varepsilon_C
+
\varepsilon_{\rm res}
+
\varepsilon_J
+
\varepsilon_S
```

と整理する。境界ファイバーが殻端から離れ、誤差が結果と設定に一様なら、共同法則の補正を $O(\varepsilon_{\rm Bell})$ とする。全係数を持つ一様上界は未完成である。

結果または設定に相関した殻幅、漏れ、境界分解能、Jacobian、完了率は、規格化で消えず、高次調波、設定頻度の変化、非信号性残差を生み得る。

### R17 一般測定器

- 導出状態：**予想・未解決**
- 対象モデル：M0

現在の明示モデルでは、既存の結果種を

```math
A=\sigma(s_A),
\qquad
B=\sigma(s_B)
```

として伝達ベクトルと指針へ写すだけである。一般の

```math
A=\mathscr A(a,\lambda_A),
\qquad
B=\mathscr B(b,\lambda_B)
```

を設定、到来変数、装置微視状態の相互作用から形成し、増幅、安定記録、消去、再初期化まで統合する Hamiltonian は未構成である。

### R18 境界適合を含む試行周期

- 導出状態：**予想・未解決**
- 対象モデル：M0

共通殻準備、局所測定、境界適合、記録、消去、再初期化を、外部開始数と記録数を減らさない1周期として構成していない。数学上の2境界解集合を、事後選別なしの実験頻度として実現することは未解決である。

## 4. 否定的結果

### N1 U(2) 混合

- 導出状態：**厳密結果**
- 性質：**否定的結果**
- 対象モデル：M7

残余2モードの U(2) 等方性だけでは、異なる $J_+$ のファイバー間質量を決められない。

### N2 固定された損失のない接合部

- 導出状態：**厳密結果**
- 性質：**否定的結果**

固定された正準接合部は Liouville 測度を保存するが、単一の初期状態から一様殻測度を生成しない。

### N3 純粋な一方向漏れ

- 導出状態：**厳密結果**
- 性質：**否定的結果**

```math
\dot C=-\gamma_C C
```

は $C=0$ へ緩和するだけで、$C_0>0$ の一様作用殻を準備しない。

### N4 有限閉鎖 Hamiltonian 流れの強収束

- 導出状態：**厳密結果**
- 性質：**否定的結果・適用限界**

有限閉鎖 Hamiltonian 流れは微細 Liouville 密度を保存するため、一般の初期密度が $L^1$ または各点で一様密度へ収束するとは言えない。有限浴では有限時間・有限分解能の混合として扱う。

### N5 共有浴と共通未来

- 導出状態：**厳密結果**
- 性質：**否定的結果**

左右局所セクターを同じ浴へ置くだけでは、共通未来の境界条件を除けない。除くには、設定依存の共通過去分布、局所窓の大きな交差応答、または非局所相互作用へ相関源を移す必要がある。

### N6 順時間的結果重みづけ

- 導出状態：**厳密結果**
- 性質：**否定的結果**
- 対象モデル：M9、M10

局所記録後の Hamiltonian 共有浴結合は、全試行を1回ずつ数えた結果領域質量を変えない。結果依存の滞在時間は試行番号頻度を変えず、完了失敗による除外は事後選別になる。

### N7 Wallstrom 問題

- 導出状態：**厳密結果**
- 性質：**否定的結果**

Bell 型余弦則は配置空間位相の循環量子化を含意しない。

## 5. 物理的解釈

次は結果そのものではなく、結果に基づく解釈である。

- 静的な明・暗モード分解は、同じ有限浴内の結合方向を整理する正準基底であり、複数の独立浴を仮定することではない。
- 局所反応座標の近似分離は、基底の名称ではなく有限時間交差応答 $\varepsilon_{\rm loc}$ の小ささを意味する。
- 共通作用殻上の等方拡散は、未制御な境界方向の準備を粗視化して表す。有効確率性は基礎 Hamiltonian 方程式へ乱数を直接加えたものではない。
- 境界適合による設定依存履歴分布は、共通未来から過去への力を意味せず、完結履歴の条件付き測度を表す。
- Bell 重みは結果依存終端関数ではなく、同じ共通作用殻を切った残余ファイバー体積から生じる。
- 常時の弱い漏れは殻方向の等方化とは別の役割を持つ。漏れは再帰抑制と半径安定化に関わり、等方化には接方向の混合が必要である。
- OU の摩擦は弱開放部分系の有効緩和を表す候補であり、有限 Hamiltonian 部分の基本法則が非可逆であることを意味しない。

これらの解釈は、M0からM6へのミクロ導出や試行周期の完成を証明しない。

## 6. 最重要の未解決問題

1. 1つの構造化有限浴から、左右局所反応座標と共通境界3モードを同時に導き、交差応答を評価する。
2. その浴の相関関数から U(3) 等方な殻接方向生成子を導き、異方誤差、混合時間、再帰時間を定量化する。
3. 常時の弱い漏れと流入から、狭い作用殻の半径分布とエネルギー収支を導く。
4. 境界適合、設定、測定、記録、消去、再初期化を1周期の明示 Hamiltonian と外部結合で構成する。
5. 設定、到来変数、装置微視状態から結果を形成する一般測定相互作用を構成する。
6. M0からM2への縮約を導き、弱結合、線形化、Markov、白色雑音の誤差を評価する。
7. 微視的時間発展による Nelson 停留点選択を示す。
8. 偏った準備装置まで含めても非信号性が回復するか判定する。
9. 余弦構造と Tsirelson 限界を追加原理から選ぶ。
10. Nelson 位相と生成源の作用角位相を結び、Born 則と Wallstrom 量子化へ進む。

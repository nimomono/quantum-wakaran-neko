# 固定長期目標、モデル運用状態、結果の導出状態、理論の境界

この文書は、固定長期目標とその現在地に加え、モデル、導出結果、物理的解釈を分けて管理する。固定目標と可変の進捗、モデルの運用状態、結果の導出状態、付記情報を混同しない。

## 固定長期目標

以下の目標ID、名称、意味、達成判定基準は、通常の論文更新では変更しない。個々の論文や現行モデルが変わっても一覧から削除せず、追加、削除、統合、分割または判定基準の変更が必要な場合は、独立した方針変更として扱う。

### A. 理論目標

| ID | 固定目標 | 達成判定の中心 |
|---|---|---|
| Q1 | Schrödinger 型有効力学 | 明示的な古典ミクロモデルから、適用範囲と誤差を伴う有効時間発展として導く |
| Q2 | Born 則 | 確率重みを外から置かず、準備、力学、測定過程から導く |
| Q3 | 位相量子化 | 循環量子化、節、単価性を含めて Wallstrom 問題へ回答する |
| Q4 | 重ね合わせと干渉 | 位相差に依存する干渉項を明示的なミクロモデルから導く |
| Q5 | 複合系の状態構成 | テンソル積に相当する状態構造と局所測定を古典自由度から構成する |
| Q6 | 束縛状態 | 井戸型・調和型ポテンシャルで固有エネルギー、確率密度、節、安定性を再現する |
| Q7 | トンネル効果 | 障壁高、幅、入射エネルギーに対する透過率を再現し、古典的熱活性化と区別する |
| Q8 | Rabi 振動 | 駆動2準位系の振動数、振幅、離調依存性を再現する |
| Q9 | 2重スリット干渉 | 準備から検出までの明示的なモデルで位置分布を再現する |
| Q10 | Bell 型測定統計 | 余弦共同確率、CHSH不等式の破れ、Tsirelson 限界、非信号性、Bell の前提違反を一貫して示す |
| Q11 | 完全な測定周期 | 準備、設定生成、結果形成、増幅、記録、消去、再初期化を1周期として構成する |
| Q12 | 一般性と頑健性 | 特定モデルの偶然ではなく、一定範囲の古典系に共通する機構として示す |

### B. 工学的実証目標

工学的実証では、論文で導出した縮約機構を実際に利用する。古典的な振幅ベクトルへ量子ゲートの行列を作用させるだけの既知のアナログ模擬とは区別する。

| ID | 固定目標 | 達成判定の中心 |
|---|---|---|
| D1 | 古典自由度への状態符号化 | 量子コヒーレンスを前提としない巨視的な古典変数だけで状態と位相を操作できる |
| D2 | 単一量子ビット型ゲート | 反転、位相、Hadamard 型操作を同じ装置原理で実現する |
| D3 | 2量子ビット型操作 | 複合状態とCNOT または制御位相型操作を実現する |
| D4 | 完全な装置周期 | 準備、ゲート列、測定、消去、再初期化を連続動作させる |
| D5 | 性能と資源増加の評価 | 忠実度、雑音耐性、エネルギー収支、必要自由度の規模依存性を評価する |

### 現在地

この表は論文と現行モデルの更新に合わせて変更する。「達成」は達成判定基準を満たした状態、「部分達成」は限定されたモデルまたは一部の構成要素で達成した状態、「未達」は理論上の作業対象だが基準を満たしていない状態、「未着手」は具体的なモデルまたは検証がまだない状態を表す。達成した目標も固定一覧から削除せず、達成状態と根拠を保存する。

| 目標ID | 現在地 | 根拠となる結果 | 残る課題 |
|---|---|---|---|
| Q1 | 部分達成 | R9、R12、R26 | coherent縮約多様体の準備・維持、M0からの停留点選択、配置拡散経路との同時実現 |
| Q2 | 部分達成 | R24 | 任意基底、一般測定器、共通流束因子のミクロ導出、標本化後の完全周期 |
| Q3 | 部分達成 | R27 | 節の生成・消滅、正則化極限、全ての物理的流れの単価な場からの準備 |
| Q4 | 未達 | — | 一般状態の重ね合わせ構造と干渉項の導出 |
| Q5 | 部分達成 | R14、R17–R19 | 一般的な複合状態の構成と局所測定への拡張 |
| Q6 | 未達 | — | 2種の標準ポテンシャルに対する定量検証と状態選択 |
| Q7 | 未達 | — | 透過率の定量導出と熱活性化との識別 |
| Q8 | 未達 | — | 駆動2準位系の明示モデルと定量検証 |
| Q9 | 未達 | — | 準備、伝播、検出を含む明示モデル |
| Q10 | 部分達成 | R14、R17–R19 | Tsirelson 限界、偏った準備での非信号性、完全な試行周期、M0からの力学的導出 |
| Q11 | 未達 | R20、R28 | 結果形成、再埋め込み、明反応座標の復元、記録、消去、再初期化の統合 |
| Q12 | 未達 | R11、R12、R23 | 係数整合、単一作用分配方向、共通機構が成立するモデル範囲の証明 |
| D1 | 未着手 | — | 装置原理と状態符号化の設計 |
| D2 | 未着手 | — | 単一量子ビット型操作の実装 |
| D3 | 未着手 | — | 複合状態と結合操作の実装 |
| D4 | 未着手 | — | 完全な連続動作周期の実装 |
| D5 | 未着手 | — | 性能指標と必要資源の測定 |

## 1. モデルの運用状態

| 識別 | モデル | 運用状態 | 役割と限界 |
|---|---|---|---|
| M0 | 粒子・装置・構造化2成分誘導場・外部流路を持つ弱開放系 | 現行モデル | 位相接続、配置拡散浴、境界作用殻を固定部分空間へ置く共通アーキテクチャ。全周期と一様縮約定理は未完成 |
| M1 | 1試行内の有限閉鎖系 | 補助モデル | 正準写像、Liouville モーメント、保存量、作用殻体積を厳密に計算する |
| M2 | 運動量結合した線形誘導場 | 補助モデル | 配置速度揺らぎと反作用記憶項を、初期値または二側境界条件の下で正確に分ける |
| M3 | 配置変数の二側 Markov 拡散 | 補助モデル | 前進・後退流れ、Fisher 項、時間対称平均加速度を整理する。M0からの導出は未達 |
| M4 | 線形 Fourier–Gauss 型確率表示 | 補助モデル | 作用形式の $C^1$ 収束を検証する。M0の中心モデルではない |
| M5 | Gauss 幅の Routh 縮約 | 補助モデル | 固定内部作用と Fisher 項の一致を示す可解例 |
| M6 | 最小結果符号化器 | 補助モデル | 既存の2値符号を伝達ベクトルと固定指針へ写す。一般測定器ではない |
| M7 | 3モード境界作用殻 | 補助モデル | 共通作用殻、和モード周辺、残余ファイバー体積を厳密に計算する |
| M8 | U(3) 等方殻拡散 | 補助モデル | 共通殻 Liouville 測度を一意な定常分布として与える。M0からのミクロ導出は未達 |
| M9 | 残余2モードの U(2) 混合 | 補助モデル | ファイバー内部の一様作用分配を示し、全殻測度には不足することを比較する |
| M10 | OU 位相表示 | 補助モデル | 弱開放部分系の緩和と回転の例。詳細は `notes/gaussian_nelson_examples.md` |
| M11 | 終端関数、中央比較器、相補時計による Bell 重み | 不採用モデル | Bell 固有の終端統計入力を必要とする。`notes/superseded_terminal_function_model.md` |
| M12 | 順時間的共有浴、結果依存滞在時間、相補終端半空間 | 不採用モデル | 試行頻度を得ない、事後選別になる、または余弦項を消す。詳細は `notes/` |
| M13 | 位置結合から力密度を Fisher 応力へ閉じる経路 | 不採用モデル | Fisher 項へ遠回りで、未証明の構成則を必要とする。`notes/superseded_position_coupling_fisher_closure.md` |
| M14 | 位相接続2成分活性場 | 補助モデル | coherent縮約多様体上で Nelson--Madelung 作用と条件付き循環量子化を与える。多様体の準備は未達 |
| M15 | 2モード境界作用殻による位置入口標本化 | 補助モデル | 共通流束因子の下で Born 型位置重みを与える。一般測定器と再埋め込みは未達 |

M0が現在の主モデルである。M0の固定射影、外部流路、配置拡散、局所測定器、境界殻準備、境界適合、消去、再初期化を1本の有限幅 Hamiltonian へ統合した完成モデルはない。

M1からM10、M14、M15の厳密結果は、それぞれの補助モデルまたは縮約方程式の内部での結果である。M0から各補助モデルへの接続が未完成なら、M0全体から導出済みとは呼ばない。

## 2. 共通ミクロ構成の導出結果

### R1 固定した明・暗モード基底

- 導出状態：**厳密結果**
- 対象モデル：M0、M1、M2

Fisher 側の運動量結合方向 $\operatorname{Ran}C_N^{\mathsf T}$ と、Bell 側の装置座標結合方向 $\operatorname{Ran}B_q$ を含む固定明部分空間を選べる。直交行列 $O_N$ を場座標と場運動量へ同時に作用させる変換は正準である。

暗モードを直接駆動しない条件は

```math
P_{\rm D}C_N^{\mathsf T}
=
0,
\qquad
P_{\rm D}B_q
=
0
```

である。$K_N$ と $M_N^{-1}$ による間接伝播は一般に残る。

### R2 固定位相整合射影

- 導出状態：**厳密結果**
- 性質：成立条件
- 対象モデル：M0、M1、M2

位相整合射影 $P_{\rm c}$ と欠陥射影 $P_\perp$ は、$K_N$ と $M_N$ の固定スペクトル帯、$C_N^{\mathsf T}$ と $B_q$ が生成する Krylov 部分空間、保存作用、装置構造から事前に定める。

得られた密度または目標量子状態を見て射影を選ぶ構成は採用しない。射影の正準性は、欠陥成分だけの減衰、配置 Markov 性、局所セクターの分離を意味しない。

### R3 選択的な欠陥減衰

- 導出状態：**予想・未解決**
- 性質：中心課題
- 対象モデル：M0

外部流路を $P_\perp$ へ強く、$P_{\rm c}$ へ弱く結合し、欠陥成分だけを速く除去する候補を示す。指数減衰率、流入補正、位相整合成分の寿命を、特定の外部スペクトルから一様に導く定理はない。

## 3. Fisher 側の導出結果

### R4 運動量結合 Hamiltonian の成立条件

- 導出状態：**厳密結果**
- 性質：成立条件
- 対象モデル：M0、M1、M2

運動量2次形式

```math
\frac12
\begin{pmatrix}
P\\
\Pi
\end{pmatrix}^{\mathsf T}
\begin{pmatrix}
m^{-1}I_d & C_N\\
C_N^{\mathsf T} & M_N^{-1}
\end{pmatrix}
\begin{pmatrix}
P\\
\Pi
\end{pmatrix}
```

は

```math
M_N^{-1}
-
mC_N^{\mathsf T}C_N
>
0
```

のとき正定値である。運動量の同時反転で偶なので、他の項も時間反転不変なら有限閉鎖核は時間反転対称である。

### R5 配置流束と1次モーメント

- 導出状態：**厳密結果**
- 対象モデル：M0、M1、M2

線形核の配置速度を

```math
U_N
=
\frac Pm
+
C_N\Pi
```

とすると、全 Liouville 密度から

```math
\partial_t\rho_N
+
\nabla\cdot
\left(
\rho_Nv_N
\right)
=
0,
\qquad
v_N
=
\mathbb E_N
\left[
U_N
\mid
X=x
\right]
```

を得る。正準運動量の輸送流束は $\mathbb E_N[P\otimes U_N\mid X=x]$ を含む。配置速度の1次モーメントには、場の加速度項と条件付き速度共分散が現れる。$P/m$ だけを配置速度とする旧式は使わない。

### R6 線形誘導場の正確な消去

- 導出状態：**厳密結果**
- 性質：指定初期値または指定境界条件の下で成立
- 対象モデル：M2

質量規格化した線形誘導場では、

```math
Y_N(t)
=
Y_N^{\rm free}(t)
-
\int_0^t
C_N\Omega_N
\sin
\left[
\Omega_N(t-s)
\right]
C_N^{\mathsf T}P(s)
\,\mathrm ds
```

と分けられる。第1項は自由速度揺らぎ、第2項は反作用速度記憶である。指定した Gauss 型初期集団では、

```math
R_N(t-s)
=
\Theta_NC_N
\cos
\left[
\Omega_N(t-s)
\right]
C_N^{\mathsf T}
```

を得る。有限 $N$ では相関は余弦関数の有限和であり、厳密な OU 相関ではない。自己共役な二側境界条件では Green 核が時間交換対称になる。

### R7 配置変位の Brown 極限

- 導出状態：**予想・未解決**
- 性質：中心課題・近似結果として示すべき対象
- 対象モデル：M0から有効位相空間過程への接続

目標は、再帰前の窓で

```math
\int_0^t
Y_N^{\rm free}(s)
\,\mathrm ds
\Longrightarrow
\sqrt{2\nu}\,W_t
```

を示すことである。多数モード、短記憶、スペクトル包絡、異方性、弱開放補正、有限再帰の評価は未完成である。反作用記憶項を質量繰り込み、局所ドリフト、制御可能な残差へ分ける必要もある。

### R8 配置変数の Markov 閉鎖

- 導出状態：**予想・未解決**
- 性質：中心課題
- 対象モデル：M0からM3への接続

配置雑音を持つ $(X,P)$ の位相空間過程が得られても、$X$ 射影は一般に Markov ではない。配置変数だけの前進・後退 Markov 拡散、共通拡散係数 $\nu$、小さい非 Markov 残差を同じ極限から示す必要がある。条件付き速度分散が余分な古典圧力を残さない条件も未完成である。

### R9 二側配置拡散の Fisher 構造

- 導出状態：**厳密結果**
- 性質：仮説依存
- 対象モデル：M3

共通の正の密度と共通の等方拡散係数 $\nu$ を持つ前進・後退 Markov 拡散では、

```math
v
=
\frac{b_++b_-}{2},
\qquad
u
=
\frac{b_+-b_-}{2}
=
\nu\nabla\log\rho
```

が成立する。従って

```math
\frac m2
\int
\rho|u|^2
\,\mathrm dx
=
\frac{m\nu^2}{2}
\mathcal I[\rho]
```

である。量子ポテンシャルと Fisher 応力は

```math
Q[\rho]
=
-2m\nu^2
\frac{\Delta\sqrt\rho}{\sqrt\rho},
```

```math
P_F[\rho]
=
-m\nu^2\rho
\,\nabla\nabla\log\rho
```

であり、

```math
-\nabla\cdot P_F[\rho]
=
-\rho\nabla Q[\rho]
```

が厳密に成立する。

### R10 時間対称動力学

- 導出状態：**予想・未解決**
- 性質：中心課題
- 対象モデル：M0からM3への接続

配置拡散だけから

```math
ma_{\rm ts}
=
-\nabla V
```

は従わない。時間対称 Green 応答、反作用記憶、条件付き変分からこの Newton 則へ収束することを示す必要がある。これが成立し、$v=\nabla S/m$ と書ける領域では $\hbar_{\rm eff}=2m\nu$ の局所 Schrödinger 表示を得る。Wallstrom 問題は含まない。

### R11 Gauss 幅の Routh–Fisher 一致

- 導出状態：**厳密結果**
- 性質：成立条件・補助検証
- 対象モデル：M5

1次元 Gauss 密度では $\mathcal I[\rho_\sigma]=1/\sigma^2$ である。2次元内部座標の循環作用を $J=m\nu$ に固定して Routh 縮約すると、

```math
-\frac{J^2}{2m\sigma^2}
=
-\frac{m\nu^2}{2}
\mathcal I[\rho_\sigma]
```

となる。配置拡散極限または時間対称 Newton 則を導くものではない。

### R12 線形 Gauss 型作用の <i>C</i><sup>1</sup> 収束

- 導出状態：**厳密結果**
- 性質：成立条件・補助検証
- 対象モデル：M4

有限 Fourier–Gauss 型駆動、線形流れ、Gauss 初期分布、正定値の有限分解能終端記録、2次ポテンシャル、滑らかな有限次元パラメータ族について、

```math
\left\|
\mathcal A_{N,h}^{R,U}
-
\mathcal A_{\rm GM}^{R,U}
\right\|_{C^1(K)}
\leq
C_K
\left(
\frac hT
+
\frac{T^2}{Nh^2}
\right)
```

が成立する。Guerra--Morato 表示と Nelson 表示は正の密度領域で厳密に一致する。

M0からM4を導くこと、微視的運動が作用停留点を選ぶこと、R7、R8、R10を証明することは含まない。

## 4. Bell 側の導出結果

### R13 局所セクターの近似分離

- 導出状態：**近似結果**
- 性質：成立条件・適用限界
- 対象モデル：M0、M1

測定窓の全交差応答比 $\varepsilon_{\rm loc}\ll1$ なら、左右局所記録を $O(\varepsilon_{\rm loc})$ の範囲で分離して扱う。全応答には座標–座標核と運動量–座標混合核を含む。結合ベクトルの直交性だけでは十分でない。

### R14 和・差作用の余弦幾何

- 導出状態：**厳密結果**
- 対象モデル：M1、M6、M7

位相同期した実2次元伝達ベクトルについて、

```math
I_\pm^{AB}
=
I_0
\left[
1
\pm
ABV\cos\Delta_{ab}
\right],
\qquad
I_+^{AB}
+
I_-^{AB}
=
2I_0
```

を得る。

### R15 U(2) 等方性の不足

- 導出状態：**厳密結果**
- 性質：否定的結果
- 対象モデル：M9

残余2モードの $U(2)$ 不変性は、固定 $J_+$ のファイバー内部を一様化するだけで、異なる $J_+$ のファイバー間質量を決めない。

### R16 U(3) 共通殻測度と等方拡散

- 導出状態：**厳密結果**
- 性質：仮説依存
- 対象モデル：M7、M8

$U(3)$ 不変な固定作用殻測度は一意である。縮約方程式

```math
\partial_tf
=
D_\partial\Delta_{S^5}f
```

の一意な定常確率密度は共通殻上の定数密度である。

M0からこの生成子を導くことは未完成である。常時の弱漏れは殻方向の等方化ではなく、欠陥除去、再帰抑制、半径分布の維持を担う。

### R17 共通殻の周辺と残余ファイバー

- 導出状態：**厳密結果**
- 性質：成立条件
- 対象モデル：M7

共通作用殻 $J_++J_s+J_r=C_0$ の正規化 Liouville 測度について、

```math
p_+(x)
=
\frac{2(C_0-x)}{C_0^2}
```

を得る。共通 Jacobian で $J_+=I_+^{AB}$ を課した残余ファイバー体積は

```math
W_{AB}
\propto
C_0-I_+^{AB}
=
J_*+I_-^{AB}
```

となる。

### R18 Bell 型共同確率

- 導出状態：**厳密結果**
- 性質：仮説依存・成立条件
- 対象モデル：M6、M7、M8

共通作用殻、等しい結果セクター基準質量、共通の境界分解能と coarea Jacobian、固定総入力作用の下で、

```math
P(A,B\mid a,b)
=
\frac14
\left[
1
-
V_{\rm eff}AB\cos\Delta_{ab}
\right],
\qquad
V_{\rm eff}
=
\frac{I_0}{J_*+I_0}V
```

を得る。

### R19 履歴測度と Bell 前提監査

- 導出状態：**厳密結果**
- 性質：Bell 前提監査
- 対象モデル：M7

履歴測度は、全境界正準位相空間の Liouville 測度を作用保存と境界適合条件で制限し、解写像で許容履歴空間へ押し出す。解空間へ直接 Liouville 測度を置くものではない。

境界適合条件が $a,b$ に依存するため、完全履歴分布は一般に測定設定独立性を満たさない。対称セクターでは一側周辺は $1/2$ である。偏った準備での一般的な非信号性は未証明である。

### R20 一般測定器と試行周期

- 導出状態：**予想・未解決**
- 性質：中心課題
- 対象モデル：M0

現在の明示装置は既存の2値符号を記録する最小結果符号化器である。結果形成、増幅、安定記録、境界適合、消去、再初期化を、開始数と記録数を減らさない1周期として統合していない。

## 5. 位相接続と Born 型入口標本化の導出結果

### R21 位相接続 Hamiltonian と保存位相作用

- 導出状態：**厳密結果**
- 性質：成立条件
- 対象モデル：M14

有限セル2成分場では、

```math
\sum_i
\boldsymbol\Pi_i\cdot d\boldsymbol\Phi_i
=
\sum_i
\left(
p_{r,i}\,dr_i+j_i\,d\theta_i
\right)
```

が成立する。共通内部回転の生成子

```math
\mathcal J_\phi
=
\sum_i j_i\Delta V
```

は、全 Hamiltonian が共通回転不変なら保存される。正則化接続

```math
\mathbf a_\varepsilon
=
\frac{
\Phi_1\nabla\Phi_2
-
\Phi_2\nabla\Phi_1
}{
|\boldsymbol\Phi|^2+\varepsilon^2
}
```

と

```math
H_{\rm p}
=
\frac{
\left|
P-\mathcal J_\phi\mathbf a_\varepsilon(X)
\right|^2
}{
2m
}
+
V(X)
```

は共通内部回転に不変である。標準時間反転では $P$ と $\mathcal J_\phi$ が同時に反転するため、両符号sectorを含む全理論は時間反転対称である。

### R22 固定作用下の局所作用分配

- 導出状態：**厳密結果**
- 性質：エネルギー最小配置
- 対象モデル：M14

規格化 $\sum_i r_i^2\Delta V=1$ と固定全作用 $\sum_i j_i\Delta V=\mathcal J_\phi$ の下で、

```math
E_{\rm rot}
=
\frac{\mathcal J_\phi^2}{2I}
+
\sum_i
\frac{
\left(
j_i-\mathcal J_\phi r_i^2
\right)^2
}{
2Ir_i^2
}
\Delta V.
```

従って一意な最小配置は

```math
j_i
=
\mathcal J_\phi r_i^2.
```

閉鎖 Hamiltonian 流がこの配置へ吸引されることは含まない。

### R23 一般作用殻容量と作用分配次元の剛性

- 導出状態：**厳密結果**
- 性質：成立条件・否定的結果
- 対象モデル：M1、M7、M15

$n$個の複素作用モードの固定総作用殻容量は

```math
\Omega_n(A)
=
\frac{
(2\pi)^n
}{
(n-1)!
}
A^{n-1}.
```

活性モードへ作用を直接分配する明反応方向が $q$ 個なら、入口容量は $A^q$ に比例する。Born 型の線形重みには $q=1$ が必要である。

### R24 境界 Liouville 流束による Born 型入口密度

- 導出状態：**厳密結果**
- 性質：仮説依存・成立条件
- 対象モデル：M15

局所作用

```math
A_i
=
A_{\rm tot}r_i^2\Delta V
```

を、選択された活性モードと1つの共有明反応座標が分配する。入口チャンネルが排他的で、法線速度、障壁、coarea Jacobian、spectator因子が共通なら、

```math
P_i
=
\frac{A_i}{A_{\rm tot}}
=
r_i^2\Delta V,
```

```math
\rho_{{\rm in},i}
=
r_i^2.
```

これは位置入口分布に限定された Born 型結果であり、任意基底の一般 Born 則ではない。

### R25 coherent多様体上の密度同期差保存

- 導出状態：**厳密結果**
- 性質：仮説依存
- 対象モデル：M14

位相変分から

```math
\partial_tj
+
\mathcal J_\phi
\nabla\cdot(\rho v)
=
0
```

を得る。$j=\mathcal J_\phi r^2$ と粒子の連続の式の下で、

```math
\partial_t
\left(
r^2-\rho
\right)
=
0.
```

これは入口で作られた同期差の中立的保存であり、同期多様体への吸引ではない。

### R26 縮約多様体上の Nelson--Madelung 作用

- 導出状態：**厳密結果**
- 性質：仮説依存・成立条件
- 対象モデル：M14

coherent集中、局所作用分配、密度同期、単流束化、動径断熱化、節から離れた接続極限の下で、

```math
\mathcal A_{\rm red}
=
\int
\left[
\frac m2\rho|v|^2
-
\rho V
-
\rho
\left(
\partial_tS+v\cdot\nabla S
\right)
-
\kappa
\left|
\nabla\sqrt\rho
\right|^2
\right]
\,dx\,dt,
```

```math
S=-\mathcal J_\phi\theta.
```

係数整合

```math
\kappa
=
\frac{\mathcal J_\phi^2}{2m}
```

の下で Nelson--Madelung 作用に一致し、

```math
\hbar_{\rm eff}
=
|\mathcal J_\phi|
```

の局所 Schrödinger 型方程式を得る。M0から縮約多様体を準備し、この作用の停留点を選ぶことは未完成である。

### R27 単価な2成分場による条件付き循環量子化

- 導出状態：**厳密結果**
- 性質：仮説依存・部分達成
- 対象モデル：M14

場が閉曲線上で非零かつ単価なら、

```math
\oint
\nabla\theta\cdot d\ell
=
2\pi n,
\qquad
n\in\mathbb Z.
```

従って

```math
\oint
\nabla S\cdot d\ell
=
-2\pi\mathcal J_\phi n.
```

節の生成・消滅、正則化極限、全ての物理的流れの準備は未完成であり、Wallstrom 問題の全面解決ではない。

### R28 coherent準備、等方化、再埋め込み、再初期化

- 導出状態：**予想・未解決**
- 性質：中心課題
- 対象モデル：M0、M14、M15

2モード入口殻の等方準備、全チャンネルでの共通流束因子、標本化後の結果記録、活性場の coherent部分空間への再埋め込み、共有明反応座標の復元、garbage自由度の処理を、事後選別なしの有限 Hamiltonian 周期へ統合していない。

## 6. 否定的結果と限界

1. 運動量結合が配置速度揺らぎを作っても、その積分が Brown 運動へ収束するとは限らない。
2. $(X,P)$ の位相空間過程が Markov でも、$X$ だけの射影は一般に Markov ではない。
3. 二側配置拡散から Fisher 項が得られても、時間対称 Newton 則は自動的に従わない。
4. 自己共役 Green 核だけでは Nelson の時間対称平均加速度を導けない。
5. 選択的な弱漏れだけでは配置拡散または Fisher 項を導けない。
6. 残余2モードの U(2) 等方性だけでは、異なる $J_+$ のファイバー間質量を決められない。
7. 固定された正準接合部は Liouville 測度を保存するが、単一状態から一様殻測度を生成しない。
8. 純粋な一方向漏れは $C_0>0$ の一様作用殻を準備しない。
9. 有限閉鎖 Hamiltonian 流れは微細 Liouville 密度を一般に強収束させない。
10. 左右局所セクターを同じ誘導場へ置くだけでは、共通未来の境界条件を除けない。
11. 局所記録後の順時間的共有浴結合と結果依存滞在時間は、全試行を1回ずつ数えた結果頻度を変えない。
12. Bell 型余弦則は Born 則、Tsirelson 限界、Wallstrom 量子化を単独では導かない。
13. 閉鎖 Hamiltonian 流は、固定作用のエネルギー最小配置または coherent縮約多様体へ一般に吸引しない。
14. rank-one 2次モーメントだけでは、非線形位相接続の標本平均を閉じない。
15. $\kappa=\mathcal J_\phi^2/(2m)$ は内部回転対称性だけから従わない。
16. 密度同期差保存は、同期多様体への復元力ではない。
17. 直接作用分配方向が複数なら、入口容量は局所作用の高いべきになり、Born 型線形則を失う。
18. 位置入口の Born 型流束は、任意基底の一般 Born 則ではない。

## 7. 物理的解釈

- 構造化2成分誘導場は、位相接続経路では保存位相作用と物質微分結合の源、配置拡散経路では速度揺らぎと反作用記憶の源、境界作用殻経路では局所反応と殻接方向混合の源として使う。
- 常時の弱い漏れは、欠陥除去、再帰抑制、記録安定化を担う。配置拡散、Fisher 項、殻方向等方性を直接生む入力ではない。
- 二側条件付けは終端から過去へ力を送る記述ではなく、初期・終端の双方に適合する完結履歴の条件付き集団を表す。
- Fisher 項は、配置変数の二側 Markov 拡散が得られた後、前後ドリフト差 $u=\nu\nabla\log\rho$ から運動学的に生じる。
- Schrödinger 型PDEは、位相接続模型を coherent多様体へ制限した作用変分から生じる。実在的な Markov 経路の導出とは別である。
- Born 型位置重みは、2モード作用殻の容量と排他的入口面の Liouville 流束から生じる。
- Bell 重みは結果依存終端関数でなく、同じ共通作用殻を境界適合条件で切った残余ファイバー体積から生じる。

これらは R7、R8、R10、R16 のミクロ導出や、R20、R28 の完全周期を証明しない。

## 8. 最重要の未解決問題

1. 2モード入口作用殻を、局所作用 $A_i$ と共通流束因子で偏りなく準備する。
2. 標本化後の結果情報を保存しつつ、活性場を coherent部分空間へ再埋め込みする。
3. 共有明反応座標、記録、garbage自由度を事後選別なしで復元し、次試行へ戻す。
4. coherent多様体を有限 Hamiltonian 時間発展から準備・維持し、縮約作用の停留点選択を示す。
5. 節を含む位相接続、循環量子化、密度同期を制御する。
6. 運動量結合した有限誘導場から Brown 極限と配置 Markov 閉鎖を導く。
7. 反作用記憶を質量繰り込み、局所ドリフト、制御可能な残差へ分ける。
8. 条件付き速度分散が余分な古典圧力を残さない条件を示す。
9. 位相作用と浴拡散係数の整合 $|\mathcal J_\phi|=2m\nu_{\rm bath}$ を同じ模型から導く。
10. 同じ誘導場の運動量–座標混合核を含む全交差応答を評価し、局所測定窓で抑える。
11. 同じ誘導場から U(2) と U(3) の必要な殻接方向生成子を導き、異方誤差、混合時間、再帰時間を定量化する。
12. 常時の弱い漏れと流入から、狭い作用殻の半径分布とエネルギー収支を導く。
13. 設定、到来変数、装置微視状態から結果を形成する一般測定相互作用を構成する。
14. 任意基底へ Born 型入口標本化を拡張する。
15. 偏った準備装置まで含めても非信号性が回復する条件を判定する。
